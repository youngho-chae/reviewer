# CATCHPASS · 요구사항정의서 (PRD) — VER.1 MVP (v3.1)

> **문서 성격**: 본 PRD는 **제품 요구사항(무엇을·왜)** 만 다룬다. 세부 명세는 성격별 문서로 분리한다.
>
> | 문서 | 내용 | 경로 |
> |---|---|---|
> | **요구사항정의서(본 문서)** | 비전·문제·해결·범위·페르소나·BM·BI·로드맵·성공지표 | `PRD.md` |
> | **기능정의서** | 화면별 기능 요구·정량 제약·디자인 의도·상태머신·API·디자인 시스템 | [`docs/기능정의서.md`](docs/기능정의서.md) |
> | **운영정책서** | 검수·정산·멤버십·바이럴 재원·법무·출시 보안 체크리스트 | [`docs/운영정책서.md`](docs/운영정책서.md) |
> | **데이터정책서** | 데이터 모델·영속성·시드·개인정보 수집/보관/파기·토큰 규칙 | [`docs/데이터정책서.md`](docs/데이터정책서.md) |
> | **IA(정보 구조)** | 화면 인벤토리·라우트·BottomNav·네비게이션 | [`docs/IA.md`](docs/IA.md) |
> | **유저 시나리오** | 액터별 여정(플로우) A~G | [`flow.md`](flow.md) |
> | **화면설계서** | Figma 스토리보드 | [`docs/sdd/README.md`](docs/sdd/README.md) |
>
> 모든 문서는 **구현 코드(브랜치 `mvp/ver1`)와 1:1** 이며, UI 카피·서버 응답 원문은 큰따옴표로 표기하고 임의 변경을 금한다.
> **VER.1 출시 범위**: 결제(PG)는 직접 연동 예정으로 본 범위에서 제외 — 연동 지점만 §7(BM)에 명시.

---

## 0. 한 줄 소개

> **"선정 기다리는 체험단 말고, 등급으로 받는 체험권."**
> 리뷰어는 자기 등급에 맞는 체험권을 즉시 발급받고, 사장님은 멤버십 플랜의 월 한도만큼 모집한다.

CATCHPASS는 모바일 우선 웹앱(Next.js 15 App Router)으로 구현된 체험 마케팅 모듈이다.
체험자와 사장님은 같은 데이터(매장·캠페인·체험권)를 양쪽에서 보지만, 진입 경로(`/r/...` vs `/o/...`)와 권한은 분리된다(→ IA).

| 역할 | 진입 | 인증 |
|---|---|---|
| 체험자(리뷰어) | `/r/login` · `/r/signup` | 이메일+비밀번호 + SNS 채널(URL+영향력 직접 입력) + **약관·개인정보 필수 동의** |
| 사장님 | `/o/login` · `/o/signup` | 이메일+비밀번호 + **약관·개인정보 필수 동의** |
| 운영팀 | `/admin/login` | 이메일+비밀번호 (검수 백오피스 전용) |

> ※ OAuth·외부 SNS API·결제 게이트웨이(PG)는 VER.1 범위 외. SNS 영향력은 사용자가 직접 입력한 수치로 등급을 산정한다.

---

## 1. 배경 · 문제 · 해결

### 1.0 핵심 정책 원칙 (불변식 — 모든 문서·구현·검증의 최우선 기준)

> 아래 원칙과 모순되는 서술·구현·카피는 **그 자체로 결함**이다. 문서-코드가 서로 일치하더라도
> 이 원칙에 어긋나면 잘못된 것이며, 발견 즉시 정정한다.

| # | 원칙 |
|---|---|
| **P1** | **모든 등급(S+/S/A/B/C/N)은 모든 캠페인에 참여할 수 있다.** 등급은 참여 **자격이 아니라 혜택의 크기**(지원금 배율 S+·S100/A80/B60/C40/N10%)만 결정한다. "최소 참여 등급"·"등급 부족"·"OO등급 전용" 개념은 존재하지 않는다. 참여를 막는 조건은 ①캠페인 종료 ②잔여 슬롯 소진 ③(배송형) 참여 채널 미연동 — 이 3가지뿐이다. 방문형은 SNS 미연동도 **영수증 리뷰**(N등급 적용·지원금 10%)로 참여할 수 있다(2026-08-07 — 구 "방문형 채널 미연동 차단" 폐지). |
| **P2** | 방문형 지원금은 **매장이 결제 시 직접 할인**한다 — 회사는 정산·환급하지 않는다. |
| **P3** | 사장님은 후기를 **조회만** 한다 — 검수는 운영팀 단일 책임. |
| **P4** | 사용자에게 보이는 수치·사회적 증거는 **실제 발생 이벤트만** 집계한다. |
| **P5** | 발행하는 보상은 반드시 **실사용 경로가 구현**되어 있어야 한다. |

### 1.1 문제
기존 체험단 — 신청·심사 대기, 사장님의 직접 검수 부담, 광고표시·노쇼 마찰.

### 1.2 해결 방식
- **등급 기반 즉시 발급**: SNS 영향력 기반 S+/S/A/B/C/N 6단계(2026-08-06 개편). 누구나 **즉시 발급**(심사·선정 대기 없음), 등급은 받는 혜택의 크기를 결정.
- **멤버십 기반 대규모 모집**: 월 구독 플랜별 모집 한도(Free 5 ~ Premium 100팀 — 무제한 플랜 없음, 2026-07-31). 매장 할인은 손님에게 직접 제공.
- **운영팀 단일 검수 + 채널톡 문의**: 사장님은 후기 **조회만**. 재작성·수정 이의는 채널톡으로 운영팀 접수.
- **두 가지 캠페인**: 방문형(visit) + 자료팩 기반 비방문 기자단(press).

### 1.3 캠페인 타입

> **[MVP 범위 — 2026-07-07 회의 확정 · 2026-07-12 개정]** 1차 출시는 **방문형**을 핵심으로 하되, 레뷰 벤치마크
> ([`docs/벤치마크-레뷰.md`](docs/벤치마크-레뷰.md))에 따라 **배송형(delivery)을 범위에 추가**한다(`DELIVERY_ENABLED=true`).
> 서비스의 핵심 방향은 **"즉시 발급·자율 방문형 체험단"**이며, 기자단은 릴리스 브랜치에서 코드째 제거(2026-07-23, dev 전용),
> 예약 선정형은 방문형의 "방문 전 예약 필수" 옵션(라이트)으로만 반영하고 별도 선정 절차는 **추후 버전에서 확장**한다.

| 구분 | 방문형(visit) | 배송형(delivery — 2026-07-12 추가) | 기자단(press — MVP 제외) |
|---|---|---|---|
| 흐름 | 방문 → QR/4자리 코드 제시 → 결제 할인 → 리뷰 | 배송지 입력 신청 → 사장님 발송 → 수령 체험 → 리뷰(발송 후 7일) | 자료팩 수령 → 본인 채널 작성 → URL 제출 |
| 사장님 비용 | 멤버십 정액 + **지원금(매장 즉시 할인) 직접 부담** | 멤버십 정액 + 제품 원가·배송비 + 체험 포인트(선택 — 정산 모델 미확정, 운영정책서 §13 #17) | 종량제 선결제 |
| 정산 | 없음 — 지원금은 매장이 결제 시 직접 할인(회사 무정산, P2) | 포인트는 회사가 검수 승인 시 적립·출금 지급(**사업소득 3.3% 원천징수** — 운영정책서 §14) | 검수 통과 후 D+7 송금(3.3% 원천징수) |
| 대상 | 누구나 (등급은 지원금 크기만 결정 — P1) | 누구나 · 전국 (등급은 포인트 크기만 결정 — P1) | 누구나 + 자료팩 활용 |

### 1.4 등급 시스템 개요
6단계 **S+ → S → A → B → C → N** (2026-08-06 개편) — **등급이 높을수록 같은 캠페인에서 받는 혜택이 크다** (참여 범위는 등급과 무관 — P1).
채널별 독립 산정(네이버 블로그 가중치 1.2, 인스타/틱톡 1.0). '종합 등급' 기준은 없다 — 마이페이지 등 단일 표기는 연동 채널 중 가장 높은 등급을 표기한다(2026-07-10 정정).

```
S+ 계정(표기) 등급 전용 — 채널 S + 성실 이행·상생 만점 + 패널티 0인 달에만 (§운영정책서 10.6)
S  ≥ 500,000    A  ≥ 50,000    B  ≥ 10,000    C  그 외 연동 채널 (연동 = 최저 C)
N  SNS 미연동 전용 상태 — 채널을 1개라도 연동하면 최하라도 C
```

지원금 배율 S+·S100 / A80 / B60 / C40 / N10 (%). 실지급 = 기준 지원금 × 배율. **S+ 배율은 S와 동일 100%** — 기준 지원금이 절대 상한(P2)이라 초과 배율은 없고, S+의 추가 혜택은 배율 외 영역(포인트 적립 +10%·프로모션 최우선·골드 배지·검수 우선 처리).

**최초 산정과 월간 재평가 (2026-07-08 설계 · 2026-08-06 6단계 개편)**
- **최초 연동 시**: 캐치랭크가 보유한 **지수 평가 모델 v1**(채널 영향력 밴드 정규화, `indexScoreV1`)의 지수 단독으로 등급 산정. 위 임계값 표와 동치.
- **매월 말(KST) 재평가** — 오를 수도 내려갈 수도 있다. 직전 1개월 활동으로 채널별 **월간 등급 점수(GS)** 산출:
  `GS = 0.70×지수점수(I) + 0.20×성실 이행(F) + 0.10×상생지수(W) − 패널티(P)`
  등급 컷 S≥90 / A≥70 / B≥50 / C(연동 바닥 — N 컷 없음), 월 변동 폭 **±1등급**. **S까지 자동 평가**(구 "S 수동 부여" 폐기 — 2026-08-06). **S+**는 점수 컷이 아니라 조건 판정: 전월 표기 등급 S 이상 + 채널 최고 S + F·W 만점(중립 월 불가) + P 0인 달에만 부여, 매월 재판정(미충족·무활동 월은 S로).
- **리뷰 품질은 점수 요소에서 제외** (검수자 주관 개입 배제) — 품질 문제는 객관 사건인 **반려 종착(1회 재제출 후 추가 반려)**만 패널티로 반영.
- **패널티**: 노쇼 만료 −10/건 · 리뷰 기한 초과 −7/건 · 반려 종착 −5/건 (월 상한 −30, 연속 월 발생 시 ×1.5 가중). 취소는 무패널티(12h 재신청 제한이 담당).
- **상생지수(W)**: 지원금만이 아닌 **추가 결제**로 매장 수익에 기여한 정도. 단순 지출액 비례의 모순(등급 구매→배율 상승→차익 회수)을 막기 위해 ①자기 지원금 대비 **비율**만 사용 ②건별 상한 1.0 ③리뷰 **완료 건만** 집계 ④가중치 10% 상한. 한 달 W≥60 & 완료 3건↑이면 **상생 리뷰어 뱃지**(표시용 신뢰 표식 — 배율·참여 조건 무영향, 프로모션 발생 시 우선 지급 대상).
- **[P1 재확인]** 재평가·패널티·뱃지는 혜택 크기(지원금 배율)에만 영향 — 참여 자격은 어떤 경우에도 제한하지 않는다.

→ 산정·배율·채널 상세는 [`기능정의서.md`](docs/기능정의서.md) §8·[`운영정책서.md`](docs/운영정책서.md) §3, 재평가 전체 규정은 운영정책서 **§등급 월간 재평가**.

---

## 2. 사용자 페르소나

- **체험자(김리뷰)**: 30대, 자기 글의 영향력으로 자동 매칭받기를 원함. 떨어질 걱정 없이 등급으로 받는 안정감을 추구.
- **사장님(정사장)**: 매장 운영 중 모바일로만 확인. 매번 모집·심사·검수에 시간 뺏기는 것을 싫어함. 멤버십 한 번 결제 = 알아서 돌아가는 상태를 원함.

---

## 3. 제품 요구사항 (역할별 요약)

> 화면별 상세 기능·제약·디자인 의도는 [`기능정의서.md`](docs/기능정의서.md), 여정은 [`flow.md`](flow.md), 화면 구조는 [`docs/IA.md`](docs/IA.md).

### 3.1 체험자
- 가입 시 필수 동의 2종 + SNS 채널(블/인/틱) 입력 → 채널별 등급 자동 산정(연동 없이 N등급 시작 가능).
- 큐레이팅 홈에서 발견 — 상단 **현재 위치** 노출 + 프로모 배너 + 한눈 3정보(**이번 달 아낀 금액·작성할 리뷰·발급받은 체험권**).
  **지역 변경 시 그 지역 기준 좌표에서 반경 3km**(하버사인)로 '걸어서 갈 수 있어요'가 바뀌고,
  전국 체험단 리스트 기본 정렬은 **최신 등록순**. 홈 카드는 최소 정보만(SNS 채널·남은 수량·가게명·지원금).
  검색은 지역(주소)·매장·키워드(카테고리·강조 키워드) 전체 대응.
- 탐색은 **지도 보기가 기본** — 매장 선택 시 매장명·카테고리·거리·지원금 카드가 뜨고 좌우 스와이프로 거리순 다음 매장 탐색
  (채널·마감일·등급은 지도 카드에 미노출). 채널·카테고리는 **통합 필터 바텀시트**에서 다중 선택·즉시 반영·아이콘 초기화.
- **'이 지역 재검색'(2026-07-08 확정)**: 지도 이동마다 실시간 재호출하지 않는다 — 기준점에서 500m 이상 이동 시
  [↻ 이 지역 재검색] 버튼 노출, 탭하면 그 시점 지도 중심 **반경 3km**로 핀·카드를 다시 계산(거리순 재정렬, [전체 보기] 복귀).
- 매장 상세에서 **채널 선택 → 개인화 지원금** 확인 후 참여. 타 등급 최대 지원금 비교 문구는 노출하지 않는다.
  리뷰 조건은 사진 수·글자 수 등 핵심 중심으로 단순화(가변 데이터).
- 체험권 7-상태 라이프사이클. **발급 후 72시간 유효(연장·복구 불가)** · 리뷰는 **이용 후 7일 이내** ·
  사용 전 체험권 **동시 5장 제한** · **취소 후 동일 캠페인 12시간 재신청 제한**. 4자리 코드/QR로 사용 처리.
- 반려 시 사유 확인 + 7일 내 1회 재제출. active 상태 직접 취소 가능(슬롯 복구).
- **관심 목록**: 캠페인 단위 저장(♡), MY 하위 메뉴 진입, 진행 가능/마감 필터, 종료 캠페인은 "마감된 체험" 표기 유지(신청 불가).
- 등급 탭(정적 혜택)·혜택 탭(바이럴 보상)·MY(약관·회원 탈퇴).

### 3.2 사장님
- **사업자 인증 필수(확정 정책 9)**: 가입 시 사업자등록번호 10자리 → pending(인증 대기 화면) → 운영팀 수기 인증(영업일 2~3일) 후 "인증된 사장님"으로 화면 접근.
- 캠페인 생성: **캠페인명(내 관리용, 선택 — 미입력 시 매장명 자동, 체험자에게는 매장명 노출)** · 진행 일수 · 1인당 최대 지원금 ·
  총 모집 인원만 입력(**전 플랜 균등 자동 분배** — 우선 등급 배분 폐기) · 4자리 사용처리 코드(체험권 인증용 비밀번호) 필수 ·
  필수 채널 · 메뉴(+가격, **선택 입력·최대 5개** — 현장 강제 없음, 미이행은 운영 검수 처리) · 강조 키워드 · 매장 소개.
  등록 매장 선택 또는 **플레이스 URL로 매장 추가**(Free 플랜도 캠페인 생성 가능 — 프리 배제 금지).
- 월 모집 한도(플랜별, **팀 수 = 인원 수** 기준) 강제 + quota_bonus 가산. 사용 처리(QR/4자리). 후기 **조회 전용**(직접 검수 불가, 채널톡 문의). 성과 리포트·멤버십 관리.
- **등급·실명 비노출(확정 정책 8·10)**: 캠페인 현황은 방문 예정/방문 완료/총 모집 3종만 — 체험자 등급·닉네임은 어떤 화면·알림·리포트에도
  노출하지 않는다(익명 #last4). 부스팅(등급 우선 모집/노출)은 1차 미도입. 진입점·멤버십 변경은 캐치랭크 본체 연동 방향(연동 전 자체 로그인).
- **캠페인 노출·종료**: 발급 소진 ≠ 종료 — 발급은 끝났어도 살아있는(미사용) 체험권이 남아 있으면 계속 노출하고,
  모든 체험권이 실제 사용 완료돼 살아있는 체험권이 없을 때 종료·비노출한다. 대기(웨이팅)는 MVP 미포함.

### 3.3 운영팀
- 후기 검수 콘솔에서 통과/반려(사유 보존) 일괄 처리 + 양측 알림 — 검수 카드에 **강조 키워드·자가점검 칩**으로 수기 검수 보조(자동검수 미도입). reviewer/owner 화면·보상 대상 제외.
- **어드민 확장(확정 정책 12)**: 회원 조회(등급·월간 재평가 이력·작성 리뷰), 캠페인 목록(노출 상태·등급별 버킷은 내부 전용), 사장님 관리(사업자 인증 큐).

### 3.4 공통
- 법적 고지(약관·개인정보) 비로그인 접근. 바이럴 초대→환영 박스→양면 보상. 인앱 알림함 단일 채널.

---

## 4. 비기능 요구사항 (요약)
- 인증: JWT(jose) HS256 httpOnly·secure 쿠키 30일. 비밀번호 bcrypt 해시.
- 영속성: 3단(메모리→`/tmp`→Vercel KV). 운영은 KV 필수. DB 로드마다 라이프사이클 스윕.
- 개인정보 노출 통제: 사장님·검수 화면은 익명 #last4만.
- 데이터 진실성: 사용자 노출 수치는 실제 발생 이벤트만(허위·조작 금지).
- 성능/반응형: 리스트 SSR, 뷰포트 480px 기준, 탭 타겟 44px, WCAG AA.
→ 상세 [`기능정의서.md`](docs/기능정의서.md) §7.3 · [`데이터정책서.md`](docs/데이터정책서.md).

---

## 5. 외부 의존성 · 통합

| 통합 | 상태 | 비고 |
|---|---|---|
| Naver Maps SDK / Reverse Geocode | ✅ | 지도·현재 위치 라벨 |
| Naver Place 데이터 자동 갱신 | 부분 | 컨테이너 네트워크 정책 영향 가능 |
| 카카오 채널톡(ChannelIO) | UI 통합 | 미배포 시 mailto 폴백 |
| Vercel KV(Upstash Redis REST) | 어댑터 준비 | 미연결 시 메모리/디스크 폴백 |
| qrcode / html5-qrcode / bcryptjs | ✅ | QR 렌더·스캔·해시 |
| OAuth(SNS) | ❌ 미구현 | URL+영향력 직접 입력 |
| 결제 게이트웨이(PG) | 🔜 직접 연동 예정 | 연동 지점 §7 — VER.1 범위 외 |
| Push 알림 | ❌ 미구현 | 인앱 알림함 단일 채널 |

---

## 6. 출시 범위 · 로드맵

- **Phase 1 — VER.1 MVP(완료)**: 전 플로우 + 운영팀 검수 백오피스 + 법적 고지(약관·개인정보·탈퇴) + 라이프사이클 자동화(만료 복구·취소·재제출·기한 강제) + 실사용 가능한 바이럴 보상. **결제(PG)만 연동하면 출시 가능.**
- **Phase 2 — 결제/정산**: PG 직접 연동(멤버십 구독·기자단 선결제·membership_discount 소진), 정산 자동화.
- **Phase 3 — 확장**: SNS OAuth, 4지표 등급 자동 재산정, OS 푸시, 채널톡 실 연결, 매장 셀프 온보딩.

**미해결/로드맵 항목**: 4지표 30일 자동 재산정, 가입 후 SNS 채널 추가, OAuth 하이브리드, T4 트리거(사장님 캠페인 생성 직후 초대), 매장 셀프 추가/편집·캠페인 수정. (상세 상태는 각 정책서)

---

## 7. 비즈니스 모델 (BM)

### 7.1 수익 구조
| 수익원 | 고객 | 가격 | 결제(VER.1) |
|---|---|---|---|
| 멤버십 구독(주 수익) | 사장님 | Free ₩0 / Basic ₩13,900 / Standard ₩25,900 / Premium ₩38,900 (월) | PG 연동 전 운영팀 수기 청구(미납 시 Free 조정) |
| 기자단 종량제 | 사장님 | 캠페인별 선결제(정산 예정금 + 수수료) | 운영팀 처리 |

### 7.2 비용 부담 3자 구조 (분쟁 방지 핵심)
```
사장님 ──(멤버십 구독료)──▶ 회사
사장님 ──(지원금 = 매장 즉시 할인)──▶ 체험자   ← 회사 미경유. 정산·환급 없음.
회사   ──(기자단 정산금, 3.3% 원천징수)──▶ 체험자   ← 재원 = 사장님 종량제 선결제
회사   ──(바이럴 보상: 멤버십 할인·quota 보너스)──▶ 회원   ← 마케팅 비용
```
방문형 지원금은 매장이 결제 시 직접 할인 — 사장님 폼·체험자 카드·약관 3중 명시. 부스트(+N%)는 기준 지원금(S100%) 상한 내라 매장 초과 부담 없음. (재원·정산 SOP → [`운영정책서.md`](docs/운영정책서.md) §4·§6)

### 7.3 PG 연동 지점 (직접 연동 예정 — 코드 훅 위치)
1. `POST /api/owner/plan` — 결제 완료 후 plan 반영으로 전환(현재 즉시 반영 + 수기 청구)
2. 멤버십 결제 시 미사용 `membership_discount` 소진
3. 기자단 캠페인 생성 시 선결제 게이트
4. `/o/membership` 결제 내역 → 실 결제 이력 연동

---

## 8. 브랜드 아이덴티티 (BI)

- **회사(운영 주체): 캐치랭크(CatchRank)** — 고객센터 `help@catchrank.co.kr`, 운영팀 `admin@catchrank.co.kr`. 광고 표시 문구·법적 고지 주체.
- **제품(서비스명): 캐치패스(CATCHPASS)** — 캐치랭크가 운영하는 리뷰 체험 플랫폼. 앱 로고/헤더/온보딩.
- 관계: `캐치랭크(회사) ─운영─▶ 캐치패스(제품)`. UI 브랜딩은 CATCHPASS, 대외 책임 주체(약관·광고 문구·이메일 도메인)는 캐치랭크. 혼용 금지.
- 브랜드 보이스: 제품 UI는 **퍼플 커머스 톤(디자인 시스템 v2 — 정본 `DESIGN.md`, Pretendard·#9333EA·파스텔 SNS 배지)** + 체험자 측 B급 감성 카피(→ 기능정의서 §7.4·§7.6). 법적 문서(/legal)는 표준 존댓말. (v1 Apple 톤은 파기됨)
- 슬로건: "선정 기다리는 체험단 말고, 등급으로 받는 체험권."

---

## 9. 성공 지표 (OKR 개요)

- **Acquisition(3개월)**: 체험자 20k / 사장님 10k. 이후 자체 바이럴로 각 2배 목표.
- **바이럴 게이트**: 1개월차 K-factor ≥ 0.5.
- **Revenue**: 멤버십 구독 전환 — **PG 연동이 매출 OKR의 선행 조건**(연동 전 자동 수취 ₩0, 수기 청구만).
- **품질**: 리뷰 작성 완료율·광고표시 준수율·검수 통과율.
> OKR 세부 목표·측정은 별도 그로스 트랙에서 관리.

---

## 10. 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| **v3.7** | 2026-07-12 | **레뷰(revu.net) 벤치마크 반영** ([`docs/벤치마크-레뷰.md`](docs/벤치마크-레뷰.md)). ① §1.3 캠페인 타입에 **배송형(delivery)** 추가 — 배송지 신청·발송 처리·발송 후 7일 리뷰, 지역 무관 전국 참여. ② **체험 포인트** 도입 — 배송형 리뷰 검수 승인 시 적립(등급 배율 — P1), 출금(최소 1만P·수수료 500원·**사업소득 3.3% 원천징수·소액부징수**)으로 실사용 경로 충족(P5). 정본: 운영정책서 §14. ③ 방문형 "방문 전 예약 필수" 옵션(예약형 라이트). ④ 구매평(페이백)·쇼핑 연동은 가구매·P4 리스크로 **보류**(운영정책서 §13 #18). SEED_VERSION 1019. |
| **v3.6** | 2026-07-08 | **최종 확정 정책사항 반영 (`dev` 브랜치).** ① 홈: 3-stat을 확정 3정보(아낀 금액/작성할 리뷰/발급받은 체험권)로 정정, **지역 변경 = 기준 좌표 반경 3km**(하버사인, `geo.ts` — 항상 빈 결과이던 문자열 매칭 버그 해소). ② 검색: 지역(주소)+키워드(강조 키워드)+매장 전체 검색, `?q=`. ③ **'이 지역 재검색' 신설** — 500m 이동 감지 → 버튼 → 지도 중심 반경 3km 재계산(실지도 dragend/데모 지도 동일 UX). ④ **사장님 등급·실명 전면 비노출**(홈 버킷·로그·후기·스캔(실명 정정)·알림·리포트 ROI 제거 — 익명 #last4, 내부 데이터는 어드민 전용) + **부스팅 폐기**(priorityGrade·"A/S 우선" 표기 제거, 균등 배분). ⑤ 캠페인: **캠페인명 부활**(내 관리용·체험자는 매장명), 필수 메뉴 **선택 입력·최대 5개**, **플레이스 URL 매장 등록**(Free 배제 금지, scrapePlace 재사용+수동 폴백). ⑥ **사업자 인증**(가입 필수 10자리 → pending 대기 화면 → 운영팀 수기 인증 → "인증된 사장님"). ⑦ **어드민 확장**(탭/회원(등급·재평가 이력·리뷰)/캠페인(내부 버킷)/사장님 인증 큐 + 검수 키워드 보조). ⑧ 등급: 최초 100% 지수·둘째 달부터 활동 반영·월 1회 익월 적용은 현행 유지 확인, **N등급 현행 유지**(지시). 미확정 항목은 운영정책서 §13 제안으로 정리. SEED_VERSION 1016. |
| **v3.5** | 2026-07-08 | **등급 월간 재평가 시스템** — §1.4 재작성(캐치랭크 지수 평가 모델 v1·GS 공식·상생지수·패널티·상생 리뷰어 뱃지). 상세는 운영정책서 §10. |
| **v3.4** | 2026-07-07 | **회의 확정 사항 반영.** ① MVP 범위: 방문형만(기자단 제외 — `PRESS_ENABLED` 게이트, 예약형은 추후). ② 홈·탐색: 현재 위치 노출+지역 변경(‘걸어서 갈 수 있어요’(3km)만 변경), 전국 리스트 최신 등록순, 탐색 기본 지도 보기+거리순 스와이프 카드(매장명·카테고리·거리·지원금만), 통합 필터 바텀시트(채널·카테고리 다중 선택·즉시 반영·아이콘 초기화, 선택 채널 상단 미노출). ③ 체험권: 유효기간 24h→**72h**(연장·복구 불가), 리뷰 **이용 후 7일**(상세는 공통 문구, 보유 화면은 실제 마감·카운트다운), active 동시 보유 5장, 취소 후 동일 캠페인 12h 재신청 제한. ④ 노출: 발급 소진 ≠ 종료(살아있는 체험권 기준, `campaign-visibility.ts`), 웨이팅 미포함. ⑤ 상세: 메뉴 가격 표시, 리뷰 조건 핵심 단순화(가변 데이터), 타 등급 최대 지원금 비교 문구 제거, 플레이스 링크 현행(좌측 정렬). ⑥ **관심 목록** 신설(캠페인 단위·MY 진입·진행/마감 필터·마감 표기 유지). ⑦ 스키마 표기 형식 마스크 전환(`000,000원`/`00건`/`00개` — SBUI + 실데이터 동일 형식, ₩ 접두 폐지). v3.3의 "시안 수치는 정책과 상이" 항목은 본 회의로 **시안 수치(72h/7일)가 정책으로 채택**되어 해소. SEED_VERSION 1011. |
| **v3.3** | 2026-07-07 | **디자인 시스템 v2 전면 교체 + UI 리디자인.** 첨부 "[체험단] 디자인시스템" Figma 파일 기준으로 `DESIGN.md` 전면 재작성(기존 Apple 톤 파기 — 구조는 동일 유지): Primary 퍼플 #9333EA·Pretendard·파스텔 SNS 배지(블로그 그린/인스타 핑크/틱톡 시안)·헤어라인 라운드 카드·11~22px 컴팩트 타이포. 프로토타입 전 화면 리디자인(스키마 라벨 모드 유지): 홈=위치 선택 바+프로모 배너(블루 슬롯)+내 활동 3-stat+2단 체험 카드, 탐색=방문형/기자단 세그먼트 타이틀+아이콘 카테고리 칩+"근처 체험 N개 발견!"+[📍 지도 보기] 검정 pill FAB+지도 바텀시트/선택 카드+2줄 핀 마커, 매장 상세=info-strip(체험 마감일/리뷰 기한/잔여)+퍼플 안내 배너+채널 라디오 카드(연동 완료/필요 칩)+필수 메뉴 하이라이트+스텝 타임라인+cta-bar(지원 금액+체험권 발급받기). BottomNav 홈/탐색(깃발)/체험권/혜택(선물)/마이. 시안의 정책 표기 충돌(72h 사용/7일 리뷰)은 실제 정책(24h 사용/72h 리뷰)을 따름. **화면설계서(Figma 보드)는 미반영(지시)** — v1 기준으로 유지. |
| **v3.2** | 2026-07-07 | **등급 정책 전면 정정 — "등급 = 참여 자격"이라는 잘못된 개념 제거 (P1 불변식 신설).** 실제 정책은 "모든 등급이 모든 캠페인에 참여할 수 있고 등급은 혜택(지원금 배율)만 결정"인데, 구현·문서 전반에 최소 등급 게이트가 존재했다. ① §1.0 핵심 정책 원칙(P1~P5) 신설 — 문서-코드 일치보다 상위의 검증 기준. ② 코드: `/api/passes` 등급 검증(403 "등급부터 이용 가능") 제거, `gradeMeets`·`eligible`(최소 등급 충족) 개념 삭제 — 자격 조건은 종료/채널 미연동/슬롯 소진 3가지뿐. quota 버킷은 자격이 아닌 **배분 기록**(자기 등급 버킷 우선, 소진 시 잔여 버킷). ③ UI: 홈·탐색·지도의 "등급 부족" 오버레이·자물쇠·흐림 처리 제거, 지도 핀 등급 인코딩 제거, 매장 상세·기자단 CTA의 "등급부터 참여 가능" 분기 제거, 등급 사다리를 "모든 캠페인 참여 · 배율 차등"으로 재작성, "몰래 가는 중 🤫"류 게이팅 FOMO 카피를 혜택 지향 카피("등급이 오르면 같은 매장도 더 크게 받아요")로 교체. ④ 문서: 기능정의서(M8 폐기·오버레이·CTA 분기·B급 카피 표), 운영정책서(quota 의미), 데이터정책서(quota/consumedSlot 정의), flow.md, 화면설계서(R-01/R-02/R-03 보드·디자이너 가이드) 일괄 정정. ⑤ 재발 방지: CLAUDE.md에 정책 불변식과 검증 규칙(문서-코드 일치 ≠ 정책 정당성) 명문화. |
| **v3.1** | 2026-07-07 | **문서 체계 분리 + 프로토타입 정합화.** 혼재돼 있던 PRD를 성격별 문서로 분리: 본 PRD는 **요구사항정의서**(비전·문제·해결·범위·페르소나·BM·BI·로드맵·성공지표)만 유지하고, ① 화면별 기능·상태머신·API·디자인 시스템 → [`docs/기능정의서.md`](docs/기능정의서.md)(정본 승격), ② 검수·정산·멤버십·바이럴 재원·법무·출시 체크리스트 → [`docs/운영정책서.md`](docs/운영정책서.md)(신설), ③ 데이터 모델·영속성·시드·개인정보·토큰 → [`docs/데이터정책서.md`](docs/데이터정책서.md)(신설), ④ 화면 인벤토리·라우트·BottomNav → [`docs/IA.md`](docs/IA.md)(신설), ⑤ 시나리오 A~G → [`flow.md`](flow.md)(신설)로 이관. **구현 검증·정합화**: 초대 토큰 charset 오기재(“base62”→31자 대문자, 혼동 문자 제외) 정정, 시드가 7-상태 전부 커버하도록 `cancelled` 데모 패스 추가, 도달 불가하던 `/r/press` 인덱스 화면 제거(기자단은 `/r/passes` 탭). **스토리보드(디자인) 모드**: `STORYBOARD` 플래그로 실 목데이터를 한글 스키마 라벨로 치환(디자인 파트용), 실데이터 토글 가능, SEED_VERSION 스토리보드 계열(1010) 분리. 하드코딩 시크릿(AUTH_SECRET 폴백·Naver 키)은 출시 보안 체크리스트에 정확히 명시. |
| **v3.0** | 2026-07-03 | **VER.1 MVP — 출시 준비 개편** (브랜치 `mvp/ver1`). "결제(PG)만 붙이면 출시 가능한 수준"을 목표로 정책 맹점·법적 공백·바이럴 허구 요소를 전면 해소. ① 법적 고지(`/legal/terms`·`/legal/privacy`·가입 필수 동의·회원 탈퇴). ② 체험권 라이프사이클 자동화(`pass-lifecycle.ts` — 만료 슬롯 복구·`consumedSlot`·noShow·72h 초과·6h 리마인드). ③ 참여 취소(`/api/passes/cancel`·`cancelled` 상태). ④ 반려 사유 보존 + 72h 1회 재제출. ⑤ 리뷰 기한·광고표기 서버 강제(`adNoticeConfirmed`). ⑥ 바이럴 보상 3종(부스트/멤버십할인/quota보너스)으로 축소 + 전부 실사용 경로 구현, `cash`/`spotlight_pass` 폐기. ⑦ 라이브 카운터 조작 noise 제거(실데이터), 평점 출처 표기. ⑧ 지원금 부담 주체 명문화. ⑨ 보안 하드닝(AUTH_SECRET 경고·refresh-stores 503·`.env.example`). ⑩ BM/BI/컴플라이언스/출시 체크리스트 신설. SEED_VERSION 10. |
| v2.16 | 2026-06-30 | 채널별 등급 + 채널 선택 참여 개편 — 채널 3종(블/인/틱)·`channelGrades`·`SUPPORT_MULTIPLIER`·`channelOffers`/`bestEligibleSupport`·`StoreParticipate`·채널별 자가점검·강조 키워드. SEED_VERSION 9. |
| v2.15 | 2026-06-30 | 운영팀 검수 백오피스 신설(`admin` 역할·`/admin/reviews`·`/api/admin/reviews/decide`·양측 알림). SEED_VERSION 8. |
| v2.14 | 2026-06-28 | 홈 3-카드 큐레이션 라벨/필터 정비(최근 등록/곧 마감/파격 지원금 → sort=new/closing/topSupport). SEED_VERSION 7. |
| v2.13 | 2026-06-28 | BottomNav [등급]→[체험권] 교체, 등급은 MY 하위로. |
| v2.12 | 2026-06-28 | 체험권 화면 4자리 코드 표시 → 사장님 직접 입력 필드(`/api/passes/use-by-code`). |
| v2.11 | 2026-06-28 | 사용 처리 코드 8자→캠페인별 4자리(`Campaign.useCode`) 개편. SEED_VERSION 6. |
| v2.10 | 2026-06-25 | 홈 큐레이션 2섹션(가까운 곳 + 전체 리스트). |
| v2.9 | 2026-06-25 | 등급/혜택 책임 분리 — `/r/grade` 등급 탭·`/r/rewards` viral 전용·BottomNav 5탭. |
| v2.8 | 2026-06-25 | 바이럴(레퍼럴) 메인 흡수 — 데이터 모델·`referral.ts`·API 3종·`/r/invite/new`·`/r/i/[token]`·`/welcome/box`. SEED_VERSION 5. |
| v2.7 | 2026-06-16 | B급 감성 카피·톤 적용(체험자 측). 카피 변환 표·상태별 카피·시각 가이드. |
| v2.6 | 2026-06-16 | 체험자 IA 재구성 — `/r/home` 큐레이팅 분리 + `/r/explore` 신설 + RowCard + `/r/rewards` + BottomNav 재구성. |
| v2.5 | 2026-05-29 | 멤버십·메뉴 가격 개편 — 월 모집 팀 수 차별화(5/15/50/∞), 가입 기본 Free, S~C 전 플랜 모집. SEED_VERSION 3. |
| v2.1~2.4 | 2026-05-28 | 기자단 URL+자가점검 통일, 사장님 홈 카드 3구간 카운터, 체험단/기자단 탭 분리. |
| v2.0 | 2026-05-28 | 배포 기준 전면 재작성 — 4탭 BottomNav, 사장님 조회 전용+채널톡, 캠페인 제목 자동, 플랜 자동 분배, Apple 디자인 시스템, 매장 20곳, 자가점검 모델, 8자 코드, 길찾기 FAB. |
| v1.0~1.1 | 2026-05-20 | HTML 프로토타입 역기획 + 미해결 항목 확정. |
