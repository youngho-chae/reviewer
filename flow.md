# CATCHPASS · 유저 시나리오 (User Flows) — VER.1

> 이 문서는 **사용자 관점의 여정(플로우)**만 다룬다. 화면별 기능 요구·제약은 [`docs/기능정의서.md`](docs/기능정의서.md),
> 화면 구조·라우트는 [`docs/IA.md`](docs/IA.md), 정책·운영 규칙은 [`docs/운영정책서.md`](docs/운영정책서.md),
> 데이터·개인정보는 [`docs/데이터정책서.md`](docs/데이터정책서.md), 제품 요구사항은 [`PRD.md`](PRD.md)를 본다.
>
> 표기: `[/route]` = 화면, `→ POST /api/...` = 서버 호출. 모든 흐름은 **구현 코드(브랜치 `mvp/ver1`) 기준**이며, 카피 원문은 큰따옴표로 표기한다.

액터: **체험자(Reviewer, `/r/*`)** · **사장님(Owner, `/o/*`)** · **운영팀(Admin, `/admin/*`)** · **비회원**.

---

## 시나리오 A — 체험자: 가입부터 첫 방문형 리뷰까지 (핵심 여정)

```
[/r/signup] Step 0 — 컨셉 히어로 "선정 기다리는 체험단 말고, 등급으로 받는 체험권." → [시작하기]
[/r/signup] Step 1 — 이메일 / 비밀번호(최소 6자) / 닉네임 + 필수 동의 2종(이용약관·개인정보, /legal/* 링크)
    · 둘 다 체크해야 [다음] 활성 (클라이언트) + /api/auth/signup가 agreeTerms 미포함 시 400 (서버)
[/r/signup] Step 2 — SNS 채널 3종(블로그/인스타/틱톡) URL + 영향력 수치 직접 입력 → 채널별 등급 자동 산정
    · 1개 이상 연동 시 영향력 기반 N~A 산정 · [연동 없이 시작 (N등급)] 보조 액션
    → 가입 완료 → 세션 쿠키(30일) → invite 파라미터 있으면 /welcome/box, 없으면 /r/home

[/r/home] 홈 (발견 전용 — 리스트/필터/지도는 /r/explore에서) · 디자인 시스템 v2
    · 상단 바: {지역명} ⌄ │ ⊕(GPS) + 검색·알림 아이콘 — 지역명 탭 → [/r/location] 현위치 설정 페이지 전환,
      ⊕ 탭 → 선택 지역 해제 + 현 위치(reverse-geocode)로 갱신
[/r/location] 현위치 설정 — 상단 📍 현재 지역 + [⊕ 현위치로 설정] · 좌측 시도 레일(1차) → 우측 전체·시군구 리스트(2차)
    · 항목 탭 → /r/home?area= 복귀 → '걸어서 갈 수 있어요'만 해당 지역 기준으로 변경(전국 리스트는 무관)
    · 프로모 배너(블루 마케팅 슬롯) + 내 활동 3-stat(이번 달 아낀 금액 / 밀린 리뷰 / 🎫 참여 예정 → /r/passes)
    · "걸어서 갈 수 있어요👀" **1단 가로 캐러셀**(반경 3km, 가까운 순 최대 10개) + "내가 체험할 수 있는 전체 리스트👀"(2단 그리드 · **최신 등록순**)
    · 카드 = 4:3 사진 + SNS 배지(블로그/인스타/틱톡) + 🎫 00개 남음(발급 소진 시 "발급 마감 · 체험 진행 중") + 가게명 + "최대 000,000원 지원"
    · [P1] 등급 부족 오버레이 없음 — 모든 카드 참여 가능, 금액만 내 채널 등급 기준 개인화

[/r/explore] 탐색 — **기본 화면은 지도 보기**(?mode=list로 리스트) · 디자인 시스템 v2 · 진입 시 현재 위치 기준
    · 헤더 = [방문형] 세그먼트 타이틀(기자단은 MVP 제외 — PRESS_ENABLED 시에만) + 검색(지역·매장·키워드)·알림 아이콘
    · 카테고리 아이콘 칩(다중 선택=검정 보더) + 필터 버튼(활성 시 퍼플 dot) · "근처 체험 N개 발견!" + 정렬방식 ⌄(5종)
    · **통합 필터 바텀시트**: SNS 채널·카테고리 다중 선택, 적용 버튼 없이 즉시 반영, ↺ 초기화 아이콘.
      선택한 채널값은 상단에 노출하지 않는다
    · experience-row: 96px 썸네일 + SNS 배지 + 가게명 + 카테고리·거리 + "최대 000,000원 지원" + 우상단 🎫 00개 남음(소진 시 "발급 마감")
    · 하단 중앙 검정 pill FAB [☰ 목록 보기] ↔ [📍 지도 보기]
[/r/explore] 지도(기본) — 풀맵 + 2줄 흰 pill 마커(매장명/최대 금액, 선택=퍼플 강조)
    · 핀 미선택 시 피크 바텀시트(핸들+카테고리 칩+필터 아이콘까지만) — 위로 쓸어올리면 목록 보기로 자동 전환
    · 핀 탭 → 하단 카드 캐러셀(좌우 스와이프 = 거리순 다음 매장)
    · 지도 카드 = 매장명·카테고리·거리·지원금만 (채널·마감일·등급 미노출 — 과도한 정보 배제)

[/r/store/[id]] 매장 상세 · 채널 선택 참여 · 디자인 시스템 v2
    · 4:3 풀블리드 사진 + SNS 배지 + 가게명 + 플레이스 URL 링크
    · 상단 우측 ♡ 관심 목록 토글(캠페인 단위 저장 → POST /api/interests)
    · info-strip 3분할(체험 마감일 "00월 00일 까지" / 리뷰 마감 기한("이용 후 7일 이내" 공통 문구) / 🎫 잔여) + 퍼플 안내 배너("발급 후 72시간 내 미사용 시 소멸")
    · "어느 SNS로 체험할까요?" 라디오 카드 — 연동 채널(등급·[연동 완료]·금액) 선택=퍼플 보더 / 미연동=[연동 필요]·확인불가 비활성
    · 필수 메뉴(택 1, 퍼플 하이라이트 행) · 리뷰 키워드 #칩 · 매장 소개 · 지도 스니펫+주소 복사 · 이용방법 스텝 1-2-3
    · 리뷰 작성 조건 = 핵심 조건 칩(사진 수·글자 수 등, 채널별 가변 데이터) + "광고 표시 문구 필수"
      — 타 등급 최대 지원금 비교 문구는 노출하지 않음 · 종료 캠페인은 열람 가능 + "마감된 체험입니다" + CTA 비활성
    · "지원금은 채널별 내 등급에 따라 달라져요 · 매장이 결제 시 직접 할인해 드리는 금액이에요" 고지
    · cta-bar(하단 고정): 지원 금액 000,000원 + [체험권 발급받기]
    → POST /api/passes { campaignId, channel }
        · [P1] 등급 자격 검증 없음 — 채널 연동·잔여 슬롯만 확인. 추가 가드: 종료 캠페인 400 / active 5장 보유 400 /
          동일 캠페인 취소 후 12h 이내 400 / 기자단(MVP 제외) 403
        · 선택 채널 등급으로 지원금 확정, quota 버킷 차감(consumedSlot 기록) · Pass 생성(active, **72h TTL — 연장·복구 불가**) · 사장님 알림
        → /r/passes/{passId}

[/r/passes/[id]] active = 티켓 + QR
    · 할인 큰 숫자(등급 배율 지원금, 미사용 초대 부스트 보유 시 "🎁 초대 보상 +N% 부스트 포함" 가산)
    · QR(pass 8자 코드) + "결제 시 사장님께 보여주세요" · 72h 카운트다운(실제 마감 일시 기준)
    · 사용 처리 입력 폼 — 4자리 코드 미노출, 사장님이 직접 입력(+선택 결제 금액)
      → POST /api/passes/use-by-code (체험자 세션, code === campaign.useCode 검증 시 used)
    · [방문이 어려워요 — 참여 취소] → 2단 확인 → POST /api/passes/cancel → cancelled + 모집 슬롯 즉시 복구 (노쇼 미집계)

  (경로 B) 사장님 /o/scan에서 QR 스캔 또는 4자리 조회 → POST /api/passes/use → used + 체험자 알림

[/r/passes/[id]] used = 리뷰 인증 폼
    · 결제·지원 적용액 + usedAt+7일 카운트다운(보유 화면은 실제 마감 일시·카운트다운 명시)
    · ReviewForm: ① 채널 고정 표기 ② 광고 표시 문구 박스[복사]+포함 확인 체크 ③ URL 입력 ④ 채널별 자가점검
    → POST /api/passes/review { passId, reviewUrl, selfCheck, adNotice:true }
        · 서버 검증: 이용 후 7일 기한 / adNotice 필수 / 자가점검 전 항목 / 게시 유지(90일) 동의(keepAgreed) → review_submitted (adNoticeConfirmed=true)

[/r/passes/[id]] review_submitted → 운영팀 검수(영업일 기준 최대 3일) → completed(등급 반영) 또는 rejected
[/r/passes/[id]] rejected — 사유(rejectReason) 원문 노출 + 반려 후 7일 내 1회 재제출(resubmitCount<1) → 재검수
[/r/passes/[id]] cancelled — "직접 취소한 체험권입니다. 같은 캠페인이 모집 중이면 취소 12시간 뒤부터 다시 참여할 수 있어요."
```

---

## 시나리오 B — 체험자: 기자단(비방문) 콘텐츠 작성 — **MVP 제외 (추후 버전)**

> 2026-07-07 회의: 1차 출시는 방문형만 제공. 기자단은 `PRESS_ENABLED=false`로 게이트되어 아래 흐름은
> 신규 진입이 차단된다(브리프 redirect·발급 API 403). 기존 발급 건의 작성·검수·정산만 마무리 허용.

```
[/r/explore] 기자단 탭 → 카드(자료팩 N장 · 정산 예정금 ₩ · "3.3% 원천징수 후 입금")
[/r/press/[id]] 기자단 브리프 — 정산 예정금 · 마감 D-N · 자료팩(신청 전 blur) · 필수 키워드 · 게시 채널
    → [참여 신청하기] POST /api/passes (kind=press) → /r/passes 기자단 탭 등록
[/r/passes] 기자단 탭 — 통계 스트립(작성 중 / 검수 중 / 정산 예정 ₩) + 상태별 CTA
[/r/press/[id]/write?pass=...] 기자단 작성 — 자료팩 풀공개 + 광고 문구 박스[복사]
    · PressWriteForm: ① 작성 채널 ② 광고 문구 포함 체크(필수) ③ 게시 URL ④ 자가점검(필수 키워드·자료팩 활용)
    · 본문 입력 UI 없음 — 본인 채널에 게시, URL만 제출(운영팀 표본 검수)
    → POST /api/passes/review { reviewChannel, reviewUrl, pressSelfCheck } → review_submitted → 검수 → completed(정산)
    · 반려 시 캠페인 종료 전 1회 재제출
```

> ※ 기자단은 **별도 보관소 화면이 없다.** 신청·작성·검수·정산 현황은 모두 `/r/passes`의 기자단 탭에서 본다.

---

## 시나리오 C — 사장님: 첫 진입부터 캠페인 오픈

```
[/o/login] → /o/home
[/o/home] 인사 + "최근 후기 N건 운영팀 검수 중" + 현재 플랜 스트립 + 이번 달 모집 현황
    · 진행 중 캠페인 [체험단 N]/[기자단 N] 탭 — 카드에 등급별 모집 인원 + 3구간 카운터(방문 예정/완료/총 모집)
    · [+ 새 캠페인]
[/o/campaign/new] 새 캠페인
    · 매장 선택("제목은 매장명으로 자동") · 진행 일수 · 지원금(= S등급 100% 기준치)
    · 총 모집 인원(단일 입력) — 월 한도 카드(사용/보너스/잔여), 초과 시 [생성] 비활성
    · 사용처리 코드(숫자 4자리 필수, 진행 중 캠페인 간 중복 차단, 체험자 미노출)
    · 필수 채널(블/인/틱 1개+) · 필수 메뉴(+가격) · 강조 키워드(최대 5개) · 매장 소개(최대 500자)
    → POST /api/campaigns { storeId, days, supportAmount, totalQuota, useCode, requiredChannels[], requiredMenus[], highlightKeywords[], description }
        · title 자동 · distributeQuota 자동 분배 · 월 한도 검증(quota_bonus 가산·소진) · 즉시 활성
```

---

## 시나리오 D — 사장님: 손님 체험권 사용 처리 (QR 또는 4자리)

```
[/o/scan] [📷 카메라 스캔](QR=pass 8자 코드 단건 조회) 또는 4자리 숫자 입력(캠페인 useCode 조회)
    → POST /api/passes/lookup { code } (4자리→useCode 최근 발급분 / 8자→pass 고유 코드)
[/o/scan] 결과 카드 — 캠페인명 · 익명 #last4(등급) · 상태 · 지원금 한도
    · 실 결제 금액 입력 → 적용 지원금 = min(결제액, 한도+부스트)
    → POST /api/passes/use { code } → used + supportApplied 기록 + 체험자 알림 → /o/home
```

> 캠페인 생성 시 지정한 4자리는 체험자 체험권 화면에 **노출되지 않으며**, 사장님만 인지해 사용 처리에 쓴다.

---

## 시나리오 E — 사장님: 후기 모니터링 (직접 검수 없음)

```
[/o/reviews] "체험자가 게시한 후기를 조회할 수 있습니다. 사장님은 직접 검수하지 않으며,
             광고 표시 누락·재작성 요청 등은 채널톡으로 운영팀에 접수해주세요."
    · 통계(운영팀 검수 중 N / 통과 누적 N)
    · 후기 카드 — 등급 배지 + 익명 #last4 + 채널 + 상태 뱃지(검수 중/통과/반려) + 게시 URL + 본문 line-clamp-3
    · [💬 채널톡으로 문의하기] (ChannelIO, 미로드 시 mailto 폴백)
[/api/passes/approve] 410 Gone — 사장님 직접 검수 폐기
```

## 시나리오 E-1 — 운영팀: 후기 검수

```
[/admin/login] admin 로그인 → [/admin/reviews]
    · 인증 게이트(admin 아니면 redirect) · 통계(대기 N / 최근 7일 N)
    · review_submitted 리스트(오래된 것 우선) — 익명 #last4 + 종류 + 채널 + [게시물 열기 ↗] + 자가점검 칩
    · [검수 통과] → POST /api/admin/reviews/decide {approve} → completed + completedReviews++ + 양측 알림
    · [반려] → 사유 입력(≤500자, "체험자 화면에 그대로 표시…") → {reject, reason} → rejected(rejectReason/rejectedAt 보존) + 양측 알림
    · 반려된 체험자는 반려 후 7일 내 1회 재제출 → "후기 재제출"로 다시 검수 대기열 등장
```

---

## 시나리오 F — 사장님: 성과 확인

```
[/o/report] 최근 30일 누계 — 총 노출 추정(영향력×30% 도달) + 스파크라인 · 작성 완료율 · 광고표시 준수율 · CPM · 채널별 분포 · 등급별 ROI
```

---

## 시나리오 G — 바이럴(레퍼럴): 초대부터 양면 보상까지

```
[/r/passes/[id]] used(T1 트리거) — "₩X 절약 완료! 친구도 받게 해줄래요?" → /r/invite/new?store=&campaign=
[/r/rewards] 혜택 탭 — 친구 초대 박스 카드(basic/silver/gold + 진행도) + [🎁 친구에게 쏘기]
[/r/invite/new] 매트릭스(RR/RO/OR/OO) 자동 결정 + 자동 메시지 + 4채널 공유 시트
    → POST /api/referral/invite → 토큰(8자, 14일 TTL) URL 발급

[/r/i/<token>] 피추천자 랜딩(비회원) — 유효/만료/사용됨 분기. 로그인 상태면 /welcome/box 자동 이동
    → [박스 받고 가입하기] /r/signup?invite=<token>
[/welcome/box?token=] 환영 박스 — 슬롯머신(0.6s shake → 1.8s spin → 2.2s reveal) + 컨페티
    → POST /api/referral/accept { token, mode:"accept" }
        · 발신자 행운 박스: 체험자 지원금 부스트(+10/20/30%) / 사장님 quota_bonus(+3팀) 또는 membership_discount(₩10,000)
        · 피추천자 환영 박스: 첫 체험 지원금 +50% 부스트(체험자) 또는 멤버십 50% 할인(사장님)
        · viralCounter.liveStream에 실제 이벤트 1줄 기록(조작 수치 없음)
[/r/passes/[id]] completed(T2 트리거) — "검수 통과! 행운 박스 더 키우러 갈까요?" → /r/invite/new
```

> 보상 재원·어뷰징 가드·상한 규칙은 [`docs/운영정책서.md`](docs/운영정책서.md) §바이럴 참조. 보상 3종은 전부 실사용 경로가 구현되어 있다.

---

## 시나리오 H — 체험자: 관심 목록 (2026-07-07 회의 신설)

```
[/r/store/[id]] 상단 우측 ♡ 탭 → POST /api/interests { campaignId } → 저장(♥ 퍼플) / 재탭 시 해제
    · 저장 단위는 매장이 아니라 **캠페인**
[/r/me] "관심 목록" 메뉴 → [/r/interests]
[/r/interests] 필터 칩 [전체 | 진행 가능 | 마감] (?f=open|closed)
    · 진행 가능 = 지금 발급 신청 가능(open) / 마감 = 기간 종료·발급 불가(closed·issued_out)
    · 마감 항목은 목록에서 유지 + 썸네일에 "마감된 체험" 오버레이 (소진됐다 살아나는 노출 구조 없음)
    → 항목 탭 → [/r/store/[id]?campaign=] — 종료 캠페인도 상세 열람 가능, "마감된 체험입니다" 배너 + CTA [마감된 체험이에요] 비활성
```

## 시나리오 I — 체험자·사장님: 배송형 체험 + 포인트 (2026-07-12 레뷰 벤치마크)

> 정본: 운영정책서 §14 · 기능정의서 REQ11_1/11_2·REQ6_1/6_2 · docs/벤치마크-레뷰.md

1. (체험자) 홈 "집으로 배송받는 체험 📦" 또는 탐색 **배송형 세그먼트**(리스트 전용 — 지역 필터 무관 전국)에서 카드 선택.
2. 상세에서 채널 선택(연동 채널만 — P1: 등급은 포인트 크기만 결정) → [배송 체험 신청하기] → 모달에서 **배송지 입력**(수령인·연락처·주소, 발송 목적 한정 고지) → 신청 완료. 체험권 = "발송 대기"(기한: 캠페인 종료일, 발송 전 취소 가능).
3. (사장님) 홈 상단 "📦 발송 대기 N건" 큐 — 익명 #last4 + 수령인 정보(발송 목적 한정 노출) 확인, 운송장 입력(선택) 후 [발송 처리].
4. (체험자) "상품이 발송되었어요 📦" 알림(운송장 포함) — 체험권 "리뷰 작성" 전환, **발송 후 7일** 기한(기존 리뷰 스윕 재사용).
5. 리뷰 제출 → 운영팀 검수(E-1과 동일) → **승인 시 포인트 적립**: `pointReward × 채널 등급 배율`(100P 반올림) + 적립 알림.
6. (체험자) MY → 체험 포인트(`/r/me/points`): 잔액(원장 합산)·출금 신청(최소 10,000P·1,000P 단위) — **세금(사업소득 3.3%, 세액 1,000원 미만 소액부징수)·수수료 500원 차감 실지급액 미리보기** 후 신청(즉시 차감).
7. (운영팀) `/admin/points` 출금 큐 — [지급 완료](영업일 4~6일 내 입금) 또는 [반려](사유 안내·포인트 전액 복구). 처리 결과 알림.

보강 (2026-07-16 리뷰노트 벤치마크): 2번 신청 모달에서 캠페인에 **상품 옵션**이 있으면 택1 필수(발송 큐에 표시), 3번 발송 처리에 **택배사 선택** 추가(4번 알림·체험권 상세에 택배사·운송장·[배송 조회] 링크), 신청 후 **3일 미발송이면 양측 화면에 발송 지연 표시**(리뷰 기한은 발송 후 7일 롤링이라 불이익 없음 고지).

## 시나리오 J — 체험자·사장님: 예약형 체험 (2026-07-22 예약형 체험 시스템)

> 정본: 운영정책서 §15 · `src/lib/reservation.ts` — 발급 즉시(P1) · **확정 전 QR 미노출** · 시간 표기 12시간제

1. (체험자) 탐색 "📅 예약 필수" 배지 카드 → 상세: 퍼플 배너("체험권은 예약한 방문일까지 사용할 수 있어요") + 오렌지 배너("희망 방문 일시를 선택하면 사장님이 예약 확인 후 확정할 수 있어요" + `reservationNote`) → [예약 요청하기]. 예약 가능 시작일(opensAt)이 있어도 **신청은 즉시 가능** — 캘린더에서 오픈일 이전 날짜만 비활성(2026-07-23 정정).
2. **"언제 방문할까요?" 시트**(2026-07-23 시안): 참여 채널·등급·지원 금액 요약 + [예약 날짜]→**캘린더 모달**(선택 가능일만 활성 — 과거·오픈 전·휴무·차단 흐림, [다음]) + [예약 시간]→**오전/오후 시간 칩 모달**(브레이크·정원 마감·차단 칩 비활성, [완료]) + **인원 스테퍼(1~10)** → [예약 요청하기]. 유효기간 = **방문 희망일 +1일 말**, 상태 = "예약 대기" — 확정 전에는 QR 대신 예약 대기 화면(예약 변경 **1회**·참여 취소 가능).
3. (사장님) 홈 [방문 예약] 큐 또는 캠페인 카드 → **캠페인 관리**(`/o/campaign/[id]`) 예약 요청 목록(상태 필터·가까운 일정순)에서 셋 중 선택:
   - **[예약 확정]** → 확정 → 5로.
   - **[다른 일정 제안]**(1회) → 스케줄 내 대안 슬롯 최대 3개 + 수기 안내사항 → "일정 제안 중".
   - **[거절]** — 확정 전 언제든: 신청 취소, 체험자 문구 "매장 사정으로 예약이 확정되지 않았어요" — **패널티·12h 재신청 제한 없음**(즉시 재신청 가능).
4. (체험자·제안 수신 시) 알림 → 체험권 상세 라디오 최대 4행(제안 3 + 기타): 슬롯 수락 → **추가 승인 없이 즉시 확정 + QR 활성** / 기타 = 재제안(1회) → 사장님 [예약 확정]/[거절] / 거절 = 취소(패널티 없음). 조율 이력은 체험자에겐 요약(제안 선택지 미누적), 사장님·운영자에겐 전체 유지.
5. (확정 후) 체험권 카드에 예약 일시 강조+"예약 확정" 칩, 이력은 접힘. **방문 전날 리마인드** 알림. 취소는 **방문 전날 23:59까지**(당일엔 매장 문의 안내) — 12h 재신청 제한은 체험자 직접 취소만. 사장님이 부득이 취소하면 **사유 입력 필수** → 체험자에게 원문 안내·QR 즉시 무효(패널티 없음).
6. (사장님·일정 관리) 캠페인 관리에서 **날짜 차단·시간 차단·당일 일시중지**(자정 자동 해제) — 차단해도 기존 확정 예약은 자동 취소되지 않음(경고 후 개별 취소). 방문형 캠페인에는 미제공.
7. 예약 시간 방문 → QR 제시/코드 입력(확정 후에만 — 서버 검증) → 리뷰 제출 기한 = **확정 방문일 +7일**. 미방문 시 방문일 +1일 경과로 만료(문구 "예약 방문일이 지나 만료" — 추가 패널티 없음). **미확정 상태로 방문 시각이 지나면 자동 취소**(매장 사정 — 패널티 없음, 신청 24h 무응답 시 사장님 리마인드).
8. (운영팀) `/admin/reservations` — 전체 예약 로그(상태/캠페인/사장님/체험자 필터·이력·취소 주체/사유) + 수동 취소("운영 정책에 따라…" 문구).
