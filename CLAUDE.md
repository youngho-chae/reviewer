# CATCHPASS 프로젝트 규칙 (CLAUDE.md)

CATCHPASS: 등급 기반 리뷰 체험권 플랫폼 (Next.js 15 App Router, 모바일 웹 480px).
문서 맵: 요구 `PRD.md` · 기능 `docs/기능정의서.md` · 운영 `docs/운영정책서.md` · 데이터 `docs/데이터정책서.md` · IA `docs/IA.md` · 시나리오 `flow.md` · 화면설계서 `docs/sdd/`.

## 핵심 정책 불변식 (PRD §1.0 — 절대 위반 금지)

- **P1. 모든 등급(S+/S/A/B/C/N)은 모든 캠페인에 참여할 수 있다.** 등급은 참여 **자격이 아니라 혜택의 크기**(지원금 배율 S+·S100/A80/B60/C40/N10%)만 결정한다. "최소 참여 등급"·"등급 부족"·"OO등급 전용"·등급 잠금/오버레이/흐림 처리는 **어떤 형태로도 만들지 않는다**. 참여를 막는 조건은 ①캠페인 종료 ②잔여 슬롯 소진(SNS 채널 참여만 — 영수증 참여는 무차감·무영향) ③(배송형) 채널 미연동 — 3가지뿐. **방문형은 미연동도 영수증 리뷰로 참여 가능**(2026-08-07 — 구 "방문형 채널 미연동 차단" 폐지, 정본 `src/lib/channels.ts` RECEIPT_* · 혜택 = **결제 금액의 10% 할인**(정액 아님 — `receiptSupportFor` 결제액×10%·상한 = 기준 지원금, 표기는 전 화면 "10% 할인"·사용 처리 결제 금액 필수·초대 부스트 미적용·W 표본 제외 · **모집 인원 무차감**(consumedSlot 없음·복구 없음·소진 게이트 미적용 — 종료 전까지 무제한 모집, 소진 시 상세 라디오 "지금도 참여 가능" 칩)) · 리뷰 제출 = **URL 공통**(`reviewUrl` — My 플레이스 안내+이동 버튼 `NAVER_MY_PLACE_URL`, 구 캡처 업로드 폐기·`reviewImage` deprecated) · **광고 문구 표기 불요**(체크리스트·서버 검증·상세 카드 제외 — RECEIPT_AD_NOTICE 폐기)). quota{S,A,B,C} 버킷은 배분 기록이지 자격이 아니다.
- **P2.** 방문형 지원금 = 매장이 결제 시 직접 할인 (회사 무정산).
- **P3.** 사장님은 후기 조회만 — 검수는 운영팀 단일 책임 (`/api/passes/approve`는 410).
- **P4.** 사용자 노출 수치·사회적 증거는 실제 발생 이벤트만 (조작·noise 금지).
- **P5.** 발행하는 보상은 반드시 실사용(소비) 경로가 구현되어 있어야 한다.

## 검증 규칙 (2026-07-07 판단 실패의 교훈)

1. **문서-코드 일치 ≠ 정책 정당성.** 문서와 코드가 서로 일치해도 위 불변식에 어긋나면 **양쪽 다 결함**이다. 감사·검증 시 반드시 불변식 목록과도 대조하라. (실사례: PRD와 코드가 똑같이 "최소 등급 게이트"를 갖고 있어 일치 감사를 통과했지만, 실제 정책 P1 위반이었다.)
2. **내적 모순은 정책 오류의 신호다.** 예: `SUPPORT_MULTIPLIER`에 N=10%가 정의되어 있는데(N등급도 혜택을 받는 전제) 게이트가 N의 참여를 전부 차단하고 있었다 — "쓰이지 않는 정책 값"이나 "도달 불가 분기"를 발견하면 정합성 문제로 보고하라.
3. 불변식과 모순되는 요구·구현을 발견하면 **조용히 유지하지 말고** 사용자에게 확인하거나 정정하라.
4. 정책 수치·규칙 변경 시 코드 + 문서 6종 + `docs/sdd/figma-scripts/data/*` + Figma 보드를 함께 갱신한다 (카피 원문주의).

## 기술 메모

- **디자인 시스템 v2 (정본 `DESIGN.md`)**: Primary 퍼플 #9333EA · Pretendard · 파스텔 SNS 배지 · 헤어라인 라운드 카드 · 타이포 11~22px. v1 Apple 톤(Action Blue·다크 타일·SF Pro)은 파기 — 재도입 금지. 규칙: 퍼플=인터랙션, 검정=가치(금액), 파스텔=SNS 아이덴티티, 블루=마케팅 슬롯 전용. 화면설계서(Figma 보드)는 v1 기준으로 남아 있음(v2 반영은 별도 지시 대기).
- 등급·배율: `src/lib/grade.ts` (gradeMeets는 P1 위반으로 삭제됨 — 재도입 금지). **6단계 개편(2026-08-06, 정본 운영정책서 §10.3·§10.6)**: Grade = **S+/S/A/B/C/N** — **N = 채널 미연동 전용**(연동하면 최하라도 C — INDEX_BANDS N 밴드 폐지·C lo=1·연동 바닥 점수 30·apiGrade 최저 C) · **S까지 자동 평가**(지수 S 밴드 [50만,500만)→90~100 신설, 구 "S 수동 부여"·sCandidate 폐기) · **S+ = 계정 표기 등급(Reviewer.grade) 전용 레이어** — 채널 등급·Pass.reviewerGrade·quota{S,A,B,C} 버킷·발급 로직은 상한 S로 무변경, 스윕이 "전월 S 이상+채널 최고 S+F100+W100(중립 불가)+P0+표본 충족" 충족 월에만 부여(미충족·무활동 스킵 월 = S로, 채널 연동/해제 시에도 S로 리셋) · **S+ 혜택 = 배율 외만**(골드 배지 `gradeSplus`·배송형 포인트 +10% `SPLUS_POINT_BONUS`·프로모션 최우선(S+>상생 뱃지>일반)·어드민 검수 큐 우선 정렬) — 배율은 S와 동일 100%(P2 상한), 보유 한도·노출·발급 우대는 P1 위반으로 채택 불가.
- **등급 월간 재평가 (2026-07-08 · 산식 고도화 2026-08-05)**: `src/lib/grade-regrade.ts` — 매월 말(KST) lazy 스윕. GS = 0.70 지수(`indexScoreV1`) + 0.20 성실 이행 + 0.10 상생지수 − 패널티. **F = 완료율 60%+기한 준수 40%**(완료 건별 마감까지 남긴 시간 비율, 분모에 노쇼·기한 초과·반려 종착·**반려 방치**(1차 반려 후 7일 미재제출 — 무감점)) · **W = min(r,1) 평균 × 표본 가중 min(1,n/3)**(적용 지원금 1,000원 미만 건 제외·절대 금액 금지, **결제 표본 0건 월 = 상생 중립** — W 제외·GS=(0.7I+0.2F)/0.9 재정규화, `wNeutral` 이력) · **패널티 = ×1.5 가중 후 상한 −30**(노쇼 −10/기한 초과 −7/반려 종착 −5, 취소 무패널티) · 컷 S90/A70/B50/C0(연동 바닥 — N 컷 폐지, 2026-08-06) · 월 ±1등급 · **S까지 자동 평가·S+는 조건 판정**(6단계 개편 — sCandidate 폐기) · 리뷰 품질 요소 없음 · 기한 초과 귀속·리뷰 제출 최초 기한 = `reviewDeadline` 통일 · 채널 대상 URL 유무 무관 · 상생 리뷰어 뱃지(W≥60&완료 3건↑, 표시 전용 — 전용 아이콘 `WinWinBadge`(하트 악수 마크)를 닉네임 옆 표기, 2026-08-07) · 만료 처리 정본 `expirePass`(pass-lifecycle — 스윕·use·use-by-code 공유). **재평가 산출물(GS·패널티·뱃지)을 참여/발급 분기에서 참조하면 P1 위반.** **수치 노출(2026-08-06)**: 가중치·점수·컷 등 산식 수치는 어드민 `/admin/grading` 평가 기준표 전용(코드 상수 직접 렌더) — 체험자 `/r/grade`는 수치 비노출: 행동 가이드(지난달 감점 요인 건수+"다음 달 등급을 올리려면" 카드)와 변동 방향만. **행동 가이드 개인화(2026-08-07)**: 정적 설명 폐기 — 이번 달 활동을 `collectMonthlyActivity`(스윕과 동일 정본)로 실측 집계해 지수별 현황 카드(성실 이행 완료·감점 건수/상생 만점 표본 "2배 결제 n/3건" 도트 게이지/채널 등급 배지/반려 재제출 대기)로 "만점까지 남은 행동"을 건수 단위로 노출(산식 수치는 계속 비노출). 정본: 운영정책서 §10.
- 발급: `src/app/api/passes/route.ts` — 자기 등급 버킷 우선, 소진 시 잔여 버킷(N→C부터). `consumedSlot`으로 만료/취소 시 복구.
- **2026-07-07 회의 확정 수치**: 체험권 유효 **발급 후 72h**(연장·복구 불가) · 리뷰 **이용 후 7일** · active 동시 보유 **5장** · 취소 후 동일 캠페인 **12h 재신청 제한** · '걸어서 갈 수 있어요' 반경 **3km**. MVP는 방문형만 — **기자단(press)은 릴리스 브랜치에서 코드째 제거**(2026-07-23, dev 브랜치에서만 유지). 캠페인 노출은 발급 소진 ≠ 종료(`src/lib/campaign-visibility.ts`). 관심 목록은 캠페인 단위(`db.interests`, `/r/interests`). 스키마 라벨은 형식 마스크(`000,000원`/`00건` — `SBUI`), 금액은 원 접미(₩ 접두 금지).
- 스토리보드 모드: `src/lib/storyboard.ts`의 `STORYBOARD` 플래그 (true = 스키마 라벨 렌더, 시드 버전 1000+ 계열).
- **최종 확정 정책 (2026-07-08, `dev` 브랜치)**: 사장님 화면에 체험자 **등급·실명 절대 비노출** — **2026-07-31 개정: 익명 ID(#last4)도 비노출**(리뷰/로그/스캔/알림/예약 화면은 거래 단위 번호 `passRefNo`(정본 `src/lib/owner-review-status.ts`)로 구분 — **단 2026-08-03: 사장님 예약관리·예약 정보 화면은 예약번호 표기 자체 삭제**(구분은 일시·캠페인명·매장명, 리뷰/로그의 체험권 번호는 유지), 체험자-예약·리뷰 연결·식별정보(이메일·휴대폰)는 어드민 전용) · 부스팅(우선 등급) 폐기 — `distributeQuota` 균등 · **사업자 인증**(Owner.bizStatus, pending이면 /o/* 대기 화면, 인증은 /admin/owners) · 캠페인명 = 사장님 관리용(체험자는 매장명 노출) · 필수 메뉴 선택 입력·최대 5개 · 지역 3km·지도 '이 지역 재검색'은 `src/lib/geo.ts`(하버사인·regionCenter) 기준 · N등급 5단계 유지는 **2026-08-06 6단계 개편으로 개정**(위 등급 메모 참조). 미확정 항목 제안: 운영정책서 §13.
- **추가 작업 리스트 반영 (2026-07-10, `dev`)**: ① 파생 표시 상태는 `src/lib/pass-display.ts` 단일 정의 — **PassStatus를 확장하지 않는다**(overdue="제출 기한 초과"/resubmit_expired="재제출 기한 초과"는 렌더 시점 계산, 탭 분류는 실상태·칩/뱃지만 파생 상태) ② **추천순** = `src/lib/recommend.ts`(issued_out 최후 → 사장님 `PLAN_RANK` Premium>Standard>Basic>Free → 최신순) — 사장님 멤버십 기준 노출 우대이지 리뷰어 등급이 아니므로 P1 무관 ③ 시드 useCode 전 캠페인 **"1234"** 통일(QA 기준) · **코드 중복 허용**(2026-07-28 — 매장 확인용·사용 처리는 패스 단위 대조, 고유성 검증 삭제) · **캠페인 종료 = 생성일 기준 n일차 자정(KST) 직전**(2026-07-28 — 생성일 00시+n일−1ms) · 4자리 코드 오입력 **연속 5회 → 10분 잠금**(Pass.useCodeFailCount/LockUntil, 실패 상태 불변) ④ 게시 유지 **전 채널 90일**·별도 필수 동의(`keepAgreed`, 자가점검은 `selfCheckConditions()`=keep 제외 — ReviewForm·review route·시드가 반드시 공유) ⑤ 탐색 `?area=`(행정 기준점 3km)·`?ch=` URL 유지, 필터 시트는 draft+[적용하기](`explore/FilterSheet.tsx`) — 지역은 **3상태**(미선택 기본(전국)/현위치 토글 해제 가능(`?loc=me`)/지역, 상호 배타), 전국 축소(zoom<10) 시 시도 클러스터(`nearestSido`/`SIDO_CENTERS`) — 홈 '전체 리스트' 더 둘러보기 = `?scope=all`(전국 줌 7 시작·area 무시), '걸어서' = `?mode=list&sort=distance&area=`; **반경 3km 필터는 지도 전용**(리스트는 필터 시트 기준) ⑥ **전 지역 시드**: REGIONS 시군구 229곳 전부 매장·캠페인 1건씩(demo4 사장님) — 좌표 정본은 복합 키 **"{시도} {시군구}"** 기준 `geo.ts GUGUN_CENTERS`. **표기 라벨은 `regions.ts regionLabel`**(2026-07-12): 시군구명이 시도 간 중복이면 "{시도} {시군구}", 전국 유일하면 시군구 단독 — 단독 라벨도 regionCenter가 findSido→복합 키로 해석. SEED_VERSION 1018.
- **SNS 소개글 인증코드 연결 (2026-07-25 개편, 정본 `src/lib/sns-bio-verify.ts`)**: [연결하기] = 8자리 1회성 코드 발급 → **[복사] = 클립보드 복사 + 서명 쿠키 30분 무장 + 주소 입력 활성화**(2026-07-28 [인증하기] 버튼 흡수 — 무장 중 재클릭은 복사만·만료 후 재무장) → 소개글 맨 앞 코드 삽입 → [인증완료] = 즉시 크롤링 검증(미검출 "계정 인증 코드를 확인할 수 없습니다."+[재시도], 성공 verifiedVia "bio"·코드 소각). **인스타 인증 = 캡처 업로드+OCR(2026-07-28 확정)**: 서버 크롤 bio 접근 불가 → [업로드] 버튼으로 프로필 캡처(계정 ID+코드 노출) 업로드 → `confirm-image` API가 **NCP CLOVA OCR**(`src/lib/ncp-ocr.ts` — env `NCP_OCR_INVOKE_URL`/`NCP_OCR_SECRET`, 미설정 503, 스텁 `/ocr`)로 텍스트 추출 → ID+코드 동시 검출 시 점유 인증. **도메인 = Template 타입 확정(2026-07-30)**: Field 01=SNS 아이디·Field 02=인증코드 — 검증은 필드명(01/02)→필드 순서→전체 텍스트 순 후보 대조(General 호환), 실패 trace에 템플릿명·필드 추출값 노출(코드 소각·insta-index 등급 동일). 블로그·틱톡은 크롤 인증 유지. **구 크롤 다층화(2026-07-27 — 인스타 UI에선 미사용·도먼트)**: 인스타는 비로그인 크롤에 로그인 벽 반환 → ① 앱 API 호스트(`i.instagram.com` web_profile_info, `INSTAGRAM_APP_API_BASE`) ② 웹 호스트 web_profile_info(x-ig-app-id) ③ 프로필 HTML ④ insta-index 응답 원문 ⑤ **브라우저 렌더**(`sns-bio-browser.ts` — headless Chromium으로 모달 X(실측 class `.xdg88n9…`+aria-label 폴백) 닫고 DOM 검출 · Vercel=@sparticuz/chromium(`outputFileTracingIncludes`로 bin 포함)·로컬=`PW_CHROMIUM_PATH` — **단 데이터센터 IP는 로그인 페이지 리다이렉트로 검출 불가 확정(2026-07-28)**, 확정 해법=insta-index 응답에 biography 추가 시 ④층 즉시 검출) 순 검사(틱톡은 ①HTML ②index 폴백) — 실패 시 **층별 진단 trace**를 422 응답·로그·시트 에러 하단에 노출(내부 QA). **등급 산정 전 채널 자동**: 네이버 블로그 = blog-analyzer(**실 스키마 확정 2026-07-28: grade=`influence.grade` · 일 방문자=`blog.visitor_trend.current`**(폴백 `metrics.daily_visitors_est`→daily 키 딥 서치) — 확정 경로 우선+딥 서치 폴백(중첩·"C등급" 장식·쉼표 문자열 수용), **총 방문자 `blog.profile.total_visitors`·subscores 점수·mate.grade는 배제**, 실패 시 원문 로그 — INDEX_BANDS·재평가 지수와 일 방문자 규모로 정합) · 인스타/틱톡 = insta-index(POST /api/analyze·/api/tiktok, body {username} → followers·score, **score 밴드 S88/A78/B66/C52/미만 N** — 스펙 D38+는 5단계 체계상 N 흡수) → `SnsAccount.apiGrade`·influence(channelGradesFromSns가 공식보다 우선, 장애 시 소프트 실패·수동 입력 없음). 크롤·분석 베이스 env 오버라이드(`*_CRAWL_BASE`/`BLOG_ANALYZER_BASE`) + 스텁 `scripts/sns-bio-stub.mjs`. 구 OAuth/데모 검증 API는 잔존(도먼트).
- **SNS 채널 연동·검증 (2026-07-10)**: 채널 관리 `/r/me/channels`(연동/해제/본인 인증) — OAuth 코어 `src/lib/sns-oauth.ts`(네이버/Meta/틱톡 로그인, state CSRF·**토큰 미저장**), 키 미설정 시 데모 검증(`/r/channels/verify`, verifiedVia="demo"·실키 시 403). verified는 신뢰 표식 — **참여 게이트는 연동 여부뿐(P1)**. 해제 = 등급 재계산·진행 패스 유지. 서버리스 인스턴스 불일치는 `src/lib/sns-cookie.ts` 쿠키 스톱갭(체험자 화면 6곳+발급 API가 `effectiveChannelState()` 병합)으로 본인 시점 즉시 반영 — cross-actor는 KV 필수.
- **레뷰 벤치마크 — 배송형·포인트 (2026-07-12, `dev`)**: 근거·적용/보류 판정은 `docs/벤치마크-레뷰.md`(구매평·페이백은 P4 리스크로 보류 — §13 #18). ① **배송형** `CampaignKind "delivery"`(`flags.ts DELIVERY_ENABLED=true` — "MVP 방문형만"의 범위 개정): 신청 시 배송지 필수(`Pass.shipping` — **발송 목적 한정으로 사장님 발송 큐에만 노출**, 익명 원칙의 명시적 예외·등급은 계속 비노출), active 기한=캠페인 종료일, 사장님 발송 처리(`/api/owner/ship`)가 usedAt 세팅 → **발송 후 7일** 리뷰(기존 스윕 재사용), 발송 후 취소 불가. 탐색 배송형 세그먼트는 **리스트 전용**(`?tab=delivery` — 지도·거리·지역 필터 미적용). ② **포인트** `src/lib/points.ts`(정본: 운영정책서 §14): 적립 = 배송형 리뷰 **검수 승인만**(P4) — `pointReward × SUPPORT_MULTIPLIER`(P1·100P 반올림), 원장 `PointTxn` append-only(잔액=합산), 출금(P5) 최소 1만P·1천P 단위·수수료 500원·**사업소득 3.3% 원천징수+소액부징수(세액 1,000원 미만 0)** — 세액은 신청 시점 확정 보존, 미리보기와 서버가 `quoteWithdrawal` 공유. 처리 큐 `/admin/points`. **출금 = 계좌 본인 인증 필수**(2026-07-12 고도화): KFTC 오픈뱅킹 계좌실명조회(`src/lib/openbanking.ts` — 2-legged 토큰 메모리 캐시·생년월일은 요청에만 사용·미저장, 키는 `.env.local`의 KFTC_CLIENT_ID/SECRET, 미설정 시 데모 인증 폴백, `KFTC_API_BASE`로 스텁 `scripts/kftc-stub.mjs` 검증) — [본인 인증하기]→예금주 대조→[출금 요청] 2단계, HMAC 증빙 쿠키 10분을 출금 API가 재검증. **테스트베드=시뮬레이터라 실계좌 조회 불가**(KFTC 구조적 제약 — 등록 테스트 데이터만 응답, 코드로 우회 금지): 실계좌 인증=운영망+이용기관 승인 운영키뿐. **API 베이스 기본값(2026-07-16): 배포(Vercel/production)=운영망 openapi · 로컬=테스트베드**, `KFTC_API_BASE`로 오버라이드 — 테스트베드용 키로 운영망 호출 시 토큰 발급부터 실패(망·원인 화면 표시). 테스트베드 연결 중엔 출금 폼에 "테스트베드 모드" 배너 노출(`isTestbedBase()` — 운영망 자동 소멸). ③ 방문형 `reservationRequired` 옵션(예약형 라이트 — 선정 절차 없음) ④ **배송형 카테고리는 플레이스 분류가 아니라 상품군 분류**(`src/lib/delivery-categories.ts` 단일 정본 — 식품/뷰티/리빙/패션잡화/디지털/키즈·펫/건강, `Campaign.productCategory` 배송형 필수·생성 API 검증) — 탐색 배송 칩·배송 필터 시트(지역/참여 방식 미노출)·생성 폼 공유, `?dcat=` URL 유지 ⑤ **참여 방식 필터**: 방문형 필터 시트에 전체/바로 방문/예약 필수(`?v=`, 단일 선택, 지도·리스트 공통) + 카드 "📅 예약 필수" 배지 — 배송은 세그먼트 자체가 방식(3방식 각각 필터 가능) ⑥ **내 체험권도 세그먼트 분리**: 배송 패스는 방문형에 섞지 않고 배송형 세그먼트(서브 탭 "신청 내역"·발송 대기 칩)에서만 노출(`PassesView`). SEED_VERSION 1020.
- **리뷰노트 벤치마크 — 예약형·배송형 구체화 (2026-07-16, `dev`)**: 근거·판정은 `docs/벤치마크-리뷰노트.md`(선정 절차·구매형·노쇼 배상은 보류 — 즉시 발급 유지·P1/P4). ① **예약형**(`reservationRequired`, 정본 운영정책서 §15·`src/lib/reservation.ts`): 신청 시 희망 방문 일시 필수(오늘~14일·공통 슬롯 11:00~20:30) → `Pass.reservation{date,time,status requested|confirmed}` — **예약=일정 조율이지 선정 아님(사장님 일방 거절·취소 금지 — 취소 결정권은 체험자)**, **v2(2026-07-16 회의): 예약 확정 전 QR·코드 미노출 + 사용 API 게이트**(use/use-by-code — 참여(발급)는 즉시라 P1 유지), expiresAt=**방문 희망일 +1일** 말 KST(2026-07-17 — QR 다음날까지 유지, 72h의 명시적 예외)·신청 시 **방문 인원수(1~10) 필수**(`reservation.partySize` — 사장님 큐 표시), 사장님 홈 예약 큐 = [예약 확인](`/api/owner/reserve-confirm`) + **[다른 시간 제안]**(`/api/owner/reserve-propose` — 슬롯 최대 3+수기 안내사항 200자 원문 노출, 익명 #last4), 체험자 응답(`/api/passes/reservation-respond`, 체험권 상세 라디오 최대 4행=제안 3+기타 직접 입력): 수락=확정·QR 활성/기타=**재제안(1회)**/**거절=취소(패널티·12h 쿨다운 없음** — `Pass.cancelledVia:"proposal_declined"`), **v3(2026-07-16): 협상 히스토리 `reservation.history`**(append-only — 양측 타임라인 노출·횟수 판정 정본, `reservationHistoryLines`) + **제안 서로 각 1회**(사장님 propose 1회→체험자 counter 1회→사장님 [예약 확인]/[거절]`/api/owner/reserve-decline`(재제안 후에만·취소·패널티 없음), 서버 400 검증), 예약 변경 `/api/passes/reservation`(확인 대기 복귀·기한 재계산·히스토리 request 기록·proposed 중 불가), `Campaign.reservationNote`(예약 안내 80자 — **2026-08-03: 생성 폼 인풋 제거**, 신규 캠페인 미입력·기존 캠페인/시드 표기용 필드만 유지). **사장님 홈(v3)**: 최상단 이번 달 모집 현황+플랜 통합 카드·'최근 후기' 카드 제거·[방문 예약|발송 대기] 내부 탭(`HomeQueues`) ② **배송형 구체화**: `Campaign.productOptions`(최대 5, 설정 시 신청 택1 = `Pass.shipping.option` — 발송 큐 표시), 발송 처리 택배사 선택(`Pass.courier`, 정본 `src/lib/couriers.ts` — 조회 URL로 [배송 조회] 링크), **발송 지연 3일**(`SHIP_DELAY_NOTICE_MS`) 경과 시 양측 표시 전용 안내(상태 전이 없음 — 리뷰 기한은 발송 후 롤링이라 불이익 없음 명시). 사장님 홈 예약 큐는 **신청이 있어야 렌더** — 시드에 데모 예약 3건(확인 대기·제안 응답 대기(체험자=demo)·확정) 포함. **캠페인 사진**(2026-07-17): `Campaign.photos` 3~20장 필수(생성 폼 업로드·640px 리사이즈, [0]=대표) — 탐색 카드 캐러셀 140×105·상세 풀폭 4:3 캐러셀·검색 100×75·체험권 88×66, 미보유 시드는 `photosForCampaign` 폴백 3장. SEED_VERSION 1024.
- **예약형 체험 시스템 (2026-07-22, `release/main-no-delivery`)**: 정본 운영정책서 §15 · `src/lib/reservation.ts`. ① 캠페인 유형 = 방문형|예약형(|배송형) — 예약형은 **`Campaign.reservationSchedule`**(요일·운영시간 30분·브레이크 단일 구간·예약 가능 시작일 `opensAt`(**2026-07-23 정정: 신청 게이트가 아니라 방문 날짜 하한** — 캘린더에서 하한 이전 날짜만 비활성, 신청은 즉시 가능, 14일 윈도우 기준점도 하한일로 이동 — **2026-08-03: 오픈 당일 방문 예약 불가** — 실하한 = max(opensAt, 오픈일+1)(`reservationOpenDate`), 미설정 시 내일부터·사장님 시작일 피커도 min=내일)·시간대 정원 `slotCapacity` 1~5(기본 1, §15.8-A 기본안)) + **`Campaign.reservationBlocks`**(날짜/시간 차단·당일 일시중지 `pausedDate`=오늘만 유효 — 자정 자연 해제, 예약형 전용·기존 확정 예약 자동 취소 없음) ② 검증 단일 정본 = `validateReservationForCampaign`(발급/변경/재제안/수락 공유 — 요일·브레이크·차단·정원·과거·오픈일·종료일), 선택지 빌더 = `buildReservationPicker`(비활성 사유 포함 — 클라이언트는 렌더만) ③ **예약 변경 1회**(`reservation.changeUsed` — counter 1회와 별개), 확정 후 변경 불가 ④ **사장님 거절 확정 전 상시 허용**(v3 '재제안 후에만' 개정 — `owner_declined`)+**확정 취소 사유 = 4지선다+직접 입력**(2026-08-04 — 예약 가능 시간 착오/예약 인원 확인 착오/메뉴·상품 준비 불가/매장 운영 문제/직접 입력, `Pass.cancelReasonCode` 데이터화·`/admin/reservations` 사유별 통계 · 체험자 안내 = 정제 사과 2~3줄 `ownerCancelledReviewerCopy`(상세·목록·알림 공유, 단답 "(사유: …)" 폐기) · **매장 패널티: 확정 취소 건 = 사용 처리 간주·모집 슬롯 미복원**(restoreQuotaSlot 미호출·취소 UI 사전 고지, §15.3)), 운영자 취소 `/api/admin/reservations/cancel`(`admin_cancelled`) — **12h 재신청 제한은 cancelledVia 없는 체험자 직접 취소만**(판정 `!p.cancelledVia` — **취소 패널티 3분안(2026-07-23)**: 제안 전(requested)·확정 후 직접 취소 = 12h / **proposed 중 직접 취소는 cancel API가 `proposal_declined`로 매핑해 무패널티**(거절 버튼과 동일 처리) / 사장님 거절·취소 = 무패널티), 취소 서브 문구 = `cancelledCopy`(§15.4) ⑤ 체험자 확정 후 취소 = 방문 전날 23:59까지(`reviewerCancelBlockedReason`) ⑥ **12시간제 통일**(`fmtTime12` — fmtReservationLabel도 "오후 2시") ⑦ 스윕: 미확정+방문 시각 경과 = 자동 취소(owner_declined·§15.8-B), 신청 24h 사장님 리마인드·방문 전날·만료 24h 전(플래그 ownerRemindNotified/visitRemindNotified/expiringSoonNotified) ⑧ **리뷰 기한 = 확정 방문일 +7일**(`pass-lifecycle.ts reviewDeadline` — 예약형만, 일정 변경 시 자동 재계산) ⑨ 사장님 **캠페인 상세 `/o/campaign/[id]` = 언더라인 탭 [캠페인 관리|예약 관리|후기]**(2026-07-31 시안 개편 — 캠페인 관리 탭 = 사진 캐러셀([0]=대표사진)·배지(방문형 다크 칩+바로 방문 그린/예약 필수 오렌지)·모집 마감 n일 전(**종료 시 "모집마감"**)·현황 타일 2×2(방문 예정|사용 완료 n/총, 지원금|매장 확인 번호 — 시안 블루→brandSoft·금액 검정)·**[캠페인 종료하기](미종료만·확인은 바텀시트)** + **예약형은 '예약 운영' 섹션**(구 '상태관리' 탭 흡수 — 운영 정보 카드(요일·시간·브레이크·정원·시작일, 종료 후에도 유지) + 미종료만 **당일 예약 일시 중지 토글·날짜 차단 칩·특정 시간 차단 [일정 선택] 시트**(`BlocksManager`) — **종료 캠페인은 차단 카드 미제공**) + 캠페인 기본 정보(모집 인원·유형·방식·진행 일수·모집 기간)·모집 조건(SNS 채널·필수 메뉴·필수 키워드 칩)·매장 정보(카테고리+매장소개 `ExpandableDesc` 더보기), [예약 관리] 탭 = [관리]-[예약관리] 동일 기능(공유 빌더 `manage/reservation-items.ts`)을 캠페인 범위로·매장 필터 없음·예약형·미종료 전용, 확정 취소(사유)는 예약 정보 상세 confirmed 상태(2026-07-28 홈 개편으로 이관), 방문형·배송형·종료는 [캠페인 관리|후기] 2탭), 어드민 **예약 탭 `/admin/reservations`** ⑩ 체험자 이력 = `reservationHistoryLinesForReviewer`(propose 미누적)·확정 후 접힘, 사장님/운영자는 전체 유지. 확정 필요 A~F 기본안(승인 대기)은 §15.8 표. SEED_VERSION 1026(스케줄·차단·피부과 오픈일 데모). **예약 신청 플로우(2026-07-23 시안)**: 상세 CTA "예약 요청하기" → `ReserveSheet`("언제 방문할까요?" — 채널·등급·지원금 요약+[예약 날짜]캘린더 모달+[예약 시간]오전/오후 칩 모달+인원 스테퍼 1~10), 상세 배너 2줄(퍼플 기한+오렌지 확정 안내)·리뷰 마감 "방문 후 7일 이내"·유의사항 예약형 카피.
- **체험 서비스 회의 확정 (2026-07-12, `dev`)**: ① 카드 배지 정리 — '참여 중'·'마감 임박' 배지 금지(상태는 상세 CTA로), 필터 개수 숫자 배지 금지(아이콘 강조만), 카드 지역 정보는 `regions.ts regionFromAddress`("1차 2차") — '걸어서'=잔여+거리, 홈 전체 리스트=지역, 탐색=카테고리·지역·거리 ② **지도 재검색 ↔ 지역 필터 동기화**: 두 지도 컴포넌트의 `onRegionChange` 콜백 — 재검색=`geo.ts nearestRegionLabel`(최근접 시군구)로 지역 필터 변경+적용과 동일(SNS·카테고리 유지), 전국 줌아웃/전체 보기=필터 해제 ③ 홈 설정 지역 → 탐색 진입 기본값(`recent-local.ts cp_home_area_v1`, URL 지역 파라미터 없을 때 1회) ④ 검색: `/r/search` 통합 검색(전체 DB·같은 페이지 결과·최근 검색어 유지) vs 탐색 내 '현재 결과에서 검색' 분리 — 공통 헤더 검색 아이콘은 전 화면 통합 검색 ⑤ 발급 모달 '꼭 확인해주세요' 금지 — 정책 안내는 상세 하단 **유의사항** 단일 위치(90일 유지 포함, 리뷰 조건에는 keep 미노출) ⑥ QR 인증 화면에 참여 취소 없음(리스트에서), 취소 알럿 = 핵심+12h만 ⑦ 코드 오입력 잠금 **5회→3분**(재잠금 동일 3분, 단계 증가 금지) ⑧ 리뷰 제출: 'URL 플레이스홀더 공통'+형식 검증, 초대 카드(T1/T2) 금지. 미확정 A~F 기본안: 운영정책서 §13 #22~27.
- **체험자 가입·간편로그인 (2026-07-23, 정본 운영정책서 §8.1)**: **휴대폰 번호 = 체험자 PK**(`Reviewer.phone` — 가입 시 인증번호 필수·번호당 1계정) — `src/lib/phone-verify.ts`(OTP 6자리·5분 → 증빙 15분, **서버 미저장** AUTH_SECRET 서명 쿠키 왕복, 가입 API 재검증 403), SMS는 SOLAPI 3키 설정 시 실발송·미설정 시 데모 모드(코드 화면 배너). **간편로그인** = 네이버·카카오 `src/lib/social-login.ts`(`/api/auth/social/[provider]/start|callback` — `Reviewer.social` 고유 ID만 저장·토큰 미저장, 미가입은 신원 증빙 쿠키 후 가입 플로우 합류, 키 미설정 시 `/r/login/social-demo` 데모 폴백·실키 시 데모 콜백 403). 가입 단계 SNS 자기신고 폐지 — 완료 시 `/r/me/channels?welcome=1`로 연동·본인 인증 유도. 소셜 가입은 비밀번호 없음(무작위 해시)·이메일 프리필. SEED_VERSION 1027(데모 계정 phone).
- **사장님 [관리] 탭 (2026-07-28, 화면 개편 1단계)**: 바텀 네비 2번째 `/o/manage`(calendar-check 아이콘) — 헤더 토글 [캠페인|예약 관리|**리뷰 관리**(2026-08-03 병합 — 구 단독 메뉴 흡수·`/o/reviews`는 `?tab=reviews`(+`st`) 리다이렉트·`ReviewsPanel`, **바텀 네비 4개**: 홈·관리·QR 스캔·마이)]. 캠페인 = 유형 세그먼트(방문형·배송은 플래그, 기자단 미노출)+전체/진행중/종료 칩+홈과 동일 카드(**생성 링크 없음** — 새 캠페인은 홈 전용, 2026-07-28). 예약관리 = 매장 셀렉터+[전체|요청|조율|확정|취소] 칩+상태별 카드(요청/재제안 [예약 확정]=reserve-confirm, 재제안 카드 = 원 일정 흐림+오렌지 제안 일정) — **[예약 정보] = 다음 depth `/o/manage/reservation/[passId]`**(2026-07-28 시안: 상태 칩·정보 행·[예약 확정]+[거절(즉시 처리·"취소되었습니다" 모달)]·[다른 일정 제안(캘린더+오전/오후 시트·오렌지 슬롯 칩·최대 3)]·예약 내역 = reservationHistoryCards 재사용) — **제안·거절·확정 취소는 홈 [방문 예약] 큐 유지**, 시안 블루 액센트는 v2 규칙(퍼플=인터랙션)로 치환. **홈 개편(2단계 2026-07-28)**: 로고(매장별 드롭다운 필터는 2026-08-04 제거 — 홈은 전 매장 기준, 마이페이지 프로필 카드도 이메일 제거·'대표매장'+매장명+카테고리로 정리) · 현황 카드(방문 예정/사용 완료/후기 검수 대기 + 모집 한도 프로그레스·플랜 — **2026-08-03 잔여 카운팅**: "n 남음 / 한도"로 감소 표기, 게이지 색 = 잔여 50% 초과 퍼플/11~50% 노랑/1~10% 빨강(숫자도 빨강)/0 = errorSoft 배경+안내+[플랜 올리기] CTA(Premium은 리필 CTA)) · [새 캠페인 등록|예약 관리(→/o/manage?tab=reservations)] · 진행 중인 캠페인(유형 칩+신형 카드: 즉시 입장/예약 필수 배지·D-n·**썸네일 88×66**(2026-08-05 — 체험권 화면과 동일 비율, 매장 정보 카드도 통일)·타일) + 전체보기→[관리] — **구 홈 큐(HomeQueues/ReservationQueue/ShipQueue) 삭제**, 예약 처리 전부 [관리]·상세로 이관. **새 캠페인 등록 개편(2026-07-31 시안)**: 섹션 = 매장→캠페인명→총 모집 인원(플랜 카드 — 2026-08-04 홈과 동일 "n 남음 / 한도" 잔여 카운팅+단계 색 게이지)→캠페인 유형(방문형|배송형)→**캠페인 방식(바로 방문|예약 필수 2카드)**→진행 일수(7/14/30 칩+**직접입력 7~30일**(2026-08-03 — 서버도 동일 클램프)+마감일 안내)→사진→SNS 채널(+채널별 리뷰 작성 조건 토글)→필수 주문 메뉴(카드형)→필수 키워드→매장소개→지원금 ⓘ→매장 확인 번호 ⓘ→권한 확인→[등록하기] + **이탈 확인 모달**(입력 존재 시 뒤로가기). **매장 선택 = 대표 매장 기본**(`Owner.primaryStoreId` — 지정은 마이페이지 [매장 정보] `/api/owner/primary-store`, 미지정 시 첫 매장) + **[URL로 매장정보 불러오기] = 조회 전용**(`POST /api/owner/stores`가 정보만 반환·DB 미등록 — 임시 항목 "(URL 불러옴)"은 화면 세션 한정·이탈 시 휘발, 캠페인 등록 제출 시 `newStore`로 저장·같은 naverPlaceId 재사용·검증 실패 시 미저장).
- **결제 주기 (2026-08-03 확정, 정본 `src/lib/billing-cycle.ts`)**: 모집 한도 기산 = **캘린더 월 폐기** — 유료 = **결제(플랜 시작/변경 = `Owner.planStartedAt`, plan API가 갱신) 시점~재결제 전**, Free = **가입일 기준 익월 동일일 −1일**(예: 07.25 가입 → 07.25~08.24 유효, 08.25 재갱신 · anchor 일자가 없는 달은 말일 클램프). 캠페인 생성 검증·owner/me·사장님 홈이 `billingCycle(owner)`를 공유하고, **리필 가산분도 사용한 주기까지만**(`refillBonus`가 주기 윈도우 판정). 홈 카드 = **"모집 현황" + 우측 기간 표기**(`cycleLabel` — "07.25 ~ 08.24"), '이번 달' 카피는 '이번 주기'로 정리.
- **사장님 리뷰 현황 (2026-07-31, 정본 `src/lib/owner-review-status.ts`)**: 상태 = **작성 대기**(used — 기한 초과는 포함+배지)/**검수중**(review_submitted)/**검수 완료**(completed)/**반려**(rejected) — 라벨은 2026-08-04 시안 개정("검수 중"→"검수중"·"완료"→"검수 완료"·"재작성 요청"→"반려", 상태 정의·집계는 §4-1 그대로) · **모수 = 이용 완료 체험권**(모집 인원·미사용 발급분 제외, 캠페인 종료여도 개별 기한 내면 작성 대기 유지) · 홈 스탯 **"검수 중인 리뷰"** = 검수중만 · **리뷰 관리**(구 '후기' 메뉴 — 2026-08-03 `/o/manage?tab=reviews`로 병합, 구 `/o/reviews`는 리다이렉트 — **2026-08-04 시안 개편**: 인트로 카드(리뷰 모니터링+[채널톡 문의하기] 단일 진입 = `ReviewActions` 재사용·카드별 문의 버튼 제거)+요약 타일(이용 완료|작성 대기·검수중·검수 완료 3분할)+필터 칩(전체/검수중/검수 완료/반려 — 작성 대기는 칩 없이 전체 노출·`?st=` 딥링크 유지)+카드(캠페인명 2줄·상태 칩·채널 파스텔 배지·날짜·[리뷰 보러가기]·본문 3줄·상태 밴드: 검수중 "영업일 기준 최대 3일 이내로 검수 완료되어요"/반려 = 반려 사유)), 작성 대기 카피 "체험자가 아직 리뷰를 제출하지 않았어요…"/기한 초과 "운영팀에서 확인 및 안내를 진행합니다"(**독촉 기능 없음**) · 캠페인 상세 **[리뷰] 탭** = "이용 완료 n건 / 작성 대기·검수중·검수 완료" 동일 집계(§4-7 정합) · 용어 통일: 사장님·어드민 화면 **후기→리뷰**.
- **멤버십·쿠폰함 통합 (2026-08-10 설계안, 정본 기능정의서 2.04)**: `/o/membership` = 다크 히어로(플랜+결제 방식 칩·다음 결제)→이번 주기 모집 게이지(홈 정본 공유)→한도 업셀 배너(직전 3주기 월평균 ≥ 한도 80%)→{플랜} 혜택 카드(`PLAN_BENEFITS` — 캐치패스·캐치랭크 한 카드·§8 우선순위 · **캐치랭크 수치 = 멤버십 안내 표 원문**(2026-08-10 — 키워드 등록 10/100/500개·순위 알림톡 5/20/30건·누적 세부로그·내가 쓴 캠페인 1% 적립, 키워드 분석 5/20/**50**건·Free 캐치랭크 혜택 없음))→리필권 섹션(구 쿠폰함 흡수 — `/o/coupons`는 #refill 리다이렉트)→관리 리스트(연간↔월간 전환 `BillingSwitchRow`). 플랜 비교 `/o/membership/plans` = 결제 방식 토글·**할인율 표기 금지(절감 금액+2개월 무료)**·연간 탭 하단 고정 "언제든 월간으로 변경". `Owner.billing`(monthly|yearly — 별개 상품 아님·연간가 = 월가×10 `yearlyPrice`), billing만 변경 시 planStartedAt 미갱신(모집 주기 유지).
- **모집 한도 리필권 BM (2026-07-31 채택, 정본 `src/lib/limit-refill.ts` · 운영정책서 §3.1)**: 12,900원/장 · 지급 = 구매 시점 플랜 월 한도(Basic 15/Standard 50/Premium 100) · **Free 미판매**(Basic 업그레이드 유도) · **쿠폰형** — 구매 = 발급(자동 적용 아님, `LimitRefill.usedAt/usedMonth`) → [지금 쓰기]/[나중에 쓰기], 사용 시 그 **결제 주기** 한도 가산·**가산분 해당 주기 한정**(2026-08-03 주기化)(미사용 쿠폰은 계속 보관) · 구매 횟수 무제한(주기 2회↑ Premium은 `/admin/refills`에서 Enterprise 후보 식별·매출은 구매 시점 기준 수기 청구) · 진입 = 홈/새 캠페인 등록 한도 카드 **[리필하기]**(`src/components/RefillFlow.tsx` — 보유 無 구매 시트(CTA **[구매하기]** 금액 미표기, 2026-08-04)·구매 완료 안내와 보유 쿠폰 사용 확인("보유한 리필권 n개 중 1개를 사용할까요?")은 **중앙 모달**(2026-08-04 — 시트 아님)) + 마이페이지 **쿠폰함 `/o/coupons`**(구매·보유 [사용하기]·사용 내역 — 멤버십은 링크만) · **표기 = 기본 플랜 한도 기준**(리필 누적 비노출 — 표시 사용량에서 리필분 차감 → 게이지 리필, 생성 검증·잔여 판정은 리필 포함 실한도) · 한도 소진 업셀(생성 폼 — **2026-08-04 인라인 섹션 → 진입 시 바텀 시트 1회 자동 오픈**(타이틀+X·추천 카드·리필권 구매 카드([리필권 구매]=mode buy 고정)·보유 시 [보유한 리필권 사용] 카드 — 2026-08-05 구매/사용 분리)): Free=Basic 업그레이드 / Basic·Standard=추천 업그레이드+보조 리필(Standard "100원 차이" 카피) / Premium=리필 메인.
- **게스트 브라우징 (2026-07-24)**: 홈·탐색·검색·매장 상세는 비로그인 공개 — `getReviewerOrNull`(server-helpers) + `(app)` 레이아웃 게이트 제거(**신규 페이지는 반드시 자체 방어**). 게스트는 금액류 "로그인 후 확인" 마스크·홈 stat 3종 "회원 전용"·상세 CTA [로그인 하러가기]. 로그인 복귀 `?next=`(내부 /r/만, 소셜은 `cp_login_next_v1` 쿠키). 랜딩 체험자 카드 = /r/home 직행.
- 시크릿: env 전용 (AUTH_SECRET은 production fail-closed, Naver 키 폴백 금지).
- 빌드 검증: `npx next build` (unused import/var는 빌드 실패).
