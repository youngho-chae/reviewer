# CATCHPASS 설계 문서 세트 (기능정의서 + 화면설계서)

> 문서 계층: **PRD(제품 요구) → 기능정의서(화면별 기능 요구·제약·디자인 의도) → 화면설계서(스토리보드/시각화)**.
> 디자이너·프론트엔드는 **기능정의서를 단일 기준**으로 삼고, 화면설계서로 배치를 확인한다.
>
> - **기능정의서**: [`기능정의서.md`](./기능정의서.md) — IA 전 화면의 요구사항 ID 계층 + 정량 제약(최대/최소·자릿수·기간) + 디자인 의도(토스트·고정영역·키보드·캐러셀). FE·디자이너 공용 원본.
> - **화면설계서(SDD)**: Figma `MCP-TEST` — https://www.figma.com/design/gzLEmvFbtwhktwEGN1En3F/MCP-TEST · 생성 스크립트는 아래 §3.
> - 두 문서 모두 PRD v3.0(`PRD.md`)·구현 코드와 1:1 (카피 원문주의).

## 1. 현재 Figma에 구축 완료된 것 (2026-07-06)

대상 파일: `gzLEmvFbtwhktwEGN1En3F` · 페이지 `TEST` (`72514:68633`) — 편집 권한 계정으로 연동해 생성 완료.
8개 화면설계서 보드가 `TEST` 페이지에 x축으로 나란히(1460px 간격) 배치되어 있다.

| 보드 | 노드ID | x | 내용 |
|---|---|---|---|
| `BOARD · R-00` | `72537:2` | 0 | 가입·온보딩 (3-step + 필수 동의) |
| `BOARD · R-03` | `72538:2` | 1460 | 매장 상세 · 채널 선택 참여 |
| `BOARD · R-04a` | `72539:2` | 2920 | 체험권 상세 (7-상태 분기 + 리뷰 폼) |
| `BOARD · O-10` | `72541:2` | 4380 | 새 캠페인 생성 |
| `BOARD · O-02` | `72542:2` | 5840 | 사용 처리 (QR/4자리) |
| `BOARD · AD-01` | `72543:2` | 7300 | 후기 검수 콘솔 |
| `BOARD · R-01` | `72559:2` | 8760 | 홈 (큐레이팅 · 발견 전용) |
| `BOARD · R-02` | `72560:2` | 10220 | 탐색 (리스트뷰 + 지도뷰) |

각 보드 하단에는 **🎨 디자이너 작업 가이드(v1.2)** 섹션이 부착되어 있다 — A 요소별 데이터 정의(형식·예시·한계값·빈값 처리) ·
B Default 상태(첫 진입) · C 인터랙션 동작(트리거→결과) · D 디자인해야 할 화면 상태(V# Variants, ★Default 표시) ·
🔒고정/🎨자유 범위. 원본 데이터와 재적용 스크립트는 `figma-scripts/03-designer-guide.js`.

각 보드는 레퍼런스 형식을 따른다: **다크 DocHeader**(화면ID 칩·화면명·라우트·Device/Ver/작성일/작성자/상태 칩)
+ **390px 목업**(정책이 걸린 요소마다 번호 핀) + **6열 Description** + **예외·유효성 표**(실제 카피·HTTP 코드)
+ **상태 칩** + **데이터·연동**(API·lib 함수) + **변경이력**. 모든 텍스트는 PRD v3.0·구현 코드와 1:1.

> 이전 파일(`E2eXCCZSZQ2zZnqKXNcCah`, help@ 계정 Starter)에 만들었던 Guide/Kit 페이지(작성 표준·ID 체계·
> 마커 원칙·칩 팔레트)는 그 계정 한도로 중단된 초안이며, 표준 내용은 본 README §4~6에 그대로 문서화되어 있다.

## 2. 재연동·한도 메모

- Figma MCP는 **인증 계정의 시트 등급**으로 한도가 갈린다: View/Collab = 월 6회, Full/Dev = 일 200회(Pro).
- 쓰기(`use_figma`)는 대체로 한도에서 제외되어, 편집 권한만 있으면 보드 생성이 가능했다.
- 계정 전환은 claude.ai 커넥터 재인증(시크릿 창 + 대상 Figma 계정 사전 로그인 권장)으로 처리한다.

## 3. 스크립트 사용법

각 스크립트는 Figma MCP `use_figma` 도구에 **그대로 붙여넣어 1회 호출로 보드 1개**를 생성한다.

```bash
cd docs/sdd/figma-scripts && ./build.sh   # data/*.js + generator.js → dist/board-*.js
```

| 파일 | 만드는 것 |
|---|---|
| `dist/board-R-00.js` | R-00 가입·온보딩 (필수 동의) |
| `dist/board-R-03.js` | R-03 매장 상세 · 채널 선택 참여 |
| `dist/board-R-04a.js` | R-04a 체험권 상세 (7-상태 분기) |
| `dist/board-O-10.js` | O-10 캠페인 생성 |
| `dist/board-O-02.js` | O-02 사용 처리 (QR/4자리) |
| `dist/board-AD-01.js` | AD-01 후기 검수 콘솔 |

- 각 `data/*.js`의 `pageId`는 대상 파일의 페이지 노드ID, `x`는 배치 위치. 다른 파일/페이지에 그릴 땐 이 둘만 바꾸면 된다.
- `generator.js`는 AnnotationPin을 **인라인 생성**하므로 대상 파일에 마스터 컴포넌트가 없어도 자체 완결로 동작한다.
- 서로 다른 x라 겹치지 않으므로 여러 보드를 병렬 호출해도 안전하다.

## 4. 보드 규격 (생성기가 강제하는 표준)

- **BOARD**: 1360px 고정 폭 · 흰 배경 · radius 24 · 세로 auto-layout · 프레임명 `BOARD · {화면ID}`
- **DocHeader**: 다크 바 — [화면ID 칩] 화면명 [라우트 칩] ←→ Device · Ver · 작성일 · 작성자 · [상태 칩]
- **좌측 목업(390px)**: 실제 앱 라이트 톤 와이어프레임. 정책이 걸린 요소마다 `AnnotationPin` 인스턴스(번호)
- **우측 Description**: 6열 (No./구분/명칭/기능 및 설명/예외·상태/이동·연관) — 핀 번호와 1:1
- **예외·유효성 표**: 상황 / 노출 문구(실제 카피 원문) / 처리(HTTP 코드)
- **상태 칩** + **데이터·연동**(API 메서드·경로·필드·lib 함수) + **변경이력**
- 폰트: Noto Sans KR(R/M/B) + Inter(Bold) — 파일에서 사용 가능 확인됨

## 5. 설계서 내용의 원본

각 보드에 들어가는 텍스트(정책·수치·카피·API)는 `figma-scripts/data/*.js`에 구조화되어 있다.
**이 데이터가 곧 화면설계서의 원문**이며, 전부 PRD.md v3.0 / 구현 코드에서 그대로 옮겼다
(카피 원문주의 — 서버 에러 문자열과 diff 가능).
Figma 없이 내용만 검토하려면 data 파일을 읽으면 된다.

## 6. 신규 화면 설계서 추가 방법

1. `figma-scripts/data/` 에서 기존 파일 하나를 복사해 DATA만 수정 (PRD §3의 ID·라우트 준수)
2. `./build.sh` → `dist/board-{ID}.js` 생성 → `use_figma`로 실행
3. Guide 페이지의 인벤토리 표에서 해당 행을 `✅`로 갱신
