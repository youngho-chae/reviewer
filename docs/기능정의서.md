# CATCHPASS 기능정의서 (Functional Requirements Definition) · VER.1

| 항목 | 값 |
|---|---|
| Service Area | CATCHPASS — 등급 기반 리뷰 체험권 플랫폼 (모바일 웹) |
| Project ID | CP-VER1 |
| Service ID | CP |
| 운영 주체(회사) | 캐치랭크(CatchRank) · 고객센터 help@catchrank.co.kr |
| 기준 문서 | 요구 [`../PRD.md`](../PRD.md) · IA [`IA.md`](IA.md) · 운영정책 [`운영정책서.md`](운영정책서.md) · 데이터정책 [`데이터정책서.md`](데이터정책서.md) · 시나리오 [`../flow.md`](../flow.md) · 구현 브랜치 `mvp/ver1` |
| 화면설계서(스토리보드) | Figma `MCP-TEST` — https://www.figma.com/design/gzLEmvFbtwhktwEGN1En3F/MCP-TEST (보드 목록 [`sdd/README.md`](sdd/README.md)) |
| 작성자 / 작성일 | PO / 2026-07-07 (v1.3) |

> **이 문서 하나로 프론트엔드 개발자와 디자이너가 추가 질문 없이 100% 이해·작업할 수 있도록 작성되었다.**
> §0~§7은 전 화면 공통 규약(용어·권한·IA·전역 UX·상태·알림·데이터·API·비기능·디자인 시스템·BM/법무),
> §8~§11은 화면별 기능 요구사항이다. 화면별 표를 읽기 전에 §0~§7을 먼저 숙지한다.
> 문서 계층: **요구(PRD) → 기능(본 문서) → 화면설계서(Figma)**. 데이터 모델·개인정보 상세는 [`데이터정책서.md`](데이터정책서.md), 검수·정산·법무·출시 운영 상세는 [`운영정책서.md`](운영정책서.md)가 정본.

---

## 0. 문서 사용 규칙 & 표기법

이 문서는 **IA(정보 구조)를 기반으로 각 화면에 반드시 들어가야 하는 기능 요구사항**을 정의한다.
**디자이너는 여기 정의된 기능·제약·디자인 의도의 범위 안에서만** 디자인하고,
**프론트엔드 개발자는 같은 표를 기준으로** 구현한다.

**기능내역(요구사항기능내역) 열 작성 규칙 — 아래 4요소를 반드시 포함:**
1. **위치·형태**: "ㅇㅇ 기능은 ㅇㅇ 화면 ㅇㅇ 위치에 ㅇㅇ처럼 들어간다."
2. **정량 제약**: 최대/최소 개수·자릿수·기간·시간(초)·금액 단위를 숫자로.
3. **디자인 의도**: 토스트 노출 시간, 고정 영역(bg fixed), 포커스·키보드 타입, 넘침 처리(캐러셀/스크롤), input 타입, 비활성 조건, 애니메이션 타이밍.
4. **데이터·연동**: 서버 호출 시 API 메서드·경로·필드·에러 코드.

**요구사항 ID 체계:** `CP_R_*` 체험자 · `CP_O_*` 사장님 · `CP_AD_*` 운영팀 · `CP_C_*` 공통.
요구사항(화면/기능영역) → 요구사항기능(개별 기능). 예 `CP_R_REQ4_4` = 매장 상세 화면의 4번째 기능(채널 선택 칩).

**개발가능여부:** ✅ 구현 완료 · 🔷 정책 확정·구현됨 · ⏳ 로드맵(미구현). 본 VER.1은 결제(PG) 외 전부 ✅/🔷.

**본 문서 문자열 규칙:** 큰따옴표(" ")로 감싼 문장은 **실제 UI 카피/서버 응답 원문**이다. 디자인·개발 시 **글자 그대로** 사용하며 임의 변경 금지(QA가 diff로 검증).

---

## 1. 용어 정의 (Glossary)

| 용어 | 정의 |
|---|---|
| 체험자(Reviewer) | SNS 채널을 연동하고 매장 체험 후 리뷰를 작성하는 회원. 진입 `/r/*`. |
| 사장님(Owner) | 매장을 등록·운영하고 캠페인을 개설하는 회원. 진입 `/o/*`. |
| 운영팀(Admin) | 리뷰 검수 전용 백오피스 계정. 진입 `/admin/*`. reviewer/owner 화면·보상 대상에서 제외. |
| 등급(Grade) | 채널 영향력 기반 5단계 **S / A / B / C / N**. 채널별로 독립 산정되며, 종합 등급 = 연동 채널 중 최상위. **[P1] 등급은 참여 자격이 아니다 — 모든 등급이 모든 캠페인에 참여하며, 등급은 지원금 배율(혜택 크기)만 결정한다.** |
| 채널(Channel) | 리뷰 게시 SNS. **네이버 블로그 / 인스타그램 / 틱톡 3종만**(유튜브 없음). 내부 키 `naver_blog`·`instagram`·`tiktok`. |
| 캠페인(Campaign) | 사장님이 개설하는 체험 모집 단위. 종류 = 방문형(visit) / 기자단(press). |
| 체험권(Pass) | 체험자가 캠페인에 참여해 발급받는 이용권. QR + (사장님용) 4자리 코드. 7-상태 라이프사이클. |
| 지원금(Support) | 체험권 사용 시 결제 금액에서 차감되는 **할인 금액**. **매장이 직접 제공**(회사 무정산). 기준 지원금 × 등급 배율. |
| 기준 지원금(supportAmount) | 사장님이 캠페인 생성 시 설정한 금액 = **S등급 100% 기준치(최대)**. |
| 등급 배율 | S 100% · A 80% · B 60% · C 40% · N 10%. 실지급 = 기준 지원금 × 배율(100원 반올림). |
| quota / used | 캠페인의 등급별 슬롯 배분(quota)과 발급 소진 수(used). **참여 자격이 아니라 배분 기록** — 발급 시 자기 등급 버킷 우선 차감(N은 C 버킷부터), 소진 시 잔여 버킷 사용. 총 잔여 0일 때만 "마감". 만료/취소 시 복구. |
| consumedSlot | 발급 시 실제 차감한 슬롯(자기 등급 버킷과 다를 수 있음). 만료·취소 시 이 슬롯을 복구하는 근거 필드. |
| 부스트(support_bonus_pct) | 초대 보상. 다음 체험권 **사용 처리 시** 지원금 한도에 +N% 가산(상한 = 기준 지원금 S100%). |
| 멤버십(Plan) | 사장님 구독 플랜 Free / Basic / Standard / Premium. 월 모집 한도로 차등. |
| 4자리 코드(useCode) | 캠페인별 사용처리 코드. **체험자 화면에 노출 안 함** — 사장님만 인지. |
| 자가점검(selfCheck) | 리뷰 작성 조건 충족을 체험자가 직접 체크. 채널별 항목 상이. |
| 광고 표기(adNotice) | 경제적 대가 고지 문구. 리뷰 제출 시 **서버 필수 검증**·보존(`adNoticeConfirmed`). |
| 익명 #last4 | 사장님·검수 화면에서 체험자는 id 끝 4자리만 노출(닉네임·이메일·SNS URL 비노출). |

---

## 2. 액터 · 권한 (Actors & Permissions)

| 액터 | 로그인 | 세션 | 할 수 있는 것 | 할 수 없는 것 |
|---|---|---|---|---|
| 체험자 | `/r/login`·`/r/signup` (약관·개인정보 필수 동의) | JWT httpOnly·secure 쿠키, 30일 | 발견·참여·사용·리뷰·취소·초대·탈퇴 | 검수, 타인 체험권 열람(404), 4자리 코드 조회 |
| 사장님 | `/o/login`·`/o/signup` (필수 동의) | 동일 | 캠페인 생성·사용 처리·후기 조회·플랜 변경·탈퇴 | **리뷰 직접 검수(`/api/passes/approve` 410)**, 타 매장 체험권 사용(403) |
| 운영팀 | `/admin/login` | 동일(role=admin) | 리뷰 통과/반려(사유) | 체험자/사장님 화면·보상 접근 |
| 비회원 | — | 없음 | `/legal/*` 열람, `/r/i/[token]` 초대 랜딩 | 그 외 전부 로그인 게이트 |

권한 위반 시 응답: 미인증 401 · 권한 없음 403 · 리소스 없음/타인 404 · 폐기된 경로 410.

---

## 3. 정보 구조(IA) & 네비게이션

**체험자 BottomNav (5탭, 고정 하단, 높이 72px):** 홈(`/r/home`) · 탐색(`/r/explore`) · 체험권(`/r/passes`) · 혜택(`/r/rewards`) · MY(`/r/me`).
등급(`/r/grade`)은 MY 하위 진입. 알림함(`/r/notifications`)은 전 화면 상단 종 아이콘.

**사장님 BottomNav (4탭):** 홈(`/o/home`) · QR 스캔(`/o/scan`) · 후기(`/o/reviews`) · MY(`/o/me`). 캠페인 생성·멤버십·로그·매장정보·리포트는 MY 허브 진입.

**운영팀:** BottomNav 없음(데스크톱 백오피스 톤). `/admin` → `/admin/reviews` redirect.

**전체 라우트 맵:**
```
/                     역할 선택 랜딩
/r/signup /r/login    체험자 가입·로그인
/r/home               홈(큐레이팅·지역 변경 ?area=)   /r/explore  탐색(기본 지도 보기, ?mode=list)
/r/store/[id]         매장 상세·참여        /r/passes   내 체험권(방문형/기자단 탭)
/r/passes/[id]        체험권 상세(7-상태)   /r/grade    등급
/r/rewards            혜택(바이럴)          /r/me       MY(+탈퇴)   /r/notifications 알림함
/r/interests          관심 목록(MY 하위, ?f=open|closed)
/r/press/[id](/write) 기자단 브리프·작성 — MVP 제외(PRESS_ENABLED=false 게이트, 기존 건 마무리만)
/r/invite/new         친구 초대             /r/i/[token] 피추천자 랜딩(비회원)  /welcome/box 환영 박스
/o/signup /o/login /o/home /o/scan /o/reviews /o/campaign/new /o/membership /o/logs /o/stores /o/report /o/me /o/notifications
/admin/login /admin/reviews
/legal/terms /legal/privacy   (비로그인 접근)
```

---

## 4. 전역 UX 규약 (전 화면 공통 — 각 화면 표에서 재기술하지 않음)

| 상황 | 규칙 |
|---|---|
| **로딩** | 서버 호출 중 트리거 버튼은 비활성 + 라벨 "처리 중..."/"발급 중..."/"등록 중..." 등으로 전환. 리스트 로딩은 스켈레톤 없이 SSR(force-dynamic)로 초기 렌더. |
| **빈 상태(empty)** | 리스트가 0건이면 점선 테두리 카드 + 안내 문구 + 유도 CTA. 예: 체험권 "아직 발급된 방문형 체험권이 없어요." + [홈에서 체험권 받기 →]. |
| **에러(폼)** | input 하단 인라인 에러 텍스트(`text-error`, 14px). 서버 에러 문구는 §5 카탈로그 원문 사용. |
| **에러(치명)** | 데이터 조회 실패 시 안내 카드 + [다시 시도]/[홈으로]. 페이지 미존재는 `notFound()`(404). |
| **토스트** | 일시 안내는 화면 하단 토스트, **노출 3초** 후 자동 소멸(예: 코드 복사 "복사됨"). |
| **모달** | 확인/입력 모달은 하단 시트(dim `ink/45`, radius-top 18). 배경 탭 시 닫힘. 파괴적 동작(탈퇴·취소)은 **2단 확인**. |
| **키보드/Input 타입** | 이메일=email, 비밀번호=password, 금액·수치·4자리 코드=`inputMode="numeric"` + 숫자만 필터. 숫자 input은 포커스 시 넘버 키보드 활성화. |
| **넘침 처리** | 가로 목록(카테고리 칩 등)은 가로 스크롤. 칩 그룹은 `flex-wrap`. 긴 본문은 line-clamp. |
| **카운트다운** | 사용 기한/리뷰 마감은 1초 폴링 갱신(`Countdown`). 만료 시 "마감 지남" 표기. |
| **접근성** | 최소 탭 타겟 44px, 명도 대비 WCAG AA, 아이콘 버튼 `aria-label`, 상태색은 텍스트 라벨 병행(색맹 대응). |
| **금액 표기** | 천단위 콤마(`toLocaleString`) + **"원" 접미**(₩ 접두 금지 — 2026-07-07 표기 형식 규칙), 100원 단위. 큰 숫자 `tabular-nums`. |
| **표기 형식(스키마 마스크)** | 스토리보드 모드의 데이터 자리는 라벨명이 아닌 **형식 예시**로 노출: 금액 `000,000원` · 건수 `00건` · 수량 `00개` · 장수 `00장` · 인원 `00명` · 날짜 `0000.00.00` · 마감일 `00월 00일 까지` · **거리 `00m`(기본 마스크, 실데이터는 `00m`/`0.0km`/`00km` — 1km 미만 m, 10km 미만 소수 1자리, 이상 정수 km · `formatDistance`)** · 평점 `0.0`. 실데이터도 동일 형식(`src/lib/storyboard.ts` SBUI). |
| **반응형** | 모바일 뷰포트 폭 480px 기준. 초과 시 중앙 정렬 max-width 480. 운영팀만 데스크톱. |

---

## 5. 메시지 · 에러 카탈로그 (원문 · HTTP 코드)

| # | 상황 | 노출 문구 (원문) | 코드 | 발생 API |
|---|---|---|---|---|
| M1 | 필수 동의 미체크 | "이용약관과 개인정보 수집·이용에 동의해주세요" | 400 | signup |
| M2 | 이메일 중복 | "이미 가입된 이메일입니다" | 409 | signup |
| M3 | 비밀번호 짧음 | "비밀번호는 6자 이상이어야 합니다" | 400 | signup |
| M4 | 로그인 실패 | "이메일 또는 비밀번호가 올바르지 않습니다" | 401 | login |
| M5 | 마감 | "마감되었습니다" | 400 | passes(발급) |
| M6 | 채널 미선택 | "참여할 채널을 선택해주세요" | 400 | passes |
| M7 | 채널 미연동 | "선택한 채널이 연동되어 있지 않습니다" | 403 | passes |
| M8 | ~~등급 부족~~ **폐기 (v1.3)** | (제거됨 — [P1] 등급은 참여 자격이 아니므로 이 에러는 존재하지 않는다. 참여 차단은 M5 마감 / M6 채널 미선택 / M7 채널 미연동뿐) | — | — |
| M9 | 코드 불일치 | "사용처리 코드가 일치하지 않습니다" | 400 | use-by-code |
| M10 | 유효하지 않은 코드 | "유효하지 않은 체험권 코드입니다" | 404 | use/lookup |
| M11 | 타 매장 체험권 | "다른 매장의 체험권입니다" | 403 | use |
| M12 | 이미 사용됨 | "이미 사용 처리된 체험권입니다" | 400 | use |
| M13 | 만료됨 | "만료된 체험권입니다" | 400 | use |
| M14 | 광고 표기 미확인 | "경제적 대가 표기(광고 문구) 포함 여부를 확인해주세요" | 400 | review |
| M15 | 리뷰 기한 초과 | "리뷰 제출 기한(이용 후 7일)이 지났습니다" | 400 | review |
| M16 | 재제출 기한 초과 | "재제출 기한(반려 후 7일)이 지났습니다" | 400 | review |
| M17 | 재제출 2회째 | "재제출은 1회만 가능합니다. 고객센터로 문의해주세요." | 400 | review |
| M18 | 자가점검 미충족 | "자가점검 항목을 모두 체크해주세요" | 400 | review |
| M19 | 모집 인원 0 | "모집 인원을 1명 이상 입력해주세요" | 400 | campaigns |
| M20 | 4자리 형식 오류 | "사용처리 코드는 숫자 4자리로 입력해주세요" | 400 | campaigns |
| M21 | 4자리 중복 | "사용처리 코드 NNNN는 진행 중인 다른 캠페인에서 사용 중입니다. 다른 4자리를 입력해주세요." | 400 | campaigns |
| M22 | 필수 채널 0개 | "필수 채널을 1개 이상 선택해주세요" | 400 | campaigns |
| M23 | 월 한도 초과 | "{플랜} 플랜은 월 N팀까지 모집 가능합니다 (이번 달 N팀 사용 · 보너스 +N팀 · 잔여 N팀)." | 400 | campaigns |
| M24 | 검수 대기 아님 | "검수 대기 상태의 후기만 처리할 수 있습니다" | 400 | admin/decide |
| M25 | decision 오류 | "decision은 approve 또는 reject" | 400 | admin/decide |
| M26 | 사장님 직접 검수 | (경로 폐기) | 410 | passes/approve |
| M27 | 종료 캠페인 발급 | "마감된 체험입니다" | 400 | passes(발급) |
| M28 | 동시 보유 초과 | "체험권은 동시에 5장까지 보유할 수 있어요. 보유한 체험권을 사용하거나 취소한 뒤 발급받아주세요." | 400 | passes(발급) |
| M29 | 취소 후 재신청 제한 | "취소한 캠페인은 12시간 뒤에 다시 신청할 수 있어요. (약 N시간 남음)" | 400 | passes(발급) |
| M30 | 기자단 MVP 제외 | "기자단 캠페인은 준비 중입니다" | 403 | passes(발급) |
| M31 | 사업자번호 형식 오류 | "사업자등록번호 10자리를 입력해주세요" | 400 | signup(owner) |
| M32 | 플레이스 URL 파싱 실패 | "플레이스 URL에서 매장 ID를 찾지 못했어요…" | 400 | owner/stores |
| M33 | 플레이스 조회 실패(폴백) | "플레이스 정보를 불러오지 못했어요. 매장명을 함께 입력해주세요." | 422 | owner/stores |
| M34 | 코드 불일치(잔여 시도) | "사용처리 코드가 일치하지 않습니다 (남은 시도 N회)" | 400 | use-by-code |
| M35 | 코드 오입력 잠금 | "코드를 연속 5회 잘못 입력해 잠시 잠겼어요. 약 N분 후 다시 시도해주세요." | 400 | use-by-code |
| M36 | 유지 동의 미확인 | "게시 유지(90일) 동의가 필요합니다" | 400 | review |
| M37 | 네트워크 오류(클라) | "네트워크 오류가 발생했어요. 연결을 확인하고 다시 시도해주세요." + [다시 시도] | — | PassTicket · /o/scan (fetch 실패) |
| M38 | 카메라 사용 불가(클라) | "카메라를 사용할 수 없어요 — 브라우저 권한을 확인하거나 매장 확인 번호 입력으로 진행해주세요." + [코드로 입력하기] | — | /o/scan (Html5QrScanner) |

> **인증 실패 시 상태 불변 원칙 (2026-07-10 §2)**: M9~M13·M34·M35·M37 어떤 실패에서도 체험권 상태는 변경되지 않는다
> (유일한 예외 = 기한 경과 시 expired 전이라는 정당한 라이프사이클 전이). 사장님 스캔의 비-active 결과는
> raw 상태 대신 한글 안내(이미 사용/만료/취소 구분)로 표기하고, 반복 실패 시 고객센터(help@catchrank.co.kr) 안내를 병기한다.

---

## 6. 상태 머신 · 알림 매트릭스

### 6.1 체험권(Pass) 7-상태 전이

```
[발급] active ──(72h 미사용, 스윕)──▶ expired   (슬롯 복구 + noShow+1 + 양측 알림 · 연장/복구 불가)
       active ──(체험자 취소)──────────▶ cancelled (슬롯 복구, 노쇼 미집계, 동일 캠페인 12h 재신청 제한)
       active ──(사장님 사용 처리)──────▶ used
       used  ──(리뷰 제출, 이용 후 7일 내)─▶ review_submitted
       used  ──(7일 초과 미제출, 스윕)──▶ used 유지 + noShow+1 + 양측 알림(1회)
       review_submitted ──(운영팀 통과)─▶ completed (completedReviews+1)
       review_submitted ──(운영팀 반려)─▶ rejected  (사유 보존)
       rejected ──(반려 후 7일 내 1회 재제출)─▶ review_submitted (resubmitCount=1)
```
방문형 사용 기한 **발급 후 72h(연장·복구 불가)**, 리뷰 **이용 후 7일**. 기자단은 사용 기한=캠페인 종료, 재제출 기한=캠페인 종료.
발급 가드(2026-07-07): 종료 캠페인 M27 / active 동시 5장 M28 / 취소 후 12h M29 / 기자단(MVP 제외) M30.
노출: 발급 소진이어도 살아있는(active) 체험권이 남으면 캠페인 계속 노출(`campaign-visibility.ts` — open/issued_out/closed).
라이프사이클 스윕(`src/lib/pass-lifecycle.ts`)은 DB 로드마다 실행되며 만료 6시간 전·리뷰 마감 24시간 전 리마인드 알림도 발송.

#### 6.1a 상태별 정의 매트릭스 (2026-07-10 — 7 실상태 + 파생 표시 2종)

**파생 표시 상태**는 저장하지 않고 렌더 시점에 계산한다(`src/lib/pass-display.ts`): used+기한 초과 → `overdue`, rejected+재제출 기한/횟수 소진 → `resubmit_expired`.
'방문 예정 / 노쇼(별도 상태) / 패널티 적용' 상태는 **도입하지 않는다** — 예약 개념이 없는 즉시 발급·자율 방문 모델이므로 **만료(expired) = 노쇼**이며, 패널티는 상태가 아니라 월간 재평가 집계(운영정책서 §10)다.

| 상태 | 목록 표시명(뱃지) | 상세 안내 | CTA(활성) | 전이 조건·시점 | 알림 | 패널티(재평가) | 검색·탐색 노출* |
|---|---|---|---|---|---|---|---|
| active | 사용가능 (successSoft) | QR/코드 입력 티켓 + 남은시간 | [체험권 보기]·[참여 취소] (활성) | 발급 즉시. 72h 경과 시 expired(스윕) | 발급 시 사장님 / 만료 6h 전 체험자 | — | 캠페인 잔여에 계상 |
| used | 작성 대기 중 (brandSoft·퍼플 보더) | 리뷰마감 D-day + 자가점검 폼 | [리뷰 제출] (활성) | 사장님 스캔 또는 4자리 코드 인증 즉시 | 사용 처리(체험자) / 마감 24h 전(체험자) | — | — |
| *overdue* (파생) | 제출 기한 초과 (sunken) | "기한이 지나 제출 불가·감점 반영" | 없음 (서버도 M15 차단) | usedAt+7d 경과 (표시는 즉시, noShow 집계는 스윕 1회) | 기한 초과 시 양측(1회) | 기한 초과 −7/건 | — |
| review_submitted | 검수중 (sunken) | "영업일 기준 최대 3일 이내 검수" | 없음 | 리뷰 제출 즉시 | 사장님(신규/재제출) | — | — |
| completed | 검수 완료 (sunken) | 검수 통과 + 등급 반영 안내 | 없음 | 운영팀 승인 즉시(completedAt) | 양측 | — (완료 크레딧) | — |
| rejected | 반려 (errorSoft) | 사유 원문 + **재제출 기한**(반려 후 7일) | [리뷰 다시 제출] (기한 내·1회) | 운영팀 반려 즉시(rejectedAt) | 양측(+사유) | — | — |
| *resubmit_expired* (파생) | 재제출 기한 초과 (sunken) | "기한/횟수 소진 — 고객센터 안내" | 없음 (서버도 M16/M17 차단) | rejectedAt+7d 경과 또는 resubmitCount≥1 | — | 반려 종착 −5/건 | — |
| expired | 만료 (sunken) | "72h 경과 만료·연장 불가·슬롯 반환" | 없음 (동일 캠페인 재신청은 가능) | 72h 경과 스윕 시(슬롯 복구) | 양측 | 노쇼 −10/건 | 슬롯 복구 → 캠페인 잔여 복원 |
| cancelled | 취소 (sunken) | "취소함 — 12h 후 재신청 가능" | 없음 | 체험자 취소 즉시(슬롯 복구) | 사장님 | 무패널티 | 슬롯 복구 → 캠페인 잔여 복원 |

*캠페인 자체의 검색·탐색 노출은 상태가 아니라 `campaignExposure`(open/issued_out/closed)가 결정 — 종료(closed)만 비노출, issued_out은 노출+발급 불가+추천순 최후순위.

**체험 신청 상태(매장 상세 CTA, §1)**: 신청 가능(발급 버튼) / **신청 완료**(배너+[내 체험권 보기]) / **12h 재신청 대기**(비활성+잔여 시간) / **체험권 일시 소진**(issued_out — "현재 신청 가능한 체험권이 없습니다"+"미사용 체험권이 발생하면 다시 신청할 수 있어요") / **종료**("종료된 체험입니다") / SNS 연동 필요.

### 6.2 알림 매트릭스 (누가 · 언제 · 무엇을 받나 — 인앱 알림함 단일 채널)

| 이벤트 | 체험자 알림 | 사장님 알림 |
|---|---|---|
| 체험권 발급(참여) | — | "체험권 발급" (닉네임·채널·등급) |
| 사용 처리 | "체험권 사용 처리" (결제·지원 적용액) | — (본인 행위) |
| 리뷰 제출 | — | "신규 후기 등록" / (재제출 시) "후기 재제출" |
| 검수 통과 | "후기 검수 통과 ✅" | "후기 검수 통과" |
| 검수 반려 | "후기 반려 안내" (+사유 원문) | "후기 반려 처리" |
| 참여 취소 | — | "체험권 참여 취소" (슬롯 복구) |
| 만료(노쇼) | "체험권 만료" | "체험권 만료 (미방문)" |
| 리뷰 기한(7일) 초과 | "리뷰 기한 초과" | "리뷰 미제출 안내" |
| 만료 6h 전 | "체험권 만료 임박 ⏰" | — |
| 리뷰 마감 24h 전 (2026-07-10) | "리뷰 마감 24시간 전 ⏰" (기한 경과 시 제출 불가·감점 고지) | — |
| 초대 수락 | (환영 보상) | — |
| 등급 재평가 변동 | "등급이 올랐어요 🎉" / "등급이 조정되었어요" (월말 재평가, link `/r/grade`) | — |
| 상생 리뷰어 뱃지 | "상생 리뷰어가 되었어요 🤝" / (2개월 연속 미달) "상생 리뷰어 뱃지가 해제되었어요" | — |

---

## 7. 데이터 모델 · API · 비기능 · 디자인 시스템 · BM/법무

### 7.1 핵심 엔티티 (요약 — 상세는 [`데이터정책서.md`](데이터정책서.md) §1)

- **Reviewer**: id·email·passwordHash(bcrypt)·nickname·sns[{kind,url,influence}]·grade·channelGrades·completedReviews·qualityScore·noShowCount·termsAgreedAt·inviteStats.
- **Owner**: id·email·passwordHash·storeName·category·area·plan·termsAgreedAt·inviteStats.
- **Store**: id·ownerId·name·category·area·rating·reviewCount·hours·lat·lng·naverPlaceId.
- **Campaign**: id·storeId·kind·title(매장명 자동)·supportAmount·quota{S,A,B,C}·used{}·requiredChannels[]·requiredMenus[{name,price?}]·description(≤500)·highlightKeywords[≤5·각≤20]·useCode(4자리)·press필드.
- **Pass**: id·code(8자)·reviewerId·campaignId·reviewerGrade·consumedSlot·issuedAt·expiresAt·usedAt·cancelledAt·paidAmount·supportApplied·supportBoostPct·reviewChannel·reviewUrl·reviewSelfCheck·adNoticeConfirmed·rejectReason·rejectedAt·resubmitCount·status(7종).
- **Reward**: kind(support_bonus_pct|membership_discount|quota_bonus)·value·issuedAt·expiresAt·usedAt.
- **Invite**: token(8자)·referrerKind·targetKind·status·expiresAt(14일)·consumedBy.

### 7.2 API 카탈로그

| 메서드 | 경로 | 역할 | 요청/비고 |
|---|---|---|---|
| POST | /api/auth/signup | 공통 | {role,email,password,nickname/store…,sns,agreeTerms:true} · agreeTerms 필수 |
| POST | /api/auth/login | 공통 | {role,email,password} → 세션 쿠키 |
| POST | /api/auth/logout | 공통 | 세션 파기 |
| DELETE | /api/auth/account | 체험자/사장님 | 계정·알림·미사용 보상 삭제, 거래 기록 비식별 보존 |
| POST | /api/campaigns | 사장님 | 캠페인 생성(자동 분배·quota_bonus 소진) |
| POST | /api/passes | 체험자 | {campaignId,channel} → 발급·consumedSlot |
| POST | /api/passes/cancel | 체험자 | {passId} active→cancelled+슬롯 복구 |
| POST | /api/passes/use | 사장님 | {code,paidAmount} 사용 처리+부스트 가산 |
| POST | /api/passes/use-by-code | 체험자 | {passId,code(4자리),paidAmount?} |
| POST | /api/passes/lookup | 사장님 | 8자/4자리 분기 조회 |
| POST | /api/passes/review | 체험자 | {passId,reviewUrl,selfCheck,adNotice}(기자단 pressSelfCheck) · 재제출 포함 |
| POST | /api/passes/approve | — | **410 Gone** |
| POST | /api/admin/reviews/decide | 운영팀 | {passId,decision,reason?} |
| POST | /api/referral/invite | 회원 | 토큰 발급 |
| POST | /api/referral/accept | 회원 | mode click/accept → 양면 보상 |
| GET | /api/referral/counter | 회원 | 실데이터 카운터(조작 없음) |
| POST | /api/interests | 체험자 | {campaignId} 관심 목록 토글 → {saved} |
| GET | /api/map/reverse-geocode·static | 공통 | Naver 프록시 |

### 7.3 비기능 요구사항 (Non-Functional)

- **인증/세션**: JWT(jose) HS256, httpOnly·secure 쿠키, 30일. AUTH_SECRET env 필수(미설정 시 production 경고).
- **비밀번호**: bcrypt 단방향 해시. 평문 저장 금지.
- **영속성**: 3단(메모리 → `/tmp` JSON → Vercel KV). 운영은 KV 필수. DB 로드마다 라이프사이클 스윕.
- **개인정보 노출 통제**: 사장님·검수 화면은 익명 #last4만. 닉네임·이메일·SNS URL 비노출.
- **관리자 도구**: `/api/admin/refresh-stores`는 production에서 ADMIN_REFRESH_TOKEN 미설정 시 503.
- **성능**: 리스트 SSR(force-dynamic). 카운터 폴링 10초. 지도/이미지는 지연 로드.
- **데이터 진실성**: 사용자 노출 수치(카운터·보상)는 **실제 발생 이벤트만** 집계(허위·조작 금지 — 표시광고법).

### 7.4 디자인 시스템 (토큰 · 컴포넌트) — **v2.0 (정본: `DESIGN.md`)**

> v1(Apple 톤·Action Blue)은 전면 파기됨. 정본은 루트 [`DESIGN.md`](../DESIGN.md) — "[체험단] 디자인시스템" Figma 라이브러리 + 레퍼런스 화면 5종 기준.

| 토큰 | 값 | 용도 |
|---|---|---|
| brand(Primary) | **#9333EA (Purple 60)** | 유일한 인터랙션 컬러 — CTA·선택 라디오·활성 탭·선택 마커 |
| brandSoft / brandTint | #FAF5FF / #F3E8FF | 퍼플 틴트 배경(안내 배너·필수 메뉴 행·선택 마커 fill) |
| ink / ink2 / muted | #171717 / #404040 / #737373 | 텍스트 위계 (Gray 90/80/60) |
| canvas / parchment / sunken | #FFFFFF / #FAFAFA / #F5F5F5 | 배경 위계 (Bg Primary/Secondary/Tertiary) |
| hairline / borderStrong | #E5E5E5 / #D4D4D4 | 카드·칩·인풋 1px 보더 |
| info | #3B82F6 | **프로모/마케팅 슬롯 전용** (인터랙션 금지) |
| success/error/warning | #00BF40 / #FF4242 / #EAB308 | 상태색 + *Soft 틴트 배경 |
| snsBlog/Insta/Tiktok | 그린/핑크/시안 파스텔 | **SNS 배지 시그니처** — 상태·카테고리 재사용 금지 |

- **타이포: Pretendard** (400/500/600/700). 사다리 **11~22px** — 큰 디스플레이 타입 없음, 강조는 굵기·틴트로. 금액은 항상 bold 검정 + tabular-nums (퍼플 금지 — 퍼플=인터랙션, 검정=가치).
- Radius: xs4 / sm8 / **md12(기본)** / lg16 / xl20 / pill. 섀도는 떠 있는 표면(FAB·지도 카드·시트)만 — 문서 흐름 카드는 1px 헤어라인.
- 컴포넌트: **button-primary(52px rounded-md 퍼플 — pill 아님)** · map-fab(검정 pill) · sns-badge · category-chip(아이콘+라벨 pill, 선택=검정 1.5px 보더) · radio-select-card(채널 선택, 선택=퍼플 보더) · link-state-chip(연동 완료/필요) · menu-highlight-row · info-strip/stat-strip(3분할) · notice-banner(퍼플 틴트) · step-timeline · map-marker-pill(2줄) · bottom-sheet(rounded-t-xl+핸들) · cta-bar(지원 금액+발급 버튼) · GradeBadge · ChannelIcons(파스텔 배지) · Countdown.
- 선택 문법 구분: **필터 선택 = 검정 보더**(카테고리 칩), **데이터 선택 = 퍼플**(채널 라디오) — 혼용 금지.
- **B급 감성 카피(체험자 측만)**: 유쾌·친근·발견의 재미("걸어서 갈 수 있어요👀", "근처 체험 N개 발견!"). 사용 금지 카피·변환 표는 §7.6. 사장님·법무·검수 화면은 표준 존댓말.

### 7.5 BM · 비용 부담 · 정산 · 법무

- **수익원**: 사장님 멤버십 구독(월 ₩0/13,900/25,900/38,900) + 기자단 종량제 선결제.
- **지원금 부담 주체(분쟁 방지 핵심)**: 방문형 지원금 = **매장이 결제 시 직접 할인**(회사 무정산). 폼·체험자 카드·약관 3중 명시. 부스트도 기준 지원금(S100%) 상한 내라 매장 초과 부담 없음.
- **기자단 정산**: 검수 통과 후 D+7 송금, 3.3% 원천징수. 재원 = 사장님 종량제 선결제.
- **결제(PG)**: VER.1 범위 외 — 직접 연동 예정. 연동 전 멤버십은 운영팀 수기 청구(미납 시 Free 조정). 훅 지점 [`../PRD.md`](../PRD.md) §7.3.
- **법적 고지**: 이용약관 15조 + 개인정보처리방침. 가입 필수 동의 + termsAgreedAt. 회원 탈퇴 API. 광고 표기 서버 강제. 거래 기록 5년 비식별 보존. 상세 [`운영정책서.md`](운영정책서.md)·[`데이터정책서.md`](데이터정책서.md).

### 7.6 B급 감성 카피 가이드 (v2.7 — 체험자 측)

> 컨셉 미팅 산출물(`concept/2026-06-16-tone-of-voice.txt`). 레퍼런스: 오늘의집 초기 / 당근 초창기 / 여기어때. **UI는 깔끔, 톤만 유쾌·친근·발견의 재미·살짝 장난기.** "신청/선정/리뷰 의무" 류 체험단 냄새 카피 회피.

**카피 변환 표(주요)**
| Before | After (B급) |
|---|---|
| 안녕하세요 OOO님 | "{지역} 어디 가볼까?" |
| 근처 방문 가능 매장 20곳 | 근처에 경험할 곳 20곳이 있어요 |
| 새로운 공간이 많이 생겼어요! | 이 집 아직 모르는 사람 많음 🤫 |
| 오늘 참여 가능한 체험 N개 | 오늘 가볼 만한 곳 N곳 발견 |
| 곧 마감 체험 N개 | 지금 안 가면 ⏰ N곳 곧 마감 |
| 신규 체험 N개 | 방금 등록 신상 N곳 |
| 평균 지원금 ₩X | 평균 받아요 ₩X 체험 지원 |
| 정렬: 추천/거리/신규/지원금/마감 | 우리 추천 / 가까운 순 / 방금 등록 / 많이 받는 순 / 곧 마감 |
| 검색 결과 0건 | 다른 동네 찾아볼까요? |
| 모집 중인 캠페인 없음 | 지금은 동네가 잠깐 쉬는 중 |

**상태별 카피(탐색 카드)**
| 상태 | 라벨 칩 | 보조 카피 |
|---|---|---|
| `now-createdAt < 7d` | **신상**(success) | 방금 등록 |
| `endAt-now < 1d` | **곧 마감**(error) | "지금 안 가면 남들 인스타에서 보게 됨"(본문 italic) |
| `endAt-now < 7d` | **이번 주만**(orange) | — |
| ~~등급 부족 오버레이~~ | **폐기 (v1.3)** | [P1] 등급 게이트 없음 — 잠금/흐림/오버레이 상태는 존재하지 않는다. 등급 차이는 카드의 **금액**으로만 나타난다. |

**시각 가이드**: 타이포 대비 과감(헤드라인 34~40 / 서브 11~12), 라벨 칩은 스티커 느낌(`rotate(-4deg)` + drop shadow, 카드 자체 회전 없음), 포인트 색(brand `#0066cc` / 곧 마감 error red / 신상 success green) 단독 강조.
**사용 금지 카피**: "신청해주세요"·"선정 결과 안내"·"체험단원 모집 중"(체험단 냄새), "환영합니다"·"감사합니다"(격식체), "광고"·"프로모션"(광고 표시 문구는 §5·운영정책서 별도 규정).

---

## 8. 체험자 (Reviewer) 화면별 요구사항

### CP_R_REQ1 · 가입 · 온보딩 (`/r/signup`) — 3-Step

| 기능 ID | 기능명 | 기능내역 (위치·제약·디자인 의도·데이터) | 비고 | 개발 |
|---|---|---|---|---|
| CP_R_REQ1_1 | Step 0 컨셉 히어로 | 진입 첫 화면 상단 display 40px "선정 기다리는 체험단 말고, 등급으로 받는 체험권." + 하단 고정 [시작하기] pill(48px) + [로그인] 링크. | — | ✅ |
| CP_R_REQ1_2 | Step 1 계정 입력 | 상단 "1 / 2". 이메일(email)·비밀번호(password, **최소 6자**)·닉네임(text) input 3종(48px, radius10). 포커스 시 border=brand. | — | ✅ |
| CP_R_REQ1_3 | 필수 동의 2종 | Step 1 하단 체크박스 2개(약관·개인정보). 밑줄 링크는 `/legal/terms`·`/legal/privacy` **새 탭**. **둘 다 체크해야** [다음] 활성. 서버 `agreeTerms` 재검증(M1). | 개인정보보호법 §22 | ✅ |
| CP_R_REQ1_4 | Step 2 SNS 연동 | 채널 카드 **3종만**(블/인/틱). 각 = URL input + 영향력 **숫자 input(숫자만 필터·넘버 키보드)**. | 1개 이상 권장 | ✅ |
| CP_R_REQ1_5 | 등급 자동 산정 | **캐치랭크 지수 평가 모델 v1** — 가중 영향력(**블로그 ×1.2**) **A≥50,000/B≥10,000/C≥1,000/N<1,000**. 종합=최상위(`bestGrade`). 최초 연동 등급은 지수 단독 산정이며, 지수점수 정규화는 `indexScoreV1`(교체 경계 — 실모델 API 도입 시 이 함수만 대체, 임계값은 `INDEX_BANDS` 공유). 이후 매월 말 재평가(REQ6_2, 운영정책서 §10). | S는 운영팀 부여 | 🔷 |
| CP_R_REQ1_6 | 연동 없이 시작 | 보조 액션 [연동 없이 시작 (N등급)]. sns 빈 배열 → N(배율 10%). | — | ✅ |
| CP_R_REQ1_7 | 가입 완료 | [연동 후 시작하기] → `POST /api/auth/signup`. 성공 시 세션 쿠키(30일). `?invite=` 있으면 `/welcome/box`, 없으면 `/r/home`. 에러 M1~M3. | — | ✅ |

### CP_R_REQ2 · 홈 (`/r/home`) — 디자인 시스템 v2

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_R_REQ2_1 | 위치 선택 바 | 상단 52px 바 좌측 **{지역명} ⌄ │ ⊕**(18px bold). ① 지역명·⌄ 탭 → **현위치 설정 페이지(`/r/location`)로 전환**(바텀시트 아님 — 2026-07-08 레퍼런스). ② 구분선 우측 **GPS(크로스헤어) 아이콘** 탭 → 선택 지역 해제 + 현 위치(reverse-geocode)로 갱신. 지역을 바꾸면 **'걸어서 갈 수 있어요'만** 해당 지역 기준으로 변경(전국 리스트·탐색은 무관). 우측 검색(→`/r/explore`)·알림(미읽음 dot) 아이콘 버튼(40px 타겟). | 2026-07-08 개편 | ✅ |
| CP_R_REQ2_1a | 현위치 설정 **바텀시트** | 홈 지역 라벨 탭 → **바텀시트**(85dvh, rounded-t-xl — 2026-07-08 시안으로 페이지→시트 전환, `LocationSheet.tsx`): 헤더(타이틀+✕) · **퍼플 틴트 배너** "위치를 설정하면 가까운 체험을 추천해드릴게요" · 회색 바(📍 현재 지역 + 우측 퍼플 **[⊕ 현위치로 설정]**) · **최근 선택**(localStorage `cp_recent_regions_v1` 최대 5 — 칩 가로 스크롤·개별 ✕·전체 삭제, 없으면 "최근 선택한 지역이 없습니다.") · 하단 2단: 좌 **시도 레일**(108px bg-sunken, 활성=흰 배경+퍼플 bold) / 우 **[전체]+시·군·구**(행 52px, 활성 퍼플 bold). 선택 즉시 최근 저장 + `/r/home?area=` 이동. `/r/location` 라우트는 홈 redirect 안전망으로만 유지. 데이터 `src/lib/regions.ts`. | 2026-07-08 시안 (구 R-13) | ✅ |
| CP_R_REQ2_6 | 검색 화면 (`/r/search`, R-14) | 홈·체험권 목록의 돋보기 → 풀스크린 검색(2026-07-08 시안): 상단 ← + **퍼플 보더 pill input** "어디로 가볼까요?"(autoFocus, 우측 돋보기 제출) · **최근 검색어**(localStorage `cp_recent_searches_v1` 최대 10 — 칩 flex-wrap·개별 ✕·전체 삭제, 없으면 "최근 검색어가 없습니다."). 제출/칩 탭 → `/r/explore?mode=list&q=`(탐색이 지역·매장·키워드 전체 검색 수행 — REQ3_1). | 2026-07-08 신설 | ✅ |
| CP_R_REQ2_2 | 프로모 배너 | `promo-banner`(블루 #3B82F6, rounded-lg, ≥104px) — [할인쿠폰] 흰 pill 태그 + 헤드라인 2줄. **마케팅 슬롯**(CMS 연동 전 placeholder 카피). 블루는 마케팅 전용 — 인터랙션 금지. | P4: 실제 진행 프로모만 게재 | ✅ |
| CP_R_REQ2_3 | 내 활동 3-stat | `stat-strip`(흰 카드+헤어라인, 3분할): **이번 달 아낀 금액**(이달 usedAt 패스 supportApplied 합) / **작성할 리뷰**(used 상태 수) / **🎫 발급받은 체험권**(active 수, 퍼플 강조, 탭→`/r/passes`) — 확정 정책 1-1의 3정보와 일치하도록 라벨 정정(2026-07-08). | 전부 실데이터 집계 | ✅ |
| CP_R_REQ2_3a | 지역 기준점 3km | 지역 선택(`?area=`) 시 '걸어서 갈 수 있어요'는 **그 지역의 기준 좌표에서 반경 3km**(하버사인, `src/lib/geo.ts regionCenter` — 시도 17+서울 시군구 정적 사전, 미등재 시군구는 시도 폴백) 매장을 거리순 노출(확정 정책 1-3). 기존 area 문자열 정확 일치 방식은 지역 라벨-시드 불일치로 항상 빈 결과였음 — 좌표 기준으로 정정. | 2026-07-08 | ✅ |
| CP_R_REQ2_4 | 걸어서 갈 수 있어요👀 | **1단 가로 캐러셀**(스냅 스크롤, 카드 폭 168px) **최대 10개**, **반경 3km 이내**(`NEARBY_RADIUS_M`) 가까운 순 + [더 둘러보기 ›]→`/r/explore?mode=list`. 카드=4:3 사진(rounded-md)+SNS 배지+🎫 00개 남음(발급 소진 시 "발급 마감 · 체험 진행 중")+가게명(1줄)+**"최대 000,000원 지원"**(16px bold 검정). | 2026-07-08: 2단 그리드→1단 캐러셀·최대 10개 | ✅ |
| CP_R_REQ2_5 | 전체 리스트👀 | "내가 체험할 수 있는 전체 리스트👀" **2단 그리드 유지**(2026-07-08 확인), 정렬 **최신 등록순**(마감 임박순 아님 — 회의 확정) + [더 둘러보기 ›]→`?mode=list&sort=new`. **[P1] 등급 오버레이·잠금 없음** — 금액만 내 채널 등급 기준 개인화. | 2026-07-07 회의 | ✅ |

### CP_R_REQ3 · 탐색 (`/r/explore`) — 디자인 시스템 v2

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_R_REQ3_1 | 세그먼트 타이틀 | 헤더 = **[방문형]** 20px bold 세그먼트 + 검색 토글·알림 아이콘. 검색 대상 = **매장명·지역(area+주소)·카테고리·강조 키워드**(확정 정책 2-1 — 지역+키워드+전체 검색), `?q=`로 초기 검색어 주입 가능. **기자단 세그먼트는 MVP 제외**(`PRESS_ENABLED=true`일 때만 노출). | MVP=방문형만 | ✅ |
| CP_R_REQ3_2 | 카테고리 칩 | 아이콘+라벨 pill(⭐전체/☕카페/🍽️식당/✂️뷰티/🏥문화/💪헬스) 가로 스크롤, **다중 선택**(선택=검정 1.5px 보더, 전체=선택 없음) + 우측 필터 아이콘 버튼(필터 활성 시 퍼플 dot). 통합 필터 시트와 같은 상태 공유. | 2026-07-07 회의 | ✅ |
| CP_R_REQ3_6 | 통합 필터 바텀시트 | 필터 버튼 탭 → 바텀시트(`FilterSheet.tsx`, 2026-07-10 개편): **지역 3상태** — **미선택(기본값**, 두 칩 모두 비활성 = 전국 기준, 뱃지 미계상, "선택하지 않으면 전국 기준이에요" 캡션**)** / **현위치**(칩 **토글 — 켜져 있을 때 재클릭하면 해제**되어 미선택 복귀, 프로토타입은 mock 현위치 거리 기준·실서비스 GPS 자리) / **지역**(시도→시군구, LocationSheet 재사용 — **탐색 한정**·홈 무영향). 현위치·지역은 상호 배타(하나를 켜면 다른 쪽 해제, 중첩 시트의 [현위치로 설정]=현위치 선택). + **SNS 채널**·**카테고리**(각각 **'전체' 칩** + 다중 선택). **draft 방식** — 열릴 때 적용값 스냅샷, 칩 조작은 draft만 변경, 하단 **[↺ 초기화(전체 해제 — 지역은 미선택으로)]·[적용하기 · 체험 N개]**로 커밋 + URL(`?cat/ch/area/loc=me`) 반영(재진입 유지). 필터 아이콘에 **적용 개수 뱃지**(현위치·지역 선택 시 +1, 미선택은 0). 선택한 채널값은 상단에 별도 노출하지 않는다. | 2026-07-10 §8 | ✅ |
| CP_R_REQ3_3 | 결과 카운트·정렬 | **"근처 체험 N개 발견!"**(16px bold, 필터 결과 실시간) + 정렬 셀렉트(**추천순**(기본 — 사장님 플랜 랭크→최신순, §4)/가까운 순/방금 등록/많이 받는 순/곧 마감). | 2026-07-10 추천순 | ✅ |
| CP_R_REQ3_4 | experience-row | 좌 **96px 정사각** 썸네일(rounded-md) + SNS 배지 + 가게명(≤2줄) + "{카테고리} · **{거리}**"(현 위치 기준, `00m`/`0.0km`/`00km`) + **"최대 000,000원 지원"** bold. 우상단 **🎫 00개 남음**(발급 소진 시 "발급 마감"). 행 간 16px(구분선 없음). | — | ✅ |
| CP_R_REQ3_5 | 지도 모드(**기본 화면**) | 탐색 진입 기본 = 지도 보기(`?mode=list`로 리스트). FAB [☰ 목록 보기] ↔ [📍 지도 보기](검정 pill, shadow-fab). 풀맵 + **2줄 흰 pill 마커**(매장명/최대 금액, 보더 #D4D4D4, 선택=퍼플 1.5px+Purple10 fill). 핀 미선택=**피크 바텀시트**(rounded-t-xl+핸들, **디폴트는 카테고리 칩+필터 아이콘 영역까지만** 노출 · **위로 40px+ 쓸어올리거나 핸들 탭 시 목록 보기로 자동 전환** — 2026-07-08) / 핀 탭=**map-bottom-card 캐러셀** — **좌우 스와이프로 다른 매장 탐색**(순서=현재 위치 기준 거리순, scroll-snap, 하단 도트 인디케이터). 카드 정보 = **매장명·카테고리·거리(현 위치 기준 `00m`/`0.0km`)·지원금만**(참여 채널·마감일·등급은 과도한 정보로 미노출). 스와이프/핀 탭 시 지도 panTo 포커스 이동 + 현 위치 파란 점·GPS 리센터 버튼. | 2026-07-07 회의 | ✅ |
| CP_R_REQ3_7 | '이 지역 재검색' (신규) | 지도 이동마다 실시간 재호출하지 않는다(확정 정책 2-3) — 기준점(초기 중심/직전 재검색 중심)에서 **500m 이상** 지도를 옮기면 상단 중앙 **[↻ 이 지역 재검색]** pill 노출(실지도=dragend/zoom_changed 감지, 데모 지도=핀 선택 이동 감지). 탭 → 그 시점 지도 중심 **반경 3km 하버사인 필터**로 핀·카드 갱신 + 카드 캐러셀을 새 중심 기준 실거리 정렬로 재구성 + 상단 "이 지역 반경 3km · N곳" 칩([전체 보기] 복귀 버튼). 결과 0건 = "이 지역엔 지금 모집 중인 체험이 없어요" + [전체 보기로 돌아가기]. GPS 리센터는 반경 모드도 해제. **[원칙 확정 2026-07-10] 반경 3km 필터는 지도 전용 탐색 도구 — 리스트 뷰는 항상 필터 시트(지역·채널·카테고리) 기준이며, 반경 모드 중 목록 보기로 전환해도 리스트에는 반경 필터를 적용하지 않는다**(뷰 전환 시 유지되는 것은 필터 시트 값·정렬뿐). | 2026-07-08 확정 정책 | ✅ |
| CP_R_REQ3_8 | 탐색 진입점 매트릭스 (신규) | ① **홈 '걸어서 갈 수 있어요' 더 둘러보기** → `?mode=list&sort=distance&area={선택 지역}` — 리스트·가까운순·지역 행정 기준점 ② **홈 '전체 리스트' 더 둘러보기** → `?scope=all` — **지도·대한민국 전체 축소(줌 7)로 시작해 시도별 건수 클러스터 즉시 노출**(§6-4 확정 — 홈 선택 지역 미전달, 탭 시 줌인→개별 핀). **데모 지도(지도 키 미연동)도 동일하게 클러스터로 시작** — 줌이 없으므로 시도 마커 탭=해당 시도 핀으로 "확대", 상단 "{시도} · N곳 · [전국 보기]" 칩으로 복귀(GPS 리센터도 클러스터 복귀). 리스트 전환 시 전국 추천순, **필터 시트의 지역은 미선택(Default)** ③ **하단 탐색 탭** → 파라미터 없음 — 지도(핀 centroid·줌 12)·추천순·지역 필터 미선택. `scope=all`은 `?area=`/`?loc=`보다 우선(전국 우선). | 2026-07-10 확정 | ✅ |

### CP_R_REQ4 · 매장 상세 · 채널 선택 참여 (`/r/store/[id]`) ★핵심 — 디자인 시스템 v2

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_R_REQ4_1 | 히어로·매장 헤더 | ← 백바(52px, 우측 **♡ 관심 목록 토글** — 캠페인 단위 저장, 저장=퍼플 ♥, `POST /api/interests`) + 4:3 풀블리드 실사진 + SNS 배지 + 매장명(20px bold) + **플레이스 URL 링크**(블루 텍스트·**좌측 정렬**, 네이버 플레이스 — 현행 유지, 회의 확정). | — | ✅ |
| CP_R_REQ4_2 | info-strip + 안내 배너 | 3분할 흰 카드: **체험 마감일**(campaign endAt "00월 00일 까지") / **리뷰 마감 기한**("**이용 후 7일 이내**" — 상세는 공통 문구, 실제 마감·카운트다운은 체험권 화면) / **🎫 잔여 00개**(퍼플). 하단 `notice-banner`(퍼플 틴트): 💬 "체험권 발급 후 **72시간** 내로 사용하지 않으면 사라져요." 종료 캠페인은 상단에 "마감된 체험입니다" 배너 + CTA 비활성. | 2026-07-07 회의로 72h/7일 확정 | ✅ |
| CP_R_REQ4_3 | 매장 소개+키워드+지도 | "리뷰에 꼭 작성해주세요" `#키워드` 칩(**≤5개·각 ≤20자**, bg-sunken) · "매장 소개"(≤500자) · 지도 스니펫(탭→네이버 지도)+**주소 복사**(복사 성공 시 "복사됨" 1.8s). | 없으면 섹션 숨김 | ✅ |
| CP_R_REQ4_4 | 채널 라디오 카드 | "어느 SNS로 체험할까요?" + 서브 "채널을 선택하면 혜택과 리뷰 작성 조건이 달라져요." `radio-select-card` 리스트: 연동 채널="{채널} · {등급}등급"+**[연동 완료]**(그린 아웃라인 칩)+우측 ₩금액, **선택=퍼플 1.5px 보더+퍼플 라디오**. 미연동="{채널}"+**[연동 필요]**+"확인불가"(bg-secondary, 선택 불가). Default=연동 채널 중 우선순위(블→인→틱) 첫 번째. | [P1] 등급 무관 | ✅ |
| CP_R_REQ4_5 | 지원금 자동 계산 | 카드 금액 = 채널 등급 × 배율(**S100/A80/B60/C40/N10**, 100원 반올림). **타 등급 최대 지원금 비교·동기부여 문구("S등급 시 최대 ○○원" 류)는 노출하지 않는다**(회의 확정) — "지원금은 채널별 내 등급에 따라 달라져요 · 매장이 결제 시 직접 할인해 드리는 금액이에요" 고지만. [P1] 등급 에러 없음. | 2026-07-07 회의 | ✅ |
| CP_R_REQ4_6 | 필수 메뉴·리뷰 조건 | "필수로 주문해야하는 메뉴가 있어요 **(택 1)**"+서브 "아래 메뉴를 필수로 주문해야 지원금을 받을 수 있어요" — `menu-highlight-row`(퍼플 틴트 bg·퍼플 메뉴명·우측 가격). "리뷰 작성 조건" = 선택 채널 기준 즉시 갱신 — **세부 조건 리스트(항목+힌트)** + **광고 표시 문구 원문 박스(+[복사], "복사됨" 1.8s) + 공정위 고지**를 여기(작성 **전**)에 노출 — "리뷰를 작성하기 **전에** 아래 조건을 꼭 확인해주세요. 제출 화면에서는 자가 점검만 진행해요." (조건은 채널별 **가변 데이터** `CHANNEL_REVIEW_CONDITIONS`). 작성 후 확인·수정이 발생하지 않도록 사전 인지가 목적(2026-07-08). 메뉴 가격은 표시(광고주 변경 시 직접 수정 또는 운영 통지 의무 — 운영정책). | 2026-07-07 회의 | ✅ |
| CP_R_REQ4_7 | 이용방법 + cta-bar | `step-timeline` 3단계(퍼플 원형 번호+연결선): 발급받기/QR 제시/리뷰 작성. **cta-bar 하단 고정**: 좌 "지원 금액" ₩(18px bold) + 우 **[체험권 발급받기]**(52px rounded-md 퍼플). 분기 4종: 발급받기 / 마감되었습니다 / SNS 연동 필요(전 채널 미연동) / 내 체험권 보기. **[P1] 등급 분기 없음.** | — | ✅ |
| CP_R_REQ4_8 | 발급 확인 모달 | 하단 시트(rounded-t-xl+핸들, 2026-07-08 시안 개편) — 중앙 타이틀 "체험권을 발급받을까요?" + **결제 기한 카드**(퍼플 틴트, 중앙: "지금 발급하면" → **"{M월 D일 (요일) 오후 H:MM}까지 결제"** 퍼플 bold(`fmtKoDateTime(now+72h)`, 마스크 `SBUI.dateTime`) → "발급 후 72시간 · 결제 전 QR을 제시해주세요") + 3행(참여 채널/채널 등급/지원 금액 — 라벨 좌 muted·값 우) + **"ⓘ 꼭 확인해주세요" 회색 카드**(불릿 4: 사용 전 취소 가능/재신청 12시간 뒤/연장·복구 불가/리뷰 이용 후 7일) + [취소]·[발급받고 체험권 보기] → `POST /api/passes`(M27~M30 가드). 발급 시 consumedSlot 기록 → 체험권으로 이동. | 2026-07-08 시안 | ✅ |

### CP_R_REQ5 · 체험권 목록 (`/r/passes`) · 상세 (`/r/passes/[id]`) — 7-상태 ★핵심

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_R_REQ5_0 | 목록 개편 (2026-07-08 시안) | 헤더 = **[방문형 \| 기자단]** 세그먼트 타이틀(기자단은 MVP 제외 시 회색 비활성, 과거 발급분 있으면 전환) + 검색(→R-14)·알림. 서브 탭 **[체험권/리뷰작성]**(퍼플 언더라인) — 체험권 탭=active·취소·만료(칩: 전체/사용가능/취소/만료, 검정 활성 pill) / 리뷰작성 탭=작성 대기·검수 중·완료·반려(칩 동일 문법). **카드**: 썸네일(76px)+가게명 1줄+"{채널} · {등급}등급 적용"+"{지원금} 지원"+우상단 상태 뱃지(사용가능=그린/작성 대기=옐로/취소·만료=회색/반려=레드). 상태별 액션 — active: 퍼플 보더 + 점선 구분 "유효기간 {M월 D일 (요일) 오후 H:MM}까지"(`SBUI.dateTime` 마스크) + [참여 취소](2단 확인, `CancelPassButton variant=row`)·[체험 정보](매장 상세) + full [체험권 보기] / cancelled: [체험 정보]+"취소 12시간 뒤 재참여" 안내 바 / expired: [체험 정보] / used: "리뷰 마감 {일시}까지"+[리뷰 작성하기] / review_submitted: 검수 중 바 / rejected: 사유 1줄+[수정 재제출] / completed: 검수 통과 표기. just_issued 퍼플 보더+배너·pending 배너·쿠키 stopgap 유지. | `PassesView.tsx` | ✅ |

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_R_REQ5_1 | active 티켓 (2026-07-08 시안 개편) | 헤더 ←+중앙 "체험권". **[▦ QR 스캔 \| T 코드 입력] 세그먼트**(활성 퍼플 filled, 기본 QR — `PassTicket.tsx`). 공통: **요약 카드**(가게명 1줄·"{채널} · {등급}등급 적용"·우측 퍼플 "{지원금} 지원", 부스트 보유 시 "🎁 초대 보상 부스트 포함" 1줄) + **남은시간 pill**(🕐 + `HH:MM:SS` 카운트다운, brandSoft). | 본인 아니면 404 | ✅ |
| CP_R_REQ5_2 | QR 스캔 탭 (경로 B) | QR(pass 8자 코드) + "결제시 사장님께 보여주세요!" + 링크 "QR이 인식되지않나요? **코드 입력으로 진행**"(탭 전환). **실 결제 금액 입력란 없음 — QR 경로의 결제 금액은 사장님 스캐너(`/o/scan`)에서 입력**(시안 확정). | 스캔 경로 B | ✅ |
| CP_R_REQ5_3 | 코드 입력 탭 (경로 A) | **4자리 입력 박스 4칸**(숨김 input + 현재 자리 퍼플 포커스) — "사장님, 매장 확인 번호 4자리를 입력해주세요!"(**코드는 화면 비노출** — 올바른 입력 = 사장님 확인) + **실 결제 금액(선택)** input + "미입력시 지원금 한도 적용됩니다." + 금액 입력 시 **계산 표**(퍼플 틴트: "적용 지원금 −{min(결제,지원금)}원" / "실제 고객결제 {결제−적용}원") + **[사용처리]**(4자리 완성 시 활성) → `POST /api/passes/use-by-code`(불일치 M9). | — | ✅ |
| CP_R_REQ5_4 | 참여 취소 | [참여 취소] 트리거(상세 텍스트/목록 행 버튼) → **중앙 확인 모달**(2026-07-08 시안): "같은 캠페인의 경우 재신청은 12시간 뒤부터 가능해요. 신중하게 선택해주세요." + **[취소할게요]**(회색, 실행)·**[계속 사용할게요]**(퍼플, 닫기) → `POST /api/passes/cancel` → cancelled+슬롯 복구. **노쇼 미집계**. | active 한정 | ✅ |
| CP_R_REQ5_5 | used 리뷰 마감 | 사용 완료 카드(결제·지원액) + **usedAt+7일 카운트다운**(dhm — 실제 마감 일시 기준). 기한 경과 시 제출 차단(M15). | — | ✅ |
| CP_R_REQ5_6 | 리뷰 폼(R-05) | ① 채널 고정 표기(**재선택 불가**) ② URL input ③ **자가 점검 전용** — 첫 항목 "광고 표시 문구를 게시물에 포함했어요"(서버 필수 M14) + 채널별 조건 전 항목. **광고 문구 원문·[복사] 버튼은 매장 상세(CP_R_REQ4_6)로 이동** — 폼에는 미노출, 하단 "조건·광고 문구 다시 보기 (매장 상세) →" 링크 제공. **모두 충족 시** 제출 활성. | adNoticeConfirmed | ✅ |
| CP_R_REQ5_7 | rejected 재제출 | **rejectReason(≤500자) 원문 노출**. 재제출 = resubmitCount<1 & rejectedAt+7일 이내(기자단 캠페인 종료 전). 충족 시 폼 재노출, 미충족 시 고객센터 안내(M16/M17). 2회째 종착. | — | ✅ |
| CP_R_REQ5_8 | 상태 라벨·스윕 | 7-상태 색·문구 분기. 스윕: 만료 확정(72h)+슬롯 복구+noShow+양측 알림 / 리뷰 7일 초과 / 만료 6h 전 리마인드. 만료 카피에 "연장·복구되지 않으며" 명시. | `pass-lifecycle.ts` | ✅ |

### CP_R_REQ6 등급 · CP_R_REQ7 혜택 · CP_R_REQ8 MY · CP_R_REQ9 기자단

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_R_REQ6_1 | 채널별 등급 카드 | 등급 탭에 채널(블/인/틱) 순회 — 연동 시 배율%+배지, 미연동 표기. 혜택 사다리(S~N)+진입 조건(**월간 등급 점수 컷** — S 90+운영팀 부여/A 70/B 50/C 30) 공개. | 기준 투명 | ✅ |
| CP_R_REQ6_2 | 월간 재평가·점수 분해 | 매월 말(KST) 직전 월 재평가(`sweepMonthlyRegrade`, lazy 스윕·소급 없음): **GS = 0.70 지수 + 0.20 성실 이행 + 0.10 상생지수 − 패널티**, 월 ±1등급, S 자동 부여 없음(후보 기록). 등급 탭에 **점수 분해 카드**(지수/성실/상생 3행 + 패널티 행(있을 때만, 에러 틴트) + GS 합계) — 점수는 `00점` 마스크, 가중치(70/20/10%)는 원문. 표본 부족 시 "지수 중심 평가"·이력 없음 시 첫 재평가 안내. **다음 재평가일**(이번 달 말일, `0000.00.00`) + "월 변동 폭 ±1등급" 병기. 리뷰 품질 요소 없음(반려 종착만 패널티). | 운영정책서 §10 | ✅ |
| CP_R_REQ6_3 | 등급 변동 이력 | `gradeHistory` 종합 요약(channel 미지정) 최근 6건 — 연월(`0000.00`) + from→to `GradeBadge` + GS/상태 라벨(활동 없음·등급 유지 / 표본 부족·지수 중심) + 🤝(상생 충족 월). 등급 변동 시 알림(상승/하락 카피 분리, link `/r/grade`). | — | ✅ |
| CP_R_REQ6_4 | 상생 리뷰어 뱃지 | 월 재평가 **W≥60 & 완료 3건↑** → 🤝 뱃지(유예 1개월, 2개월 연속 미달 회수·알림). 등급 히어로+MY 프로필에 brandSoft 칩 노출. **표시용 신뢰 표식 — 배율·참여 조건 무영향**, 프로모션 발생 시 우선 지급 대상(정책 명문화, 시스템은 추후). 안내 카피(원문주의): "상생지수는 추가 결제의 **비율과 빈도**만 반영해요. 결제 금액 자체는 반영되지 않으며, 리뷰까지 완료한 체험만 집계됩니다. 상생지수만으로는 등급이 오르지 않아요(가중치 10%)." | P1·P4 준수 | ✅ |
| CP_R_REQ7_1 | 초대 박스 카드 | 혜택 탭 상단 brand 그라디언트 — 누적 초대 N명+진행 바. 박스 **일반(0~2)/실버(3~4)/골드(5+)**. [친구에게 쏘기]. | — | ✅ |
| CP_R_REQ7_2 | 라이브 카운터 | "오늘 박스 N개가 열렸어요" — **실제 발행 보상만**(조작 없음). liveStream 비면 렌더 안 함. **10초 폴링**. | 표시광고법 | ✅ |
| CP_R_REQ7_3 | 내 보상 | 보상 3종만(부스트/멤버십할인/quota보너스). 미사용/사용완료 칩. **전부 실사용 경로**. | cash·spotlight 폐기 | ✅ |
| CP_R_REQ8_1 | 법적 고지 | MY 하단 [이용약관]·[개인정보처리방침] 밑줄 링크. | 비로그인 접근 | ✅ |
| CP_R_REQ8_2 | 회원 탈퇴 | [회원 탈퇴] → **2단 확인**(계정·미사용 보상 즉시 삭제, 거래 기록 비식별 보존) → `DELETE /api/auth/account` → 홈. | 되돌릴 수 없음 | ✅ |
| CP_R_REQ8_3 | **채널 관리** (`/r/me/channels`, R-15 — 2026-07-10 신설) | 채널 3종(블/인/틱) 카드 — **연동**: 미연동 카드에서 URL(네이버는 필수 — 계정 인증에 블로그 주소 귀속)·수치 입력 → **[{프로바이더 로그인}으로 본인 인증하고 연동]** → `GET /api/sns/{kind}/start` → 본인 검증(REQ8_4) → 성공 시 연동+등급 재계산(`channelGradesFromSns`+`bestGrade`) → `?connected=` 성공 배너. **해제**: [연동 해제] → 중앙 모달("이 채널 캠페인에 새로 참여할 수 없고 종합 등급이 재계산돼요. 진행 중인 체험권은 유지돼요") → `POST /api/sns/disconnect`. **검증 상태 칩**: ✓ 본인 인증(oauth·그린) / ✓ 데모 인증(퍼플) / 미인증·자기신고(회색 — 가입 시 입력분, [본인 인증] 버튼 노출). MY 채널 섹션·등급 탭 미연동 행·매장 상세 '연동 필요' 라디오에서 진입. **[P1] 참여 게이트는 연동 여부뿐 — verified는 신뢰 표식(참여·지원금 무영향)** | 고객센터 수기 폐지 | ✅ |
| CP_R_REQ8_4 | **본인 채널 검증 (OAuth)** | 프로바이더 로그인으로 소유 검증: **네이버 로그인**(naver_blog — 프로필 API가 아이디를 반환하지 않아 계정 인증+입력 URL 귀속, 한계는 운영정책서 §13) / **페이스북 로그인**(instagram — Meta Graph, 비즈니스 계정+앱 리뷰 시 username·팔로워 자동 조회 best-effort) / **틱톡 로그인**(Login Kit v2, user.info.basic). 플로우: start(state CSRF 쿠키 10분+pending 쿠키) → 프로바이더 인가 → callback(state 검증→토큰 교환→신원 조회→연동 커밋) — **액세스 토큰 미저장(신원 조회 직후 폐기)**, 팔로워 수 조회되면 influence 자동 채택. **키(env) 미설정 프로바이더는 데모 검증 모드**(`/r/channels/verify` 자체 승인 화면 — verifiedVia="demo" 구분 저장, 실키 설정 시 `POST /api/sns/demo-verify` 403으로 우회 차단). env: `NAVER_LOGIN_*`/`META_APP_*`/`TIKTOK_CLIENT_*` (.env.example) | 콜백 URL 콘솔 등록 필요 | ✅ |
| CP_R_REQ9_1 | 기자단 작성/재제출 — **MVP 제외** | `PRESS_ENABLED=false`: 탐색 세그먼트 숨김·브리프 redirect·발급 403(M30). 기존 발급 건은 작성→URL 제출(pressSelfCheck)→검수·정산 마무리 허용. 반려 시 캠페인 종료 전 1회 재제출. | 추후 버전 재개 | 🔷 |
| CP_R_REQ10_1 | 관심 목록 (`/r/interests`) | **MY 하위 메뉴 "관심 목록"으로 진입**(BottomNav 아님). 필터 칩 [전체\|진행 가능\|마감](`?f=`) — 진행 가능=지금 발급 신청 가능(open)/마감=기간 종료·발급 불가. 행=experience-row 문법, **마감 항목은 목록에서 유지 + 썸네일 "마감된 체험" 오버레이 + 70% 투명도**. 탭→매장 상세(종료 캠페인도 열람 가능, 신청 불가). 저장 단위=**캠페인**. 빈 상태 "아직 저장한 체험이 없어요. 매장 상세의 ♡를 눌러 담아보세요." 소진→복귀로 목록에서 사라졌다 살아나는 구조 없음(상태는 렌더 시점 계산). | 2026-07-07 회의 신설 | ✅ |

---

## 9. 사장님 (Owner) 화면별 요구사항

### CP_O_REQ1 · 새 캠페인 생성 (`/o/campaign/new`) ★핵심

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_O_REQ1_1 | 매장 선택 | 본인 소유 매장 select + **[플레이스 URL로 매장 추가]**(확정 정책 5-1): URL/숫자 ID 파싱 → `POST /api/owner/stores` → `scrapePlace` 조회, 실패 시 매장명 수동 입력 폴백(M32/M33). Free 플랜도 URL 등록으로 캠페인 생성 가능(프리 배제 금지). | — | ✅ |
| CP_O_REQ1_1a | 캠페인명 | **내 관리용 제목 입력(선택, ≤40자)** — 미입력 시 매장명 자동(확정 정책 7). 예: "신메뉴 출시 기념 체험단 모집". **체험자 화면은 항상 매장명 중심 노출**(체험권 상세 서브타이틀은 지역·카테고리로 대체). 사장님 홈 카드에는 캠페인명 표시. | — | ✅ |
| CP_O_REQ1_2 | 월간 모집 현황 | "월 한도 N·사용 N·보너스 +N·잔여 N". 한도 **Free 5/Basic 15/Standard 50/Premium ∞**. quota_bonus 가산. **초과 시 [생성] 비활성**. | — | ✅ |
| CP_O_REQ1_3 | 총 모집 인원 | 숫자 input **1명 이상**. `distributeQuota` **전 플랜 균등 배분**(확정 정책 8-3 — 플랜별 우선 등급 배분(부스팅) 제거). 버킷은 내부 기록[P1], 사장님 화면 비노출. M19. | — | ✅ |
| CP_O_REQ1_4 | 지원금 | 숫자 input = **S등급 100% 기준치**. 실지급은 채널 등급 배율. "매장 직접 할인·별도 정산 없음" **상시 고지**. | — | ✅ |
| CP_O_REQ1_5 | 4자리 코드 | **숫자 4자리 필수**(`^\d{4}$`), 중앙정렬 대형 input(tracking 0.4em). 진행 중 캠페인 간 **중복 차단**. **체험자 미노출**. M20/M21. | — | ✅ |
| CP_O_REQ1_6 | 필수 채널 | 블/인/틱 **1개 이상**(다중), 중복 제거. 0개 M22. | — | ✅ |
| CP_O_REQ1_7 | 강조 키워드 | 쉼표 구분→**최대 5개·각 20자** 칩 미리보기(초과 절삭). | — | ✅ |
| CP_O_REQ1_8 | 필수 메뉴 | [＋추가] 동적 — 메뉴명+가격(원,선택). **선택 입력 · 최대 5개**(폼+API 이중 제한, 확정 정책 6). 현장 강제 없음 — 미이행은 운영 검수 단계에서 처리. | — | ✅ |
| CP_O_REQ1_9 | 매장 소개 | textarea **최대 500자+카운터**(서버 절삭). | — | ✅ |
| CP_O_REQ1_10 | 생성 제출 | [캠페인 생성]→`POST /api/campaigns`. 즉시 활성. 한도 초과분 quota_bonus **만료 임박순 소진**. | — | ✅ |

### CP_O_REQ2 사용 처리 · CP_O_REQ3 후기 모니터링 · CP_O_REQ4 멤버십

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_O_REQ2_1 | QR 스캔 | 카메라 뷰(html5-qrcode)로 QR(8자) 스캔→단건 조회. 권한 거부 시 **4자리 폴백**. | — | ✅ |
| CP_O_REQ2_2 | 4자리 조회 | 숫자 input → useCode 최근 발급분 조회. M10. | — | ✅ |
| CP_O_REQ2_3 | 조회·사용 처리 | 결과 카드 **익명 #last4만**(개인정보 비노출). 결제 금액 input → `POST /api/passes/use`. 적용=min(결제,한도+부스트). M11~M13. | — | ✅ |
| CP_O_REQ3_1 | 조회 전용 모니터링 | 상태 뱃지(검수 중/통과/반려). **직접 검수 불가**(approve 410). 이의는 채널톡 단일 경로. **체험자 등급·실명 비노출**(익명 #last4만 — 확정 정책 8·10). | — | ✅ |
| CP_O_REQ4_1 | 플랜 변경·고지 | 4플랜 카드 — **차이는 월 모집 팀 수뿐**(Free 5/Basic 15/Standard 50/Premium ∞, 확정 정책 4). "A/S등급 우선" 등 부스팅 표기 없음(8-3). 즉시 정책 적용. "PG 연동 전 운영팀 청구·미납 시 Free 조정" + 환불/해지(약관 §10). | PG §12.3 | 🔷 |
| CP_O_REQ5_1 | 사업자 인증 (신규) | 가입 시 **사업자등록번호 10자리 필수**(형식 검증 M31) → `bizStatus: pending`. **인증 대기 화면**이 사장님 화면 전체를 대체(캠페인 생성 등 차단) — "영업일 2~3일 이내 인증 완료 안내" + 고객센터. 운영팀 인증(`POST /api/admin/owners/verify`) 시 verified + 알림 → "인증된 사장님" 권한으로 접근 허용. 구버전 계정(bizStatus 없음)은 verified 간주. | 확정 정책 9 | ✅ |
| CP_O_REQ6_1 | 등급 비노출 원칙 (신규) | 사장님 화면 전면에서 체험자 등급·실명 제거(확정 정책 8·10): 홈 캠페인 카드 = **방문 예정/방문 완료/총 모집 3분할만**(등급 버킷 그리드 삭제) · 사용 로그·후기·스캔 결과 = 익명 #last4만(스캔은 실명 노출 버그 정정, lookup API도 익명 ID만 반환) · 참여 알림 익명화 · 리포트 등급별 ROI 삭제(채널별 분포 유지). 등급 데이터는 어드민 전용. | 응대 차별 방지 | ✅ |

---

## 10. 운영팀 (Admin) · 11. 공통 (Common)

### CP_AD_REQ1 · 후기 검수 콘솔 (`/admin/reviews`)

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_AD_REQ1_1 | 인증 게이트·통계 | admin 아니면 `/admin/login` redirect. 상단 통계(대기 N·최근 7일 N). | 401 | ✅ |
| CP_AD_REQ1_2 | 검수 큐 | review_submitted **오래된 것 우선**. 익명 #last4·등급·종류·채널·시각·[게시물 열기↗]·자가점검 칩. 재제출 재등장. | — | ✅ |
| CP_AD_REQ1_3 | 통과 | [검수 통과]→`decide{approve}`→completed+completedReviews+1+양측 알림+제거. | M24 | ✅ |
| CP_AD_REQ1_4 | 반려 | [반려]→사유 input(**≤500자**, placeholder "체험자 화면에 그대로 표시…")→`decide{reject,reason}`. rejectReason·rejectedAt 보존, 미입력 시 "작성 조건 미충족". | M25 | ✅ |
| CP_AD_REQ1_5 | 검수 기준 보조 (신규) | 검수 카드에 캠페인 **강조 키워드 칩**(#키워드, brandSoft) 표시 — "사장님 입력 키워드 포함 여부" 수기 확인 지원(확정 정책 11). 자가점검 칩(채널 조건)과 병기. 자동검수 미도입 — 수기 원칙. | — | ✅ |

### CP_AD_REQ2 회원 · CP_AD_REQ3 캠페인 · CP_AD_REQ4 사장님(인증) — 어드민 확장 (확정 정책 12)

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_AD_REQ2_1 | 회원 조회 (`/admin/members`) | 체험자 목록 — 등급 배지·닉네임·이메일·완료/노쇼·🤝 상생 뱃지·S 후보 표시. 행 확장(details)으로 ① **월간 재평가 이력**(월·from→to·GS 분해 I/F/W/P) ② **작성 리뷰 목록**(매장·상태·[열기↗]). 등급·GS는 어드민 전용 노출. | — | ✅ |
| CP_AD_REQ3_1 | 캠페인 조회 (`/admin/campaigns`) | 전 캠페인 목록 — 캠페인명·매장·사장님·유형·노출 상태(모집 중/발급 마감·진행 중/종료 = campaignExposure)·모집 현황·마감일·지원금. **등급별 버킷(S/A/B/C used/quota)은 여기에만** 표시(내부 배분 기록). | — | ✅ |
| CP_AD_REQ4_1 | 사장님·인증 큐 (`/admin/owners`) | **사업자 인증 대기 목록**(매장명·이메일·사업자번호 000-00-00000·신청 시각) + [인증 완료 처리] → verified+알림. 하단 인증된 사장님 목록(플랜·사업자번호). | 확정 정책 9 | ✅ |
| CP_AD_REQ0_1 | 어드민 탭 내비 | 상단 고정 탭 4종: 검수 / 회원 / 캠페인 / 사장님 (`AdminTabs`, 활성=퍼플 보더). | — | ✅ |

### CP_C_REQ1 법적 고지 · CP_C_REQ2 환영 박스

| 기능 ID | 기능명 | 기능내역 | 비고 | 개발 |
|---|---|---|---|---|
| CP_C_REQ1_1 | 이용약관 | 비로그인 접근. 15조(정의·등급·발급/취소/만료·리뷰 의무·지원금 부담 주체·초대 보상·환불/해지). 상단 약관↔개인정보 탭. | 전자상거래법 | ✅ |
| CP_C_REQ1_2 | 개인정보처리방침 | 수집 항목·목적·보유(거래 5년 비식별)·제3자·파기·권리·보호책임자. | 개인정보보호법 | ✅ |
| CP_C_REQ2_1 | 환영 박스 | 가입 직후 슬롯머신 **0.6s shake→1.8s spin→2.2s reveal**+컨페티. 결과 카드(보상 1건: +50% 부스트 또는 멤버십 50% 할인)+"사용 시/결제 시 자동 적용·기준 초과 없음" 고지+CTA 2개. | — | ✅ |

---

## 12. 정량 제약 요약 (디자이너·FE 빠른 참조)

| 항목 | 제약 | 항목 | 제약 |
|---|---|---|---|
| 비밀번호 | 최소 6자 | 사용 기한 | **발급 후 72h**(연장·복구 불가, 만료 6h 전 리마인드) |
| 연동 채널 | 3종, 0개 허용(N) | 리뷰 제출 | **이용 후 7일** |
| 등급 임계 | A≥50k/B≥10k/C≥1k(블로그×1.2) | 반려 재제출 | 1회, 반려 후 7일 |
| 지원금 배율 | S100/A80/B60/C40/N10, 100원 | 세션 | httpOnly·secure 30일 |
| 부스트 상한 | 기준 지원금(S100%) | 월 모집 한도 | 5/15/50/∞(+quota_bonus) |
| 강조 키워드 | 최대 5개·각 20자 | 멤버십 | ₩0/13,900/25,900/38,900 |
| 매장 소개·반려 사유 | 최대 500자 | 초대 토큰 | 8자, TTL 14일, 1회용 |
| 사용처리 코드 | 숫자 4자리(미노출) | 박스 등급 | 0~2/3~4/5+ (+10/20/30%) |
| 토스트 | 3초 | 카운터 폴링 | 10초(실데이터) |
| 동시 보유(active) | **최대 5장** | 취소 후 재신청 | 동일 캠페인 **12시간 제한** |
| 걸어서 갈 수 있어요 | **반경 3km** | 캠페인 노출 | 발급 소진 ≠ 종료(살아있는 체험권 기준) |
| 거래 기록 보존 | 5년 비식별 | 뷰포트 | 480px, 탭 타겟 44px |

---

## 13. 변경 이력

| 버전 | 일자 | 내용 | 작성자 |
|---|---|---|---|
| 1.19 | 2026-07-10 | **데모 지도 전국 클러스터 시작 화면 보강** — `scope=all` 진입 시 실지도(줌 7 클러스터)만 지원하고 데모 지도(지도 키 미연동)는 개별 핀 투영으로 시작하던 누락 정정: 데모 지도도 **시도별 건수 클러스터(퍼플 마커)로 시작**, 마커 탭=해당 시도 핀으로 "확대"(줌 대응), "{시도} · N곳 · 전국 보기" 칩·GPS 리센터로 클러스터 복귀 (REQ3_8). 집계는 실지도와 동일(좌표 최근접 시도, `nearestSido`). | PO |
| 1.20 | 2026-07-10 | **SNS 채널 연동·해제 + OAuth 본인 검증** — 채널 관리(`/r/me/channels`, R-15) 신설: 미연동 채널 연동·연동 채널 해제(중앙 모달·등급 재계산·진행 중 패스 유지)를 셀프 서비스화(고객센터 수기 폐지, REQ8_3). **본인 소유 검증(REQ8_4)**: 네이버 로그인/페이스북 로그인(Meta Graph)/틱톡 로그인(Login Kit v2) OAuth 직접 구현(state CSRF·토큰 미저장·팔로워 자동 반영 best-effort), 키 미설정 프로바이더는 **데모 검증 모드**(자체 승인 화면, verifiedVia 구분·실키 시 데모 경로 403). `SnsAccount`에 verified/verifiedVia/providerAccountId/accountName. MY·등급 탭·매장 상세 '연동 필요'에서 채널 관리 진입. env: NAVER_LOGIN_*/META_APP_*/TIKTOK_CLIENT_*. [P1] 참여 게이트는 연동 여부뿐 — verified는 신뢰 표식. | PO |
| 1.18 | 2026-07-10 | **지역 필터 3상태화** — 필터 시트 지역을 2상태(현위치/지역)에서 **미선택(기본)/현위치/지역** 3상태로 정정(REQ3_6): 기존엔 `area=null`이 "현위치 활성"으로 표시되어 미선택 상태가 없었음. **미선택 = 정식 기본값**(전국 기준·뱃지 미계상 — 탐색 탭·`scope=all` 진입 공통, REQ3_8), **현위치 칩은 재클릭으로 해제**(토글), 현위치↔지역 상호 배타, [초기화]=지역 미선택(기존 '홈 기본 지역 복귀'에서 변경), `?loc=me` URL로 현위치 선택 재진입 유지. | PO |
| 1.17 | 2026-07-10 | **'전체 리스트 → 더 둘러보기' 진입 스코프 검토·정정** — 홈 '전체 리스트' 더 둘러보기를 `?area=` 전달(지역 3km 포커스)에서 **`?scope=all`(전국 축소 시작)**으로 정정: §6-4 확정대로 대한민국 전체 가시권(줌 7)에서 시도 클러스터를 즉시 노출, '걸어서 갈 수 있어요'(지역 스코프)와 진입점 차별화(REQ3_8 진입점 매트릭스 신설). **반경 3km 필터는 지도 전용** 원칙 명문화(REQ3_7 — 목록 전환 시 리스트에 미적용, 리스트는 필터 시트 기준). REQ3_3(추천순 기본)·REQ3_6(draft 필터 시트) 표를 v1.16 구현 기준으로 현행화. | PO |
| 1.16 | 2026-07-10 | **체험 서비스 추가 작업 리스트 반영 (dev)** — ① **§6.1a 상태별 정의 매트릭스** 신설(7 실상태 + 파생 표시 2종 `overdue`/`resubmit_expired` × 표시명/안내/CTA/전이/알림/패널티/노출 — `pass-display.ts`, PassStatus 비확장·'방문 예정/노쇼/패널티 적용' 상태 미도입 명시) + 체험 신청 상태 6종(신청 완료 배너·12h 쿨다운 CTA·issued_out "체험권 일시 소진" 안내 — `campaignExposure` 재사용) ② **사용 오류**: 코드 오입력 5회→10분 잠금(M34/M35), 스캔·티켓 네트워크 오류 [다시 시도](M37), 카메라 실패 안내(M38), 스캔 비-active 한글 안내, 인증 실패 상태 불변 원칙 ③ **리뷰 정책**: 게시 유지 **전 채널 90일 통일** + 자가점검에서 분리한 **별도 필수 동의**(keepAgreed, M36 — "제출한 리뷰를 등록일로부터 90일 이상 유지하는 데 동의합니다"), 리뷰 마감 24h 전 알림, rejected 날짜 라벨 "재제출 기한", 발급 모달에 감점 사전 고지 불릿 ④ **추천순**(REQ3): 기본 정렬 = 사장님 플랜 랭크(Premium>Standard>Basic>Free)→최신순, issued_out 최후순위 — P1 무관(멤버십 마케팅 혜택, 운영정책서 §3) ⑤ **지역 연동**(REQ2/REQ3): 홈 `?area=`→탐색 행정 기준점 3km 포커스·거리 기준점, 동적 타이틀 "{시군구}에서 갈 수 있어요", 직선 거리 캡션, "참여 중" 뱃지, 홈 전체 리스트 추천순 30개 ⑥ **필터 시트**(REQ3_6 개정): draft+[초기화/적용하기], 전체 칩, **지역 항목**(LocationSheet 재사용·탐색 한정), `?ch=`/`?area=` URL 유지, 필터 개수 뱃지 ⑦ 전국 축소 시 **시도 클러스터**(건수 마커·탭 줌인·재검색 억제) ⑧ 검색 0건 추천 4개·마감 임박 D-3 표기 ⑨ 취소 모달 "방문을 취소하시겠어요?" 카피(실정책 3항: 즉시 회수·12h 재신청·무패널티) ⑩ 기자단 검수 SLA 잔존 표기 정리·약관 90일/영업일 3일 통일. SEED_VERSION 1017(useCode "1234"·파생 상태 데모·플랜 다양화 사장님). | PO |
| 1.15 | 2026-07-08 | **체험권 상세·취소·리뷰작성 탭 시안 반영** — ① active 티켓을 **[QR 스캔\|코드 입력] 세그먼트**로 개편(REQ5_1~5_3, `PassTicket.tsx`): QR 탭은 금액 입력 제거(사장님 스캐너 전담), 코드 탭은 4칸 입력+실 결제 금액(선택)+계산 표(적용 지원금/실제 고객결제)+[사용처리] ② 참여 취소를 **중앙 확인 모달**로(REQ5_4 — "재신청은 12시간 뒤…신중하게", [취소할게요]/[계속 사용할게요]) ③ 리뷰작성 탭(REQ5_0): 칩 라벨(작성 대기중/검수중/검수완료/반려), 작성 대기 중=퍼플 뱃지+보더+[체험 정보]·[리뷰 제출], 검수중 안내 바, 반려=리뷰마감(반려 후 7일)+[리뷰 제출], 검수완료=회색 뱃지+[체험 정보]. **검수 SLA 표기 통일: "최대 72시간" → "영업일 기준 최대 3일"**(동일 취지 카피). | PO |
| 1.14 | 2026-07-08 | **체험자 4개 화면 UI 개편(디자이너 시안 반영)** — ① 현위치 설정을 페이지→**바텀시트**로(REQ2_1a: 안내 배너·**최근 선택** localStorage 칩·2단 리스트, `/r/location`은 redirect 안전망) ② **R-14 검색 화면 신설**(REQ2_6: 퍼플 pill input·최근 검색어 칩 → `/r/explore?q=`) ③ **발급 확인 모달 개편**(REQ4_8: 결제 기한 카드 "{일시}까지 결제"·3행·"꼭 확인해주세요" 불릿) ④ **체험권 목록 개편**(REQ5_0 신설: 방문형/기자단 세그먼트·체험권/리뷰작성 탭·상태 칩·썸네일 카드·상태별 액션(참여 취소/체험 정보/체험권 보기/리뷰 작성하기)). 유틸: `src/lib/dates.ts fmtKoDateTime`(요일·12시간제)·`recent-local.ts`(localStorage 최근 목록)·`SBUI.dateTime` 마스크. | PO |
| 1.13 | 2026-07-08 | **최종 확정 정책사항 반영 (dev 브랜치)** — ① 홈 3-stat 라벨 정정(작성할 리뷰·발급받은 체험권, REQ2_3) ② 지역 기준점 반경 3km 좌표 필터(REQ2_3a, `geo.ts` 신설 — 빈 결과 버그 해소) ③ 검색 확장(주소·강조 키워드+`?q=`, REQ3_1) ④ **'이 지역 재검색' 신설**(REQ3_7 — 500m 이동 감지→3km 재계산, 실지도/데모 동일 UX) ⑤ **사장님 등급 비노출 원칙**(CP_O_REQ6_1 — 홈 버킷·로그·후기·스캔(실명 정정)·알림·리포트 ROI 전면 제거, 어드민 전용) ⑥ 부스팅 제거(8-3 — priorityGrade 폐기·균등 배분·멤버십 문구 정리) ⑦ 캠페인명 부활(REQ1_1a — 내 관리용·체험자는 매장명) ⑧ 필수 메뉴 최대 5개(REQ1_8) ⑨ URL 매장 등록(REQ1_1, M32/M33) ⑩ **사업자 인증**(CP_O_REQ5_1, M31 — pending 가드+수기 인증) ⑪ 어드민 확장(CP_AD_REQ0~4 — 탭·회원·캠페인·인증 큐·검수 키워드 보조). **N등급 현행 유지**(5단계·배율 10%). SEED_VERSION 1016. | PO |
| 1.12 | 2026-07-08 | **등급 월간 재평가 시스템** — 최초 등급 = "캐치랭크 지수 평가 모델 v1"(REQ1_5 명명, `indexScoreV1` 교체 경계). 매월 말(KST) 직전 월 재평가: **GS = 0.70 지수 + 0.20 성실 이행 + 0.10 상생지수 − 패널티**(리뷰 품질 요소 제외 — 반려 종착만 패널티), 컷 S90/A70/B50/C30, 월 ±1등급, S 자동 부여 금지(후보 기록). REQ6_2~6_4 신설(점수 분해 카드·변동 이력·**상생 리뷰어 뱃지** W≥60&완료3건↑·유예 1개월). 알림 매트릭스에 등급 변동·뱃지 2행. Pass `completedAt`·Reviewer `gradeHistory`/`winWinBadge`·DBShape `lastRegradeMonth` 신설, SEED_VERSION 1015(직전 월 활동 시드). 상세: 운영정책서 §10 · 데이터정책서 §1.10. | PO |
| 1.11 | 2026-07-08 | **광고 표시 문구 위치 이동(작성 전 사전 인지)** — 광고 문구 원문 박스·[복사] 버튼·공정위 고지 + 세부 조건 리스트(항목+힌트)를 **매장 상세 '리뷰 작성 조건'(REQ4_6)**으로 이동, "세부 조건은 리뷰 제출 화면에서 확인" 안내 폐기. 리뷰 폼(REQ5_6)은 **자가 점검 전용**으로 단순화(광고 문구 포함 여부는 첫 자가점검 항목, M14 서버 검증 유지) + 매장 상세 재확인 링크. 작성 후 수정이 발생하지 않도록 사전 인지 원칙 확립. | PO |
| 1.10 | 2026-07-08 | **거리 표기 개편** — "도보 00분" → **현 위치 기준 매장까지 거리**(`00m` / `0.0km` / `00km`, `formatDistance`)로 전 화면(탐색 행·지도 카드·관심 목록) 통일, SBUI 기본 마스크 `00m`. 지도: 카드 스와이프/핀 탭 시 **해당 핀으로 panTo 포커스 이동**, 현 위치 파란 점 + GPS 리센터 버튼 추가(REQ3_5). | PO |
| 1.9 | 2026-07-08 | 지도뷰 하단 리스트 영역을 **피크 바텀시트**로 재설계(REQ3_5) — 디폴트는 핸들+카테고리 탭+필터 아이콘까지만 노출, 위로 쓸어올리면(또는 핸들 탭) **목록 보기로 자동 전환**. 시트 내 리스트 스크롤 제거(목록은 리스트 모드가 전담). | PO |
| 1.8 | 2026-07-08 | **현위치 설정 개편(레퍼런스 반영)** — 홈 지역 드롭다운을 바텀시트에서 **페이지 전환(`/r/location` R-13)**으로 변경: 좌측 시도(도·특별시·광역시) 1차 레일 + 우측 전체·시군구 2차 리스트, 상단 [현위치로 설정]. 홈 상단에 **GPS(크로스헤어) 현 위치 갱신 아이콘** 추가(REQ2_1/2_1a). `src/lib/regions.ts` 정적 행정구역 데이터 신설. | PO |
| 1.7 | 2026-07-08 | **리뷰 작성 조건 개선(블로그 기준 상향)** — 네이버 블로그: **사진 15장 이상 · 본문 1000자 이상 · 90일 이상 게시 유지** 3항목으로 재정의(자가점검 키 photos15/body1000/days90, `CHANNEL_REVIEW_CONDITIONS` 가변 데이터 유지). 인스타·틱톡은 현행(30일 유지). 약관 제8조·제9조를 채널별 유지 기간(블로그 90일/인스타·틱톡 30일) 기준으로 정정. SEED_VERSION 1013. | PO |
| 1.6 | 2026-07-08 | 홈 '걸어서 갈 수 있어요'를 **1단 가로 캐러셀·최대 10개**로 변경(REQ2_4), 전체 리스트는 2단 그리드 유지(REQ2_5). 시드에 채널 조합 케이스 추가 — 블/인/틱 **3채널 모두 모집** 캠페인 1건 + **틱톡 단독 모집** 매장 1곳(틱톡 미연동 계정의 [연동 필요]·"SNS 연동 필요" 예외 노출용). SEED_VERSION 1012. | PO |
| 1.5 | 2026-07-07 | **회의 확정 사항 반영** — ① 정책 수치: 사용 기한 24h→**72h**(연장·복구 불가), 리뷰 **이용 후 7일**, 재제출 반려 후 7일, active 동시 5장(M28), 취소 후 12h 재신청 제한(M29), 종료 캠페인 발급 차단(M27). ② MVP 범위: 기자단 제외(M30, `PRESS_ENABLED`), 예약형 추후. ③ 홈: 지역 변경 바텀시트(?area=, '걸어서'만 변경), '걸어서' 반경 3km, 전체 리스트 최신 등록순. ④ 탐색: 기본 지도 보기, 지도 카드 캐러셀(거리순 스와이프, 매장명·카테고리·거리·지원금만), 통합 필터 바텀시트(REQ3_6 — 채널·카테고리 다중 선택·즉시 반영·↺ 초기화·채널값 상단 미노출). ⑤ 상세: ♡ 관심 목록 토글, 리뷰 조건 핵심 칩 단순화(가변 데이터), 등급 비교 문구 제거, 플레이스 링크 좌측 정렬 현행. ⑥ **CP_R_REQ10 관심 목록 신설**(`/r/interests`). ⑦ 노출 정책(발급 소진 ≠ 종료) §6.1 반영. ⑧ 표기 형식 규칙(§4 — 마스크 `000,000원`/`00건`/`00개`, ₩ 접두 폐지). | PO |
| 1.4 | 2026-07-07 | **디자인 시스템 v2 전면 교체** — §7.4를 "[체험단] 디자인시스템"(퍼플 #9333EA·Pretendard·파스텔 SNS 배지·헤어라인 카드) 기준으로 재작성, 정본을 루트 `DESIGN.md`로 지정. 프로토타입 UI 전면 리디자인 반영(홈 상단 위치 선택+프로모 배너+3-stat, 탐색 세그먼트 타이틀+카테고리 아이콘 칩+지도 보기 FAB+바텀시트, 매장 상세 info-strip+채널 라디오 카드+cta-bar). 화면설계서(Figma 보드)는 v1 기준 유지 — 미반영(지시). | PO |
| 1.3 | 2026-07-07 | **[P1] 등급 게이트 폐기** — 등급은 참여 자격이 아니라 혜택(배율)만 결정. M8 에러 폐기, 등급 부족 오버레이/CTA 등급 분기/`accessible` 정렬 제거, quota·consumedSlot 정의를 "배분 기록"으로 정정, 용어(등급) 원칙 명시. | PO |
| 1.2 | 2026-07-07 | 문서 체계 분리에 맞춰 정본 승격(`docs/sdd/` → `docs/`). 기준 문서를 PRD 단일에서 요구(PRD)/IA/운영정책/데이터정책/시나리오 세트로 재연결. 데이터 모델 상세 포인터를 데이터정책서로, 정책·법무 포인터를 운영정책서로 조정. | PO |
| 1.1 | 2026-07-06 | 자기완결화 — §1 용어, §2 권한, §3 IA/nav, §4 전역 UX(로딩/빈/에러/토스트/모달/키보드/접근성), §5 메시지 카탈로그(M1~M26), §6 상태머신·알림 매트릭스, §7 데이터모델·API·비기능·디자인 시스템·BM/법무 추가. 화면별 표에 데이터·에러코드 병기. | PO |
| 1.0 | 2026-07-06 | 최초 작성 — IA 전 화면 기능 요구사항 + 정량 제약 + 디자인 의도. | PO |
